node_modules/
.DS_Store
*.log
graphify-out/

# The pipeline run ledger — state of a run, not of the repository (same class as graphify-out/)
.task-pipeline/

# agent-sync
.env.agent-sync

.agent-sync/

# Python bytecode from the validator and its suites
__pycache__/
*.pyc

# Secrets — never committed. A key sat here untracked-but-unignored on
# 2026-08-16, one `git add -A` away from a public repository.
.env
.env.*
!.env.example
