# Search and reading update

Scope: all 14 indexable Skills URLs, including every pack, Harness, Agents and
Routing; coordinated with the Observatory article update in its own repository.
No launcher behavior, skill doctrine, version, or submodule pin changes.

Diagnose before changes: the public crawl finds one h1 and self-canonical on every
page and a real 404 response. Existing social cards and author nodes are present.
The prior audit's four linked-content/read-budget observations recur; that metric
is an engine-specific heuristic, not a ranking gate. Do not pad the one-skill page.

Changes: lead member pages with their actual purpose, simplify repeated harness
copy, move the redundant header follow link to its existing footer location,
retain the installation and repository actions, describe pack Open Graph pages as
websites, point author.url to the author's profile, add the missing Harness
breadcrumb and link its observation section to the concrete origin story. Add
large-image preview permission to indexable pages; preserve 404 noindex.

CONFIRMED: these markup/content states are directly observable. Any ranking or
AI citation effect is unmeasured. Google documents no special AI schema requirement.
Verify with generator tests, the repository gate, full sitemap crawl, DOM/browser
checks and exact-revision Pages deployment. Revert the source commit to roll back.
