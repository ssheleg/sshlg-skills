# Board — sshlg-skills

Everything this repository knows it owes, in one place. A carry-over that
leaves a run still open lands here with an id; `/task-pipeline checkup` reads
this file when no run is in flight, which is exactly when accumulated debt is
invisible.

**Priority is computed, not felt.** `P = blast × (1 + age_runs) / effort`,
where *blast* is who is hurt if it stays (3 = a user of the pack, 2 = the
operator of this machine, 1 = a future run of this repo), *age_runs* is how
many run stamps it has survived, and *effort* is rough size (1 = under an hour,
2 = a session, 3 = its own run). Recomputed at stage 10 rather than inherited,
so a row cannot keep a rank it earned when it was new.

| id | What | Source | Blast | Age | Effort | P | Status |
|---|---|---|---|---|---|---|---|
| B-01 | `agent-sync` releases fail on one letter: `release.yml` extracts notes with `awk '$0 ~ "^## " v'` while its CHANGELOG writes `## v1.5.2`, and the terminator `/^## [0-9]/` misses the same prefix. v1.5.0, v1.5.1 and v1.5.2 each tagged and died at `no CHANGELOG section` (run `31287012133`). npm has served 1.4.3 since 2026-08-03 while three tags claim otherwise. | 2026-08-10 run | 3 | 1 | 1 | **6.0** | **closed 2026-08-10** — fixed upstream, run `31374955487` green |
| B-02 | `agent-sync` `main` is red on its own validator: `FAIL: status verdict: 'status' reported normally on a setup 'check' fails` (run `31352513346`, 2026-08-10). No release may be cut from it until this is green, so B-01's fix alone does not ship anything. | 2026-08-10 run | 3 | 1 | 2 | **3.0** | **closed 2026-08-10** — fixed upstream, run `31374949511` green |
| B-03 | `graphify-out/` is neither tracked nor ignored in five members, so a graphify run leaves every one of them dirty and the umbrella reports five modified submodules. | 2026-08-10 run | 2 | 1 | 1 | **4.0** | **closed 2026-08-10** — `graphify-out/` added to `.gitignore` on `main` in all five (`agent-sync` 5df7fe1, `make-skill` 022e9da, `seo-aeo-audit` ea12acd, `sheleg-dev` 5ef953a, `agent-stack` 3a4ee92). `super-ux` deliberately differs and was left alone: it commits `graph.json` and `GRAPH_REPORT.md` so the next harvest can query them, ignoring only `.graphify_*` and `cache/`. The local `.git/modules/skills/*/info/exclude` entries stay until each pin moves past its tag onto a commit that carries the line. |
| B-04 | ~~`sheleg-dev` and `agent-stack` have no release tags at all~~ **closed 2026-08-11** — both now tag: sheleg-dev v0.3.0, agent-stack v0.2.0 (its first ever). Original text: `sheleg-dev` and `agent-stack` have no release tags at all — `git ls-remote --tags` returns nothing for either — so their pins are commits on `main`, `check_pins.py` has nothing to compare against, and their release workflows have never run once. | 2026-08-06 port | 2 | 2 | 2 | **3.0** | **closed 2026-08-11** |
| B-05 | **closed 2026-08-12** — `lib/backup.js`: every write to an agent instruction file is preceded by exactly one `protect()`, the copy is read back and byte-compared, and a copy that cannot be taken CANCELS the write rather than degrading to "wrote anyway". Copies go to `~/.sshlg-skills/backups/`, never beside the original — `~/.cursor/rules/` globs every `*.mdc`, so the obvious fallback (the file's own parent) is the one place a backup can be re-read as an instruction. Watched failing: with the gate removed, the fixture reports *the operator file was modified with no backup behind it*. Original text: the copy of `~/.claude/CLAUDE.md` exists because an agent chose to make one — twice now, once ten minutes before it was needed. That is a habit, not a mechanism. | C-09 (2026-08-06) | 2 | 2 | 2 | **3.0** | **closed 2026-08-12** |
| B-06 | C-07: four members do not delegate to `routers --member`; only `bin/super-ux.js:354` does. The bundle writes all eight routers without it, so nothing is broken — but a lone installer for those four writes no router at all. | C-07 (2026-08-06) | 1 | 2 | 2 | **1.5** | **closed 2026-08-12** — `task-pipeline` v1.49.1, `agent-sync` v1.9.0, `make-skill` v0.16.0 and `sheleg-design` v1.18.0 all delegate to `npx sshlg-skills routers --member <this>`. The row said four members and four was right: of the eight, five own a router and only `super-ux` delegated. The other three (`seo-aeo-audit`, `sheleg-dev`, `agent-stack`) own no router — `scope({member})` returns nothing for them — so an offer there would print "nothing to write" and be noise, not a fix. Isolation verified per member by damaging TWO sections of a real block: each installer repaired its own and left the other byte-for-byte. Both the resolvable and the absent-launcher paths were exercised, as was the non-interactive case, which records an opt-out and takes the backup. |
| B-07 | C-04: the CLI launcher has no `docs/ux/` chain. Waived at intake (D-09) in favour of REQ rows asserting exact output; the fixtures in `test/cli_config_test.js` and `test/drift_test.js` are what stands in for it. Revisit only if the command surface grows past what fixtures can describe. | C-04 (2026-08-06) | 1 | 2 | 3 | **1.0** | open — waived, revisit on growth |
| B-08 | C-05: no design-time track in `seo-aeo-audit`. Different repository; the `seo-llmo` router carries the rule, and the skill stays an audit. | C-05 (2026-08-06) | 1 | 2 | 3 | **1.0** | open — deliberate |

| B-09 | A channel fed once but absent from `defaultAgents` drifts forever, and no command repairs it. `kiro-cli` and `goose` each carried 12 of the family's 19 skills. | 2026-08-10 audit | 2 | 1 | 2 | **2.0** | **closed 2026-08-10** — both ids added to `defaultAgents`, and `update` now reconciles rather than only refreshing: `skills update <id>` is a no-op for a skill installed nowhere and reports success anyway, so refreshing alone could never deliver a new member. Proven by planting the never-installed state for `agent-orchestrator` and watching the old verb restore nothing and the new one restore all seven channels. v0.30.0. |

| B-10 | `sheleg-dev`'s `npm publish` step fails `ENEEDAUTH` on every release (run of v0.3.0, 2026-08-11). The GitHub release and tag land, so the member ships and `check_pins.py` treats it as GitHub-only and stays green — which is why nobody noticed. Either the package is meant to be on npm and the workflow's auth is wrong, or it is GitHub-only and the publish job should not run. Today the release is reported failed for a step nobody wants. | 2026-08-11 run | 1 | 1 | 1 | **2.0** | **closed 2026-08-12 — the premise no longer holds, verified.** `@ssheleg/sheleg-dev` is on npm at 0.4.1, exactly the pinned version, and its last release run shows `job publish: success`. The ENEEDAUTH run of v0.3.0 was fixed at some point without the row being updated. All eight members have `PUBLISH_NPMJS=true`, `RELEASE_ENABLED=true` and an `NPM_TOKEN` secret, and all eight publish. What the row was really pointing at turned out to be B-15. |

| B-11 | Four members' release workflows disagree on the CHANGELOG heading they extract, and a mismatch fails the job *after* the tag is public — the tag then looks delivered while nothing shipped. `super-ux` wants `## <version>` and silently failed on `## [0.34.1]` (2026-08-11); `agent-sync` lost three releases to the same class before someone noticed; only `sheleg-dev` accepts every shape this family has used. One accepting pattern, copied to all eight, removes the whole class. | 2026-08-11 run | 2 | 1 | 1 | **4.0** | **closed 2026-08-12** — one accepting pattern (`^## \[?v?<ver>\]?`, terminator requiring a full semver) copied to all eight. Verified against every member's real CHANGELOG, all four heading shapes, a subsection boundary, the `1.2.3` vs `1.2.30` prefix collision, and a missing section (empty, so the existing `test -s` aborts). The historical defect was replayed: the old `super-ux` pattern against `## [0.34.1]` produces empty output. Two things had to move with it — `agent-sync`'s plant searched the old string and stopped planting (standing instruction #6), and its spill check matched `^## v?\d` and could not see the bracketed form its own terminator was widened to stop at. **The `workflow` OAuth scope was never the blocker it was reported as:** the restriction applies to OAuth pushes over HTTPS, and these repositories push over SSH. |

| B-12 | **closed 2026-08-12** — `test/validate.py` now fails in both directions, planted against; `test/audit_bundle.py` is in the repo. Original: a member that gains a skill does not announce it, and the umbrella finds out by audit. `agent-stack` shipped `agent-evals` in v0.6.0; `skills.json` still declared one skill, so `install` and `update` would both have skipped it forever — the same class v0.30.0 fixed for members, one level down at skills. `test/validate.py` compares declared against shipped and PASSED, because it only fails on a declared skill that is missing, never on a shipped one that is undeclared. Make that check symmetric. | 2026-08-12 audit | 2 | 1 | 1 | **4.0** | **closed 2026-08-12** |
| B-13 | **`check_pins.py` asks about the world, not about the commit, and blocks CI anyway.** A commit does not become invalid because someone else released a member five minutes later — yet `validate.yml` fails on exactly that, and did four times on 2026-08-10..12 while another agent released the family continuously. Every one of those failures was correct about the world and wrong about the change under test. Two candidate fixes: move the pin check out of the blocking path into `audit_bundle.py` (it is a freshness question, like the audit's), or have the release workflow bump pins itself, which is also the retirement trigger written into standing instruction #5. | 2026-08-12 audit | 2 | 1 | 2 | **3.0** | **closed 2026-08-12** — `check_pins.py` now answers two questions with different exit codes: 1 when a pin names a version that was never published (the commit is wrong on its own terms, blocks) and 2 when every pin is real but one is not the newest (a member released mid-flight; warning plus a run-summary note). `classify` is pure with five offline cases and asserts its verdicts differ. All three codes were watched firing against real plants: `99.99.99` -> 1, a genuine older release `0.36.1` -> 2, restored -> 0, and the CI step's branching proven for each with a stub. |
| B-14 | **closed 2026-08-12** — `agent-stack`'s CI had been red on every push since v0.6.0, and the validator it accused was fine: the over-long-description plant replaced the literal `"token wallet".`, which left the file when the description was compressed to fit the token budget. The `replace` no-oped, nothing was planted, the validator correctly found nothing, and the step reported `ERROR: validator accepted a description past the 1024-char limit`. Verified pre-existing on `3a31dbb` at 00:16, before the commit that surfaced it. Re-anchored on the front matter's shape (`description: >-`) with an `assert` that it planted something; all six of that repo's negative self-tests then run locally and every one still detects its defect. | 2026-08-12 B-11 work | 1 | 1 | 1 | **3.0** | **closed 2026-08-12** |

| B-15 | **closed 2026-08-12** — the npm half of `check_pins` was inert for six of eight members. `candidates()` tries the `npm` field, the member name, then the repo basename, and rejects a name whose `repository` is not ours. `npm` was unset for every member, six publish as `@ssheleg/<name>`, and `task-pipeline` publishes as `task-pipeline-skill` while the bare `task-pipeline` on npm belongs to node-task. So six pins were never compared against the registry at all and were reported `(tag; not on npm)`; git tags happened to agree, so nothing went red — the exact failure mode the file's own docstring warns about ("they can all agree and all be wrong"). Fixed by declaring `npm` per member and having the validator compare it with the submodule's `package.json` name in both directions. Deriving `@ssheleg/<name>` would have passed today and broken on the two members already publishing under a third shape. | 2026-08-12 B-10 investigation | 3 | 1 | 1 | **5.0** | **closed 2026-08-12** |
| B-16 | Three members released while this run was in flight: `super-ux` 0.38.0, `sheleg-design` 1.21.0, `seo-aeo-audit` 0.16.0. `check_pins.py` classifies this as exit 2 — every pin exists, so v0.42.0 installs exactly what it advertises — but the bundle is one sweep behind the world. Bump the three together (`skills.json` → submodule pointer → README table), never one at a time: standing instruction #5 exists because fixing the member the log named made the same run fail twice. | 2026-08-12 run, pin sweep | 2 | 1 | 1 | **2.0** | **closed 2026-08-13** — all three moved in one sweep with `task-pipeline`: `super-ux` 0.38.0, `sheleg-design` 1.24.0 (five releases behind), `seo-aeo-audit` 0.16.0. Submodule pointer, `skills.json` and the README table moved together, and `npm test` reported no skill-declaration drift — the check B-12 added for exactly this. |
| B-17 | `npm run test:negatives` in `task-pipeline` reports one broken guard when run from the **submodule** checkout: the guard reads `git log -p`, the harness copies the repo to `/tmp` without a resolving `.git`, and a submodule's `.git` is a file pointing at a relative path. Reproduced on pristine `HEAD` (`c90835b`) with a submodule-style `.git`, so it is pre-existing and not this change; CI clones normally and stays green. The harness should copy or bind the git dir, or say plainly that the guard was skipped. **Re-measured 2026-08-13 (v0.46.0): it is TWO guards, not one** — *the high-water mark lowered in a COMMITTED change* and *a version number already spoken for by a tag*, both printing `fatal: not a git repository`. The original row counted the one a log named. CI clones normally and passed (20m30s) with both firing, which is what confirms the cause is the checkout and not the guards. | 2026-08-12 run, stage 6 · re-measured 2026-08-13 | 1 | 1 | 1 | **1.0** | open |
| B-18 | Pushing a change that touches `.github/workflows/` from a submodule checkout is refused: the submodule remote is HTTPS by invariant, and an OAuth push over HTTPS needs the `workflow` scope. Worked around per-machine with `git remote set-url --push … git@github.com:…`, which leaves `.gitmodules` HTTPS. The umbrella's retro says these repositories "push over SSH" — true of the source clones in `~/DATA/`, not of the submodule checkouts, and that distinction cost a failed push. Either set the push url in `install`/`update`, or write the distinction into `CLAUDE.md`. **Fired again 2026-08-13 in `seo-aeo-audit`**, whose PR touched `validate.yml`: the push was refused verbatim and the recorded workaround cleared it in one command, leaving the fetch url — and therefore the `.gitmodules` invariant — HTTPS. A row that predicts a failure and names its remedy is worth more than the ten minutes it saved here. | 2026-08-12 run, stage 7 · fired again 2026-08-13 | 2 | 1 | 1 | **2.0** | open |
| B-19 | The family wires its hooks into `~/.claude/settings.json`; `agent-sync` ships its own from the plugin's `hooks/hooks.json` and `task-pipeline` now does too. Two channels for one mechanism. The plugin channel removes the write to the operator's file entirely — the whole risk profile of this repository — and costs the per-hook off switch, which is exactly why `superpowers` is disabled on this machine. Decide it once, deliberately, rather than letting each member answer separately. | C-01 (2026-08-12) | 1 | 1 | 2 | **1.0** | open — **not decided by the 2026-08-13 artifact-root run (v0.46.0), on purpose.** That run added output to a hook that was already wired rather than a new channel, so the write to the operator's file did not grow; deciding the channel question inside a run about a directory name would be the opposite of deliberate. One fact for whoever takes it: `statusLine` cannot move to a plugin at all — none of the 36 installed `plugin.json` files declares such a field — so the channel stays double whichever way the hooks go |

| B-20 | **The stage-6 verdict is written by the agent the release gate is gating.** `task-pipeline` v1.50.0 refuses an outward release act while the ledger records no `stage: 6 … verdict pass` — and that line is typed by the agent, so the gate reads a claim by the party it constrains. A `PostToolUse` hook should record the OBSERVED exit code of the project's declared gate command, and the release gate should require the claim and the observation to agree. Not taken here because it changes another skill's ledger grammar (`templates/run.md`, `progress.md`, `loop-guard.md` all read those lines), and that deserves its own run rather than the end of a long one. | 2026-08-13 run | 2 | 1 | 2 | **2.0** | **closed 2026-08-13** — `hooks/gate-observer.sh` records the observed exit code of the declared gate command as a fifth ledger line, and the release gate requires the claim and the observation to agree. Dogfooding it against this repository's own ledger found a second weakness the fixtures had missed: the gate accepted ANY green observation, so an earlier green satisfied it over a later red. It reads the last one now, both directions fixtured. |
| B-22 | **The repository gate judged commits belonging to other repositories.** `.claude/settings.json` wires the gate for one project and the hook ran `npm test` in `CLAUDE_PROJECT_DIR` for any `git commit`, so a submodule commit was refused on the umbrella's verdict about a change it does not contain. It deadlocked a release live: the umbrella was red *because* the submodule had not shipped, and the submodule could not commit the fix because the umbrella was red. | 2026-08-13 run | 2 | 1 | 1 | **2.0** | **closed 2026-08-13** — a commit for this project must stage something in this project; that is the decidable form of the question, since the payload carries no shell cwd. Fixture: a commit with nothing staged here is not gated, plus the existing red-suite case now runs against a real repository with a real index. |
| B-23 | **`task-pipeline` v1.50.0's gate was keyed to `stage: 6` literally**, so a project whose flow has six stages — tests green at stage 4 — could never tag anything again. The same class as the rail's denominator, one day later and with worse consequences: a wrong rail misinforms, a wrong gate stops the work. | 2026-08-13 run | 3 | 1 | 1 | **6.0** | **closed 2026-08-13** — v1.51.0 resolves the tests stage from `pipeline.json` (`state: "tests"` or a declared `gate.command`), failing that from the ledger by name; unresolvable still refuses, with a reason saying how to become readable. Reproduced against a six-stage project first. |
| B-21 | Four more hooks the pipeline could own, proposed and not taken: `SubagentStop` appending `hand:` lines mechanically (its own audit compares them against `iter:`, and today one agent writes both sides from memory); `PreCompact` stamping the ledger at the boundary the ledger exists because of; `SessionEnd` marking a run abandoned so `checkup` can see it; and `PreToolUse` refusing source edits while the ledger sits at stages 0–4. Each is small; together they are a run. | 2026-08-13 run | 1 | 1 | 2 | **1.0** | **closed 2026-08-13** — `run-lifecycle.sh` covers the compaction boundary, an abandoned run and a subagent finishing, in ONE ledger shape rather than three. `build-gate.sh` asks before the product is edited ahead of the plan, resolving the build stage BY ROLE — the lesson v1.51.0 learned from the release gate, applied before it could repeat. The `hand:` half of the original row was **not** built and the reason is recorded: that line carries judgements only the agent holds, and a hook filling them in would fabricate the evidence it exists to provide. |
| B-22 | **`update` does not refresh the wired runtime, so every hook improvement is invisible on a machine that only runs `update`.** `syncRuntime()` lives inside `cmdHooks` and is called on `hooks install` alone; `bin/sshlg-skills.js:664` is the only reference to `runtimeDir`. Watched on this machine at v0.46.0: `npx sshlg-skills@latest update` brought all six plugins to their new versions and left `~/.sshlg-skills/runtime/lib/` at 24 modules against the package's 25 — `injectors.js` absent, so the SessionStart line REQ-07 shipped could never print until `hooks install` was run by hand. The whole v0.42.0 hook suite reaches machines the same way. | 2026-08-13 run, stage 8 | 3 | 1 | 2 | **1.5** | open |
| B-23 | The retro's standing instructions are cited BY NUMBER (two CHANGELOGs and a merged PR body now do it) while the file renumbers on every retirement. v0.46.0 left slot 3 vacant rather than renumbering, which keeps the existing citations true and makes the list non-contiguous — a workaround, not a fix. Either the ids become stable (never reused, never renumbered) or citations stop using them. | 2026-08-13 run, stage 10 | 1 | 1 | 1 | **1.0** | open |
| B-24 | **The code graph cannot be refreshed on this machine, so every run inherits a stale one.** `graphify . --update` exits with `no LLM API key found (25 doc/paper/image file(s) need semantic extraction)`; the existing graph survived untouched (715 nodes, `built_at_commit a20c402`) and is now **5 commits behind HEAD**. Stage 0 of this run already marked it *⚠ not trusted for reach until refreshed*, and stage 9 could not clear the marker. `--code-only` would index the code without a key but was NOT run: it risks replacing a graph that has doc nodes with one that does not, and a narrower graph presented as a refreshed one is worse than a stale one that says so. Either a key reaches the tool, or `--code-only` is proven non-destructive first, or the graph stops being part of stage 9 here and says so. | 2026-08-13 run, stage 9 | 2 | 1 | 1 | **2.0** | open |

**Open: 4** — B-07 and B-08 (both waived deliberately) plus B-17 and B-18. B-16 and B-21 closed on 2026-08-13; B-20, B-22 and B-23 opened
and closed inside the same day's runs. Counted by reading the Status column of the table above,
not carried across from the previous edit of this file.

**Open: 8.** B-01 and B-02 opened and closed inside the same run: they live in
a repository another agent held throughout, so this run diagnosed them and
wrote down the evidence instead of touching the code. The other agent shipped
both fixes while it was in flight, and `agent-sync` is pinned at 1.7.0 here as a
result. The finding was still the deliverable — it just had a shorter life than
expected.

**Closed by the 2026-08-10 run:** C-06 — the umbrella now has `CLAUDE.md` house
rules of its own, which is the thing every member already had. B-01, B-02 and
B-03 closed the same day.
