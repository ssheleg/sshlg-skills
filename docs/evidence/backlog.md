# Board — sshlg-skills

Everything this repository knows it owes, in one place. A carry-over that
leaves a run still open lands here with an id; `/task-pipeline checkup` reads
this file when no run is in flight, which is exactly when accumulated debt is
invisible.

**Priority is computed, not felt.** `P = blast × (1 + age_runs) / effort`,
where *blast* is who is hurt if it stays (3 = a user of the pack, 2 = the
operator of this machine, 1 = a future run of this repo), *age_runs* is how
many run stamps it has survived, and *effort* is rough size (1 = under an hour,
2 = a session, 3 = its own run). Recomputed at stage 10 rather than inherited,
so a row cannot keep a rank it earned when it was new.

| id | What | Source | Blast | Age | Effort | P | Status |
|---|---|---|---|---|---|---|---|
| B-45 | **Coordination configs were written for all nine repositories in one pass and none was ever run through `check`.** Measured 2026-08-14: **41 problems across nine repos**, on the day this repository's CLAUDE.md started saying coordination was on. Two mechanical classes: patterns matching no tracked file (the umbrella's ten `skills/*` paths, which a gitlink makes unmatchable, and a `test/negatives.py` guarded in seven members that `B-26` records the decision NOT to create), and `.env.agent-sync` uncovered by `.gitignore`. Eight are clean now. **`task-pipeline` is not**, and is deliberately untouched: it carries another session's uncommitted v1.55.0, and its problem is a decision rather than an edit — three id registers declared against an `fs` backend that cannot allocate, so `reserve` fails every time there. Either the backend changes or the registers move to the parent. | 2026-08-14 run, M0 | 2 | 0 | 1 | **2.0** | **closed 2026-08-16** — the register cannot be made to allocate here (only the `outline` backend has `atomicAppend`, and standing one up is an infrastructure decision), so what was fixed is the thing that made it dangerous: the declaration read as a capability and hid the manual procedure. `task-pipeline`'s `CLAUDE.md` now carries the three steps — lease first, compute from the **committed** file, commit before releasing — plus the version rule (`git ls-remote --tags`), its shipped `documentation.md` carries the generalisable half, and a guard requires both wherever a register is declared over a backend that refuses. The tool was always honest; the project was not. |
| B-46 | **Nothing runs `agent_sync.py check` in CI, which is why 41 problems accumulated unseen.** Each repository's own validator is green and says nothing about whether its coordination declares files that exist. The repair in `B-45` was a one-time sweep and the same drift returns the next time a config is copied between members — which is exactly how it arrived. The check is a script with an exit code, so the gate is one workflow step per repo, or one step in the umbrella that walks the submodules. Standing instruction #4 on the coordination layer itself. | 2026-08-14 run, M0 | 2 | 0 | 2 | **1.0** | **closed 2026-08-16** — the umbrella's CI runs `agent_sync.py check` across every repository that declares a coordination config, refusing a run where fewer than two are found (a loop over nothing reports success for everything it skipped). Running it immediately found `task-pipeline` unhealthy on two counts, both since fixed; **all nine now exit 0**. |
| B-47 | **`skills/sheleg-dev/CONTRIBUTING.md` documents another member's files.** Its **Where things go** table routes contributions to `benchmarks.md`, `growth-plays.md`, `myths.md`, `algorithm-updates.md`, `aeo-geo.md` and `scripts/page_audit.py`; `git ls-files` in sheleg-dev matches **none** of them and `seo-aeo-audit` holds all six. A whole section was copied from a sibling and never adapted, so a contributor is told to put a benchmark in a file the repository does not have. Found 2026-08-14 while adding the coordination link to that file. Needs sheleg-dev's real file map written, which is why it is filed rather than fixed in passing. | 2026-08-14 run, M0 | 2 | 0 | 2 | **1.0** | **closed 2026-08-16** — `sheleg-dev` 0.5.2 + umbrella v0.67.0, and **the row under-counted it**: sweeping every file name found eleven absent, not six, including a reference under a skill directory that has never existed and a promised standard-library auditor in a repository with no runtime code. Rewritten against `git ls-files` — six skills, twenty references, one executable, four-way version sync, eight CI self-tests, each number counted. A guard now checks the *Where things go* table only; a general path check cannot tell a path being used from one being discussed, and its first draft demonstrated that by flagging the three sibling files the rewrite names deliberately. |
| B-48 | **`skills.json` carries a `desc` and a `skillNames` list per member, and only one of them is checked against the submodule.** `test/validate.py` compares the pinned version against the submodule's `package.json`; nothing compares `skillNames` against the skills the submodule actually ships, and nothing notices when a `desc` stops describing them. Found 2026-08-14: `agent-stack` 0.8.0 ships `agent-harness` and the registry listed three skills, so a version bumped alone would have left `npx sshlg-skills list` advertising three against four. The list was fixed in this release; the **desc** still does not mention the new skill, and writing a member's own description belongs to that member. The check is cheap — the skills directory is right there in the submodule. | 2026-08-14 run, M3 | 2 | 0 | 1 | **2.0** | **closed 2026-08-16** — the desc half was fixed at v0.59.0 by counting at release; what remained was that nothing *checks* it. Token-matching the description against the skill names was tried and **produced four false failures out of eight members** — the concept was in every description and the word was not (`ad-tracking` as "GA4/Ads/Meta", `ux-foundation` as "personas and jobs") — so it is not that check, because one that fails on four correct members is discarded whole. What is mechanical is the **co-edit**: if a member's `skillNames` set changes and its `desc` string does not, the new skill is advertised nowhere. Reads `HEAD~1`, discloses where it cannot look, and was watched failing on a planted fifth skill. |
| B-49 | **`sheleg-design`'s triggers match none of the ways an operator asks for visual work.** All fifteen are compound noun phrases (`cinematic landing`, `design tokens`, `dashboard style`, `scroll animation`), and measured 2026-08-14 by the stage-10 ladder walk: `сделай paywall красивее` → `[]`, `поменяй палитру` → `[]`, `сделай дизайн лендинга` → `[]`, and even **`make the hero more cinematic` → `[]`**, because the trigger is the phrase `cinematic landing` and not the word. So the router that owns the visual layer is the one an operator cannot reach by asking for visual work. This is the retro's 2026-08-10 class exactly — the routing table is built for people who do not know the internals and written by the people who do — and it survived that entry because nothing measures a router's coverage of the words a user brings. The constraint is real and is why this is filed rather than patched: a trigger must be a word the skill's own description advertises, so the fix starts in `sheleg-design`'s description, which is another member's release. | 2026-08-14 run, stage 10 ladder | 3 | 0 | 2 | **1.5** | **closed** — `sheleg-design` 1.37.1 + umbrella v0.62.0. Fourteen bare words added; 8/8 visual prompts route, 9/9 controls silent (question and both opt-outs included). `дизайн` stays out: it is inside the refusal «без дизайна». |
| B-01 | `agent-sync` releases fail on one letter: `release.yml` extracts notes with `awk '$0 ~ "^## " v'` while its CHANGELOG writes `## v1.5.2`, and the terminator `/^## [0-9]/` misses the same prefix. v1.5.0, v1.5.1 and v1.5.2 each tagged and died at `no CHANGELOG section` (run `31287012133`). npm has served 1.4.3 since 2026-08-03 while three tags claim otherwise. | 2026-08-10 run | 3 | 1 | 1 | **6.0** | **closed 2026-08-10** — fixed upstream, run `31374955487` green |
| B-02 | `agent-sync` `main` is red on its own validator: `FAIL: status verdict: 'status' reported normally on a setup 'check' fails` (run `31352513346`, 2026-08-10). No release may be cut from it until this is green, so B-01's fix alone does not ship anything. | 2026-08-10 run | 3 | 1 | 2 | **3.0** | **closed 2026-08-10** — fixed upstream, run `31374949511` green |
| B-03 | `graphify-out/` is neither tracked nor ignored in five members, so a graphify run leaves every one of them dirty and the umbrella reports five modified submodules. | 2026-08-10 run | 2 | 1 | 1 | **4.0** | **closed 2026-08-10** — `graphify-out/` added to `.gitignore` on `main` in all five (`agent-sync` 5df7fe1, `make-skill` 022e9da, `seo-aeo-audit` ea12acd, `sheleg-dev` 5ef953a, `agent-stack` 3a4ee92). `super-ux` deliberately differs and was left alone: it commits `graph.json` and `GRAPH_REPORT.md` so the next harvest can query them, ignoring only `.graphify_*` and `cache/`. The local `.git/modules/skills/*/info/exclude` entries stay until each pin moves past its tag onto a commit that carries the line. |
| B-04 | ~~`sheleg-dev` and `agent-stack` have no release tags at all~~ **closed 2026-08-11** — both now tag: sheleg-dev v0.3.0, agent-stack v0.2.0 (its first ever). Original text: `sheleg-dev` and `agent-stack` have no release tags at all — `git ls-remote --tags` returns nothing for either — so their pins are commits on `main`, `check_pins.py` has nothing to compare against, and their release workflows have never run once. | 2026-08-06 port | 2 | 2 | 2 | **3.0** | **closed 2026-08-11** |
| B-05 | **closed 2026-08-12** — `lib/backup.js`: every write to an agent instruction file is preceded by exactly one `protect()`, the copy is read back and byte-compared, and a copy that cannot be taken CANCELS the write rather than degrading to "wrote anyway". Copies go to `~/.sshlg-skills/backups/`, never beside the original — `~/.cursor/rules/` globs every `*.mdc`, so the obvious fallback (the file's own parent) is the one place a backup can be re-read as an instruction. Watched failing: with the gate removed, the fixture reports *the operator file was modified with no backup behind it*. Original text: the copy of `~/.claude/CLAUDE.md` exists because an agent chose to make one — twice now, once ten minutes before it was needed. That is a habit, not a mechanism. | C-09 (2026-08-06) | 2 | 2 | 2 | **3.0** | **closed 2026-08-12** |
| B-06 | C-07: four members do not delegate to `routers --member`; only `bin/super-ux.js:354` does. The bundle writes all eight routers without it, so nothing is broken — but a lone installer for those four writes no router at all. | C-07 (2026-08-06) | 1 | 2 | 2 | **1.5** | **closed 2026-08-12** — `task-pipeline` v1.49.1, `agent-sync` v1.9.0, `make-skill` v0.16.0 and `sheleg-design` v1.18.0 all delegate to `npx sshlg-skills routers --member <this>`. The row said four members and four was right: of the eight, five own a router and only `super-ux` delegated. The other three (`seo-aeo-audit`, `sheleg-dev`, `agent-stack`) own no router — `scope({member})` returns nothing for them — so an offer there would print "nothing to write" and be noise, not a fix. Isolation verified per member by damaging TWO sections of a real block: each installer repaired its own and left the other byte-for-byte. Both the resolvable and the absent-launcher paths were exercised, as was the non-interactive case, which records an opt-out and takes the backup. |
| B-07 | C-04: the CLI launcher has no `docs/ux/` chain. Waived at intake (D-09) in favour of REQ rows asserting exact output; the fixtures in `test/cli_config_test.js` and `test/drift_test.js` are what stands in for it. Revisit only if the command surface grows past what fixtures can describe. | C-04 (2026-08-06) | 1 | 2 | 3 | **1.0** | open — waived, revisit on growth |
| B-08 | C-05: no design-time track in `seo-aeo-audit`. Different repository; the `seo-llmo` router carries the rule, and the skill stays an audit. | C-05 (2026-08-06) | 1 | 2 | 3 | **1.0** | open — deliberate |

| B-09 | A channel fed once but absent from `defaultAgents` drifts forever, and no command repairs it. `kiro-cli` and `goose` each carried 12 of the family's 19 skills. | 2026-08-10 audit | 2 | 1 | 2 | **2.0** | **closed 2026-08-10** — both ids added to `defaultAgents`, and `update` now reconciles rather than only refreshing: `skills update <id>` is a no-op for a skill installed nowhere and reports success anyway, so refreshing alone could never deliver a new member. Proven by planting the never-installed state for `agent-orchestrator` and watching the old verb restore nothing and the new one restore all seven channels. v0.30.0. |

| B-10 | `sheleg-dev`'s `npm publish` step fails `ENEEDAUTH` on every release (run of v0.3.0, 2026-08-11). The GitHub release and tag land, so the member ships and `check_pins.py` treats it as GitHub-only and stays green — which is why nobody noticed. Either the package is meant to be on npm and the workflow's auth is wrong, or it is GitHub-only and the publish job should not run. Today the release is reported failed for a step nobody wants. | 2026-08-11 run | 1 | 1 | 1 | **2.0** | **closed 2026-08-12 — the premise no longer holds, verified.** `@ssheleg/sheleg-dev` is on npm at 0.4.1, exactly the pinned version, and its last release run shows `job publish: success`. The ENEEDAUTH run of v0.3.0 was fixed at some point without the row being updated. All eight members have `PUBLISH_NPMJS=true`, `RELEASE_ENABLED=true` and an `NPM_TOKEN` secret, and all eight publish. What the row was really pointing at turned out to be B-15. |

| B-11 | Four members' release workflows disagree on the CHANGELOG heading they extract, and a mismatch fails the job *after* the tag is public — the tag then looks delivered while nothing shipped. `super-ux` wants `## <version>` and silently failed on `## [0.34.1]` (2026-08-11); `agent-sync` lost three releases to the same class before someone noticed; only `sheleg-dev` accepts every shape this family has used. One accepting pattern, copied to all eight, removes the whole class. | 2026-08-11 run | 2 | 1 | 1 | **4.0** | **closed 2026-08-12** — one accepting pattern (`^## \[?v?<ver>\]?`, terminator requiring a full semver) copied to all eight. Verified against every member's real CHANGELOG, all four heading shapes, a subsection boundary, the `1.2.3` vs `1.2.30` prefix collision, and a missing section (empty, so the existing `test -s` aborts). The historical defect was replayed: the old `super-ux` pattern against `## [0.34.1]` produces empty output. Two things had to move with it — `agent-sync`'s plant searched the old string and stopped planting (standing instruction #6), and its spill check matched `^## v?\d` and could not see the bracketed form its own terminator was widened to stop at. **The `workflow` OAuth scope was never the blocker it was reported as:** the restriction applies to OAuth pushes over HTTPS, and these repositories push over SSH. |

| B-12 | **closed 2026-08-12** — `test/validate.py` now fails in both directions, planted against; `test/audit_bundle.py` is in the repo. Original: a member that gains a skill does not announce it, and the umbrella finds out by audit. `agent-stack` shipped `agent-evals` in v0.6.0; `skills.json` still declared one skill, so `install` and `update` would both have skipped it forever — the same class v0.30.0 fixed for members, one level down at skills. `test/validate.py` compares declared against shipped and PASSED, because it only fails on a declared skill that is missing, never on a shipped one that is undeclared. Make that check symmetric. | 2026-08-12 audit | 2 | 1 | 1 | **4.0** | **closed 2026-08-12** |
| B-13 | **`check_pins.py` asks about the world, not about the commit, and blocks CI anyway.** A commit does not become invalid because someone else released a member five minutes later — yet `validate.yml` fails on exactly that, and did four times on 2026-08-10..12 while another agent released the family continuously. Every one of those failures was correct about the world and wrong about the change under test. Two candidate fixes: move the pin check out of the blocking path into `audit_bundle.py` (it is a freshness question, like the audit's), or have the release workflow bump pins itself, which is also the retirement trigger written into standing instruction #5. | 2026-08-12 audit | 2 | 1 | 2 | **3.0** | **closed 2026-08-12** — `check_pins.py` now answers two questions with different exit codes: 1 when a pin names a version that was never published (the commit is wrong on its own terms, blocks) and 2 when every pin is real but one is not the newest (a member released mid-flight; warning plus a run-summary note). `classify` is pure with five offline cases and asserts its verdicts differ. All three codes were watched firing against real plants: `99.99.99` -> 1, a genuine older release `0.36.1` -> 2, restored -> 0, and the CI step's branching proven for each with a stub. |
| B-14 | **closed 2026-08-12** — `agent-stack`'s CI had been red on every push since v0.6.0, and the validator it accused was fine: the over-long-description plant replaced the literal `"token wallet".`, which left the file when the description was compressed to fit the token budget. The `replace` no-oped, nothing was planted, the validator correctly found nothing, and the step reported `ERROR: validator accepted a description past the 1024-char limit`. Verified pre-existing on `3a31dbb` at 00:16, before the commit that surfaced it. Re-anchored on the front matter's shape (`description: >-`) with an `assert` that it planted something; all six of that repo's negative self-tests then run locally and every one still detects its defect. | 2026-08-12 B-11 work | 1 | 1 | 1 | **3.0** | **closed 2026-08-12** |

| B-15 | **closed 2026-08-12** — the npm half of `check_pins` was inert for six of eight members. `candidates()` tries the `npm` field, the member name, then the repo basename, and rejects a name whose `repository` is not ours. `npm` was unset for every member, six publish as `@ssheleg/<name>`, and `task-pipeline` publishes as `task-pipeline-skill` while the bare `task-pipeline` on npm belongs to node-task. So six pins were never compared against the registry at all and were reported `(tag; not on npm)`; git tags happened to agree, so nothing went red — the exact failure mode the file's own docstring warns about ("they can all agree and all be wrong"). Fixed by declaring `npm` per member and having the validator compare it with the submodule's `package.json` name in both directions. Deriving `@ssheleg/<name>` would have passed today and broken on the two members already publishing under a third shape. | 2026-08-12 B-10 investigation | 3 | 1 | 1 | **5.0** | **closed 2026-08-12** |
| B-16 | Three members released while this run was in flight: `super-ux` 0.38.0, `sheleg-design` 1.21.0, `seo-aeo-audit` 0.16.0. `check_pins.py` classifies this as exit 2 — every pin exists, so v0.42.0 installs exactly what it advertises — but the bundle is one sweep behind the world. Bump the three together (`skills.json` → submodule pointer → README table), never one at a time: standing instruction #5 exists because fixing the member the log named made the same run fail twice. | 2026-08-12 run, pin sweep | 2 | 1 | 1 | **2.0** | **closed 2026-08-13** — all three moved in one sweep with `task-pipeline`: `super-ux` 0.38.0, `sheleg-design` 1.24.0 (five releases behind), `seo-aeo-audit` 0.16.0. Submodule pointer, `skills.json` and the README table moved together, and `npm test` reported no skill-declaration drift — the check B-12 added for exactly this. |
| B-17 | `npm run test:negatives` in `task-pipeline` reports one broken guard when run from the **submodule** checkout: the guard reads `git log -p`, the harness copies the repo to `/tmp` without a resolving `.git`, and a submodule's `.git` is a file pointing at a relative path. Reproduced on pristine `HEAD` (`c90835b`) with a submodule-style `.git`, so it is pre-existing and not this change; CI clones normally and stays green. The harness should copy or bind the git dir, or say plainly that the guard was skipped. **Re-measured 2026-08-13 (v0.46.0): it is TWO guards, not one** — *the high-water mark lowered in a COMMITTED change* and *a version number already spoken for by a tag*, both printing `fatal: not a git repository`. The original row counted the one a log named. CI clones normally and passed (20m30s) with both firing, which is what confirms the cause is the checkout and not the guards. | 2026-08-12 run, stage 6 · re-measured 2026-08-13 | 1 | 1 | 1 | **1.0** | **closed 2026-08-16** — fixed by this programme's M5/F-8 work rather than directly: the restore now resolves `git rev-parse --git-common-dir` and overlays the per-worktree dir, so a submodule checkout gets a real repository. Verified from the submodule checkout: `negatives.py -k "high-water mark lowered"` → `PASS: all 1 guards provably reject their planted defect`, where it previously reported `fatal: not a git repository`. |
| B-18 | Pushing a change that touches `.github/workflows/` from a submodule checkout is refused: the submodule remote is HTTPS by invariant, and an OAuth push over HTTPS needs the `workflow` scope. Worked around per-machine with `git remote set-url --push … git@github.com:…`, which leaves `.gitmodules` HTTPS. The umbrella's retro says these repositories "push over SSH" — true of the source clones in `~/DATA/`, not of the submodule checkouts, and that distinction cost a failed push. Either set the push url in `install`/`update`, or write the distinction into `CLAUDE.md`. **Fired again 2026-08-13 in `seo-aeo-audit`**, whose PR touched `validate.yml`: the push was refused verbatim and the recorded workaround cleared it in one command, leaving the fetch url — and therefore the `.gitmodules` invariant — HTTPS. A row that predicts a failure and names its remedy is worth more than the ten minutes it saved here. | 2026-08-12 run, stage 7 · fired again 2026-08-13 | 2 | 1 | 1 | **2.0** | **closed 2026-08-14** — hit live while pushing `agent-sync` v1.11.0: *refusing to allow an OAuth App to create or update workflow `.github/workflows/validate.yml` without `workflow` scope*. Fixed by the C-06 pattern across the whole family rather than in the one repo that failed (standing instruction #5): every submodule now has an **SSH push-url and an HTTPS fetch-url**, six already did and `agent-sync` and `sheleg-design` were the two left. Measured after: 8 of 8 fetch urls still HTTPS, which is the invariant `test/validate.py` enforces. |
| B-19 | The family wires its hooks into `~/.claude/settings.json`; `agent-sync` ships its own from the plugin's `hooks/hooks.json` and `task-pipeline` now does too. Two channels for one mechanism. The plugin channel removes the write to the operator's file entirely — the whole risk profile of this repository — and costs the per-hook off switch, which is exactly why `superpowers` is disabled on this machine. Decide it once, deliberately, rather than letting each member answer separately. | C-01 (2026-08-12) | 1 | 1 | 2 | **1.0** | **closed 2026-08-14** — decided once, on measured facts, and recorded in `docs/DOCMAP.md`: **the channel follows the shape of the thing, not a preference.** A plugin (`agent-sync`, `make-skill`, `task-pipeline`) has a manifest, so it ships from `hooks/hooks.json` and never writes to the operator's file; the cost is that enablement is the only switch, which is acceptable for a guard that fails silent and is a different question from a doctrine-injecting plugin. The umbrella is a **launcher**, has no manifest, and therefore has no second channel to prefer — and its `PreToolUse` entry is the backup that exists because `~/.claude/CLAUDE.md` was destroyed twice (B-05), so the write is the mechanism rather than a convenience, and it goes through `protect()` like every other. Measured: three events fire in both channels — six different scripts, six different jobs, not duplication. What is now forbidden and guarded: a member shipping a plugin manifest may never also wire itself into `settings.json`, because uninstalling the plugin would leave that entry firing and owned by nobody. Watched failing on a plant that adds such a writer. |

| B-20 | **The stage-6 verdict is written by the agent the release gate is gating.** `task-pipeline` v1.50.0 refuses an outward release act while the ledger records no `stage: 6 … verdict pass` — and that line is typed by the agent, so the gate reads a claim by the party it constrains. A `PostToolUse` hook should record the OBSERVED exit code of the project's declared gate command, and the release gate should require the claim and the observation to agree. Not taken here because it changes another skill's ledger grammar (`templates/run.md`, `progress.md`, `loop-guard.md` all read those lines), and that deserves its own run rather than the end of a long one. | 2026-08-13 run | 2 | 1 | 2 | **2.0** | **closed 2026-08-13** — `hooks/gate-observer.sh` records the observed exit code of the declared gate command as a fifth ledger line, and the release gate requires the claim and the observation to agree. Dogfooding it against this repository's own ledger found a second weakness the fixtures had missed: the gate accepted ANY green observation, so an earlier green satisfied it over a later red. It reads the last one now, both directions fixtured. |
| B-38 | **The repository gate judged commits belonging to other repositories.** `.claude/settings.json` wires the gate for one project and the hook ran `npm test` in `CLAUDE_PROJECT_DIR` for any `git commit`, so a submodule commit was refused on the umbrella's verdict about a change it does not contain. It deadlocked a release live: the umbrella was red *because* the submodule had not shipped, and the submodule could not commit the fix because the umbrella was red. *(was `B-22` until 2026-08-14; renumbered because the id collided — see B-40)* | 2026-08-13 run | 2 | 1 | 1 | **2.0** | **closed 2026-08-13** — a commit for this project must stage something in this project; that is the decidable form of the question, since the payload carries no shell cwd. Fixture: a commit with nothing staged here is not gated, plus the existing red-suite case now runs against a real repository with a real index. |
| B-39 | **`task-pipeline` v1.50.0's gate was keyed to `stage: 6` literally**, so a project whose flow has six stages — tests green at stage 4 — could never tag anything again. The same class as the rail's denominator, one day later and with worse consequences: a wrong rail misinforms, a wrong gate stops the work. *(was `B-23` until 2026-08-14; renumbered because the id collided — see B-40)* | 2026-08-13 run | 3 | 1 | 1 | **6.0** | **closed 2026-08-13** — v1.51.0 resolves the tests stage from `pipeline.json` (`state: "tests"` or a declared `gate.command`), failing that from the ledger by name; unresolvable still refuses, with a reason saying how to become readable. Reproduced against a six-stage project first. |
| B-21 | Four more hooks the pipeline could own, proposed and not taken: `SubagentStop` appending `hand:` lines mechanically (its own audit compares them against `iter:`, and today one agent writes both sides from memory); `PreCompact` stamping the ledger at the boundary the ledger exists because of; `SessionEnd` marking a run abandoned so `checkup` can see it; and `PreToolUse` refusing source edits while the ledger sits at stages 0–4. Each is small; together they are a run. | 2026-08-13 run | 1 | 1 | 2 | **1.0** | **closed 2026-08-13** — `run-lifecycle.sh` covers the compaction boundary, an abandoned run and a subagent finishing, in ONE ledger shape rather than three. `build-gate.sh` asks before the product is edited ahead of the plan, resolving the build stage BY ROLE — the lesson v1.51.0 learned from the release gate, applied before it could repeat. The `hand:` half of the original row was **not** built and the reason is recorded: that line carries judgements only the agent holds, and a hook filling them in would fabricate the evidence it exists to provide. |
| B-22 | **`update` does not refresh the wired runtime, so every hook improvement is invisible on a machine that only runs `update`.** `syncRuntime()` lives inside `cmdHooks` and is called on `hooks install` alone; `bin/sshlg-skills.js:664` is the only reference to `runtimeDir`. Watched on this machine at v0.46.0: `npx sshlg-skills@latest update` brought all six plugins to their new versions and left `~/.sshlg-skills/runtime/lib/` at 24 modules against the package's 25 — `injectors.js` absent, so the SessionStart line REQ-07 shipped could never print until `hooks install` was run by hand. The whole v0.42.0 hook suite reaches machines the same way. | 2026-08-13 run, stage 8 | 3 | 1 | 2 | **1.5** | **closed 2026-08-13 (v0.47.0)** — the copy moved to `lib/runtime.js`, one home called by `hooks install` (`create: true`) and `update` (`create: false`). Verified **from the published package on the machine that had the defect**: the runtime was short exactly `lib/runtime.js`, and `npx sshlg-skills@latest update` printed *refreshed 36 file(s) — 1 new, 0 changed* naming it, after which `stale()` reports 0 missing and 0 differing. New files are named individually because a count alone would have hidden this as well as silence did. **The guard is on the WIRING, not the module**: `check_update_refreshes_runtime()` reads `cmdUpdate`'s own body, because the eight fixtures prove the copy works and cannot prove anybody calls it — the exact gap that let this survive five releases. Watched failing against a planted removal; negative self-test 8 → 9 |
| B-23 | The retro's standing instructions are cited BY NUMBER (two CHANGELOGs and a merged PR body now do it) while the file renumbers on every retirement. v0.46.0 left slot 3 vacant rather than renumbering, which keeps the existing citations true and makes the list non-contiguous — a workaround, not a fix. Either the ids become stable (never reused, never renumbered) or citations stop using them. | 2026-08-13 run, stage 10 | 1 | 1 | 1 | **1.0** | **closed 2026-08-14** — the rule is written where it is governed and enforced where it can be. `docs/evidence/retro.md` now states that ids are allocated once and never reused, that a retirement leaves its slot **vacant**, and that the next id is `max(every id ever held) + 1` rather than the lowest gap. Measuring it found the defect had already happened: **`#1` was retired on 2026-08-13 and refilled the same day**, so a citation of `#1` means two different rules depending on its date. Both sides have citations in shipped documents, so the collision is **recorded rather than rewritten** — renumbering either would make a published sentence point at a rule it never meant. `check_standing_instruction_ids_are_stable()` refuses any further reuse, refuses a reorder, and refuses the rule's own disappearance from the file; watched failing on a plant that refills the vacant slot 3, which is exactly what an agent tidying a non-contiguous list will do. |
| B-24 | **The code graph cannot be refreshed on this machine, so every run inherits a stale one.** `graphify . --update` exits with `no LLM API key found (25 doc/paper/image file(s) need semantic extraction)`; the existing graph survived untouched (715 nodes, `built_at_commit a20c402`) and is now **5 commits behind HEAD**. Stage 0 of this run already marked it *⚠ not trusted for reach until refreshed*, and stage 9 could not clear the marker. `--code-only` would index the code without a key but was NOT run: it risks replacing a graph that has doc nodes with one that does not, and a narrower graph presented as a refreshed one is worse than a stale one that says so. Either a key reaches the tool, or `--code-only` is proven non-destructive first, or the graph stops being part of stage 9 here and says so. **Re-measured family-wide 2026-08-13: every graph is stale, not just this one** — `task-pipeline` 95 commits behind, `sheleg-design` 63, `agent-sync` 22, `seo-aeo-audit` 18, `super-ux` 17, `make-skill` 12, umbrella 8, `sheleg-dev` 8, `agent-stack` 8. Stage 0 of every run in every member harvests one of these, so the marker is not an exception on one repo — it is the normal state of the family. | 2026-08-13 run, stage 9 | 2 | 1 | 1 | **2.0** | **closed 2026-08-14** — the premise was wrong: `graphify . --update` (the slash-command form) needs an LLM key, but the CLI verb **`graphify update .` does not** — *re-extract code files and update the graph (no LLM needed)* — and it already refuses to overwrite with a smaller graph unless `--force`, which is the exact protection the row was waiting for. Run: **715 → 930 nodes, 1094 edges, 77 communities**, curated graph backed up to `graphify-out/2026-08-14/`. Measured honestly: `document` nodes +161, `code` +76, `concept` 0 — and **`rationale` −22, 54 labels gone**, because those come only from a semantic pass. Net richer, partially poorer, and both halves are stated. The stage-9 check then ran for the first time: all 8 god-nodes (`Verification ledger`, `log()`, `Pipeline retrospective`, `main()`, `Execution order`, `run()`, `fail()`, `cmdUpdate()`) are named in the 30 documents scanned — no undocumented seam. **Remainder, named not hidden:** the semantic half still needs `GEMINI_API_KEY` or equivalent, which is machine configuration, not a repository defect. |
| B-25 | **This repository's own `pipeline.json` is a skeleton, and nothing checks it.** Every one of its 11 stages is `{id, state, name, gate:{type}}` — **no `gate.check` anywhere** — and `run.loop` carries `enabled` and a `_note` but not the `mode` its own schema marks required. Validated against the schema `task-pipeline` ships: **24 errors** (`run/loop: 'mode' is a required property`, plus `skills` and `gate.check` missing on all 11 stages). Measured 2026-08-13 while arming a loop over it. Consequence, stated plainly: the 2026-08-13 artifact-root run passed eleven gates whose criteria do not exist in the config — the agent supplied them from doctrine, which is better than nothing and is not what the config is for. `task-pipeline`'s own `pipeline.json` is fully populated, so this is the umbrella alone. Two fixes, different sizes: `mode` plus a validator guard comparing the config to the schema is under an hour; writing eleven honest `check` criteria is a session. | 2026-08-13 audit | 2 | 0 | 2 | **1.0** | **closed 2026-08-14 (v0.50.0)** — eleven real criteria, drawn from what this repository actually does: the source ledger and the graph's MEASURED lag at stage 0, the single write path through `protect()` at 5, the CI verdict read BY THE REF PUSHED and the pin sweep at 7, the registry rather than the pipeline at 8, and at 10 releasing the lease BEFORE closing its board row (B-35, learned the same night). `version` also held the task-pipeline RELEASE where the schema wants the config-format version — 24 violations became 0. `check_pipeline_matches_its_schema()` now validates against `pipeline.schema.json` AND refuses a criterion that is blank or a placeholder, because a gate passing by being unreadable is how eleven went unnoticed; watched failing against both plants, negative self-test 10 → 11, and CI installs `jsonschema` so the contract is checked rather than disclosed as skipped. Every path, file and function the eleven criteria name was verified to resolve. Written AFTER the lease was released |
| B-26 | **Six of nine release workflows publish without ever running the proof that their guards work.** The negatives live in `validate.yml`, a DIFFERENT workflow, so a repo whose guards are provably disarmed can still reach npm. `sheleg-dev` did exactly that on 2026-08-12: `release v0.4.1` **success**, `validate v0.4.1` **failure**, npm served 0.4.1 at 01:56. Gated: `task-pipeline` (`test:all` → `negatives.py`), `agent-sync` and `sheleg-design` (`--self-test` in the release job). Not gated: umbrella, `agent-stack`, `make-skill`, `seo-aeo-audit`, `sheleg-dev`, and `super-ux` — which has no negative self-test anywhere. This is the mechanism that let B-28 ship. | 2026-08-13 audit | 3 | 0 | 2 | **1.5** | **closed 2026-08-14** — `validate.yml` is callable and `release.yml` declares `needs: validate` in all six: the umbrella, `sheleg-dev`, `agent-stack`, `super-ux`, `make-skill`, `seo-aeo-audit`. **The release runs the real suite, not a copy** — porting a 258-line negatives runner into each repo was the alternative and was rejected as six new drift surfaces. Proven on live releases, each selected BY TAG rather than by recency (instruction #9): every run reads `validate / validate=success, release=success, publish=success`, and each was closed by reading the registry rather than the pipeline. A guard holds all three parts — trigger, call, `needs` — because calling the suite without depending on it lets the jobs run in parallel, which looks gated and is not; watched failing against the planted removal, negative self-test in CI. Shipped: sheleg-dev 0.4.3, agent-stack 0.7.1, super-ux 0.38.2, make-skill 0.18.1, seo-aeo-audit 0.16.3, sshlg-skills 0.49.0. **Written AFTER the lease was released:** the claim tag and the status live in the same cell, so a close written while holding the claim is reverted by the restore — see B-35 |
| B-27 | **32 of 301 file-editing plants do not assert that they changed anything.** `task-pipeline`: 267 editing plants, **0** without an assert. Every other repo: `make-skill` 8/8, `seo-aeo-audit` 11/11, `sheleg-dev` 4/4, `agent-stack` 3/3, umbrella 6/8. Standing instruction #6 and its corollary live in THIS repository's retro and are applied in exactly one repo of eight. Any of the 32 stops planting on the next reword and then reports a healthy guard as broken — which is not hypothetical, see B-28. | 2026-08-13 audit | 3 | 0 | 2 | **1.5**| **closed 2026-08-14** — `test/plant_guard.py` in the umbrella, `make-skill` and `seo-aeo-audit`: one implementation, `snap` before a plant and `verify` after, comparing **content and mode**, ignoring `.git` churn, refusing when handed no tree or no snapshot, manifest kept outside the tree it measures. Nine fixtures, the first of them the incident. `sheleg-dev` and `agent-stack` converted their plants to Python with per-anchor asserts. **Why a script and not five careful copies:** written inline it produced a different defect in each — `cmp -s A B && …` killing the step under `set -eu` when the files DIFFER; an unquoted path; a raw triple-quoted string closed by the pattern's own leading quote; a heredoc passed where a shell function wanted an argv; and a content-only comparison against a plant whose whole effect is `chmod`, **which shipped into a PR and CI caught**. A sixth was in the helper's own docstring. Released: sheleg-dev 0.4.2, agent-stack 0.6.1, make-skill 0.17.1, seo-aeo-audit 0.16.2, sshlg-skills 0.47.1, every gate green on the commit its tag points at |

| B-28 | **`sheleg-dev`'s `main` has been red since 2026-08-12 and the guard it blames is healthy.** Its plant anchors on the literal `"BTCPay".`; the description now reads `BTCPay` without the quotes and period (`grep -c` → 0), so `s.replace(..., 1)` changes nothing, no assert notices, the validator honestly passes, and the step prints *ERROR: validator accepted a description past the 1024-char limit*. Proven healthy by planting on the real folded `>-` shape instead: *description is 1204 chars, limit 1024*, exit 1. An instance of B-27, kept as its own row because it is red right now. `crypto-payments` sits at 907 chars and `stripe-billing` at 996 of 1024 — 28 chars of headroom under a limit whose proof is disarmed. | 2026-08-13 audit | 2 | 0 | 1 | **2.0**| **closed 2026-08-14** — closed inside B-27's sweep rather than as its own patch, which is what instruction #5 asks for. The plant was re-anchored on the folded `>-` block's shape and now produces a 1204-char description the validator rejects; the other three plants in that repo left `sed -i` behind. `sheleg-dev` CI: **validate pass**, both runs, after two days red |

| B-29 | **`task-pipeline`: 99 shipped REQ rows, all 99 at `never`.** Not one requirement the pipeline itself has shipped has been confirmed by a person. Its own `checkup` exists to make exactly this visible and the number has never come down. Measured 2026-08-13 across the family: umbrella 88 rows / 0 never, `super-ux` 37/0, `sheleg-design` 51/0, `seo-aeo-audit` 34/0, `agent-sync` 28/0 — the exposure is concentrated in one member, and it is the one every other member's process depends on. | 2026-08-13 audit | 2 | 0 | 3 | **0.67** | open — **the blocker is removed, the count is not.** Built and verified 2026-08-14: `templates/exposure.sh` computes the line and the ordered check-list `references/exposure.md` calls the deliverable and which nothing ever produced, plus 13 fixtures for every prohibition that doctrine states. It found the real obstacle: ids restart per run, so 99 rows carry **14 distinct ids across 9 runs** and `REQ-001` names nine different rows — an operator told to confirm it cannot know which. The command cites the pair. **Not landed**: a concurrent session was mid-release of v1.55.0 in that submodule, so the work is parked (B-42) rather than committed over theirs. The 99 stays 99 regardless: filling `Human` is the one thing in that ledger a machine may not do. |
| B-43 | **The exposure command is built and parked, not merged.** `templates/exposure.sh` + `test/exposure_test.py` (13 cases, all green) + 3 negatives + the doctrine section naming the command, held at `scratchpad/b29-exposure/`. Blocked only by timing: a concurrent session was releasing task-pipeline v1.55.0 in the same working tree. Land it after that release, recount `MIN_EXPECTED`, and cut v1.56.0. | 2026-08-14 B-29 iteration | 1 | 0 | 1 | **1.0** | **closed 2026-08-16** — `task-pipeline` 1.61.0 + umbrella v0.68.0, and **the row had expired**: the parked work existed nowhere — not on disk, in git history, in a stash or in a dangling object. Rebuilt with 14 fixtures (three of which caught real defects, all lying in the reassuring direction) and three plants; guards 344 → 347. The validator now asserts each seeded script exists, has a shebang, and is still named by its doctrine. **The lesson is the board's:** "built and parked" is a claim about a filesystem no gate reads, and a scratchpad is not version control — a row that parks work must name the branch or commit holding it. |
| B-44 | **The premise of this row was wrong, and the measured cause is narrower and worse.** `task-pipeline` DID carry `.claude/agent-sync.json`, committed in v1.53.0 — I filed the row on an assumption instead of reading the file, which is standing instruction #5 firing on the person writing the board. What it lacked was `CHANGELOG.md` and `package.json` in `guardedFiles`: it guarded the six doc registers and nothing a release touches, so two sessions wrote both release files in that tree at once, each under its own `v1.55.0` heading, and the guard was silent because neither file was in the list. Measured both ways: from the member, `guard CHANGELOG.md` → *not a guarded file*. | 2026-08-14 B-29 iteration | 2 | 0 | 2 | **2.0** | **closed 2026-08-14** — three parts. The umbrella's `guardedFiles` gains ten `skills/*/…` patterns, because an edit made from a session rooted here resolves the project here and never consults the member's own config. Seven members that had no config at all are seeded with one naming the release surfaces. `task-pipeline`'s existing config goes 6 → 13 patterns (`cfce394`, committed as a single file so a concurrent release in that tree was not swept in). A validator guard now refuses any member whose config omits `CHANGELOG.md`, `package.json` or the workflows — not *is there a config*, which was already answered yes while the collision happened — watched failing on a plant that strips a config back to doc registers only. |
| B-30 | ~~Three~~ **Two members have no verification ledger at all** — `make-skill` and `sheleg-dev` return 0 REQ rows. Their exposure reads as zero because nothing is measured, which is the one reading a `checkup` must never produce: an absent ledger and a clean one are indistinguishable from the number alone. **`agent-stack` closed 2026-08-14**: `docs/evidence/verification.md` seeded by its `agent-interop` run — 12 REQ rows, 12 verified, 0 at `never`, each carrying the command that confirmed it and a closing section naming what the checks did *not* cover. | 2026-08-13 audit | 1 | 1 | 2 | **1.0** | **closed 2026-08-14** — `make-skill` and `sheleg-dev` each seeded with ten REQ rows, every one measured against the shipped version rather than back-filled from a CHANGELOG: validator output, the step-level conclusions of every negative in the release's own CI run (9/9 and 8/8 `success`), four version surfaces read back, the `workflow_call` gate by line number, `npm view` and the newest tag. `sheleg-dev` adds reference integrity (26 links, 0 unresolved, 0 orphans) and both installer paths against a fresh HOME. Each ledger closes with a section naming what the checks do NOT cover — vendor drift for `sheleg-dev`, the advice-as-advice for `make-skill` — so it cannot be read as coverage it lacks. Family sweep afterwards: **nine of nine repositories carry a ledger**, and `never` now appears in exactly one (B-29). |
| B-41 | **Two members have no `npm test` at all** — `make-skill` and `sheleg-dev` both carry an empty `scripts` block in `package.json`, so the family's one-command gate does not exist there and CI calls `python3 test/validate.py` directly. Found while seeding their verification ledgers (B-30), and recorded in both ledgers' *what these checks do not cover* section. The cost is not that the checks are missing — they run in CI — but that a contributor or an agent following the family's own house rule runs `npm test`, gets `npm error`, and has no signal about which command is the gate. | 2026-08-14 B-30 iteration | 1 | 0 | 1 | **1.0** | **closed 2026-08-14** — both members now carry an `npm test` that runs exactly what their CI's blocking steps run: `make-skill` → `validate.py && plant_guard_test.py`, `sheleg-dev` → `validate.py`. Nothing was unguarded before, only unnamed; the cost was that an agent following the family's house rule got `npm error` and no signal about which command is the gate. The *what these checks do not cover* paragraph naming the gap was removed from both ledgers in the same change. |
| B-31 | **The release gate requires only `stage: 6`, so a run is accepted at stage 10 with stages never stamped.** The 2026-08-13 artifact-root run closed with 0,1,2,5,6,7,8,9,10 recorded and **3 (spec) and 4 (plan) never stamped** — the status line printed `3· 4·` and 73%, correctly. The artifacts existed (the module map and REQ table sat in the brief) but their verdicts do not, so a later reader or `checkup` sees an incomplete run that was nonetheless accepted. Either stage 10 requires a verdict per declared stage, or the flow stops declaring stages it merges. | 2026-08-13 audit | 1 | 0 | 1 | **1.0** | **closed 2026-08-14** — task-pipeline v1.54.0 ships `templates/stage-coverage.sh`; the umbrella seeds it at `scripts/stage-coverage.sh` and stage 10's own `gate.check` in `pipeline.json` names it, run before the ladder walk. A validator guard requires both halves (script present AND gate naming it), watched failing on a plant that removes only the naming half. Three negative self-tests: the incident replayed (a five-stage flow stamping four), the naming half removed, and exit 2 when there is nothing to look at. This run's stages 3 and 4 were then stamped for what actually happened — folded into the module brief — taking coverage 9/11 → 11/11. Umbrella v0.51.0, npm confirmed. |
| B-32 | **`agent-stack` is pinned two commits behind `origin/main`, and both are CI fixes** — `ci: anchor the description plant on the file's shape, not its wording` and `ci: one CHANGELOG-extraction pattern for the whole family (B-11)`. No newer tag exists, so `check_pins.py` is right to pass and the pinned checkout is right at v0.6.0; the point is that the fix for B-27's instance in that repo is written, unreleased, and therefore not in the tree the umbrella carries. | 2026-08-13 audit | 1 | 0 | 1 | **1.0** | **closed 2026-08-14** — shipped inside `agent-stack` v0.6.1: both commits that had sat on `main` unreleased — *anchor the description plant on the file's shape, not its wording* and *one CHANGELOG-extraction pattern for the whole family* (B-11) — are released, and the pin moved with them |
| B-33 | **Eleven `sed -i` calls remain inside `seo-aeo-audit`'s `plant()` helper and cannot run on macOS.** BSD sed needs an argument to `-i`. `plant()` takes its command as an argv, so a heredoc cannot be substituted there — three attempts produced a `\|` delimiter colliding with a pattern full of pipes, lost sed escaping on `\*\*`, and a heredoc read at the wrong indentation (`IndentationError` in CI). They ARE guarded: `plant()` calls `plant_guard.py verify`, so a no-op is caught in CI where sed works. What remains is portability — nobody can exercise those eleven rows locally, which is the condition that hid a broken plant for two days. The fix is `python3 -c` one-liners, one row at a time, each verified by running it. | 2026-08-14 iteration 1 | 2 | 0 | 2 | **1.0** | **closed 2026-08-14, seo-aeo-audit v0.17.1** — `test/plant_edit.py` gives `plant()` an argv-shaped edit with three verbs (`sub`, `delline`, `truncate`), literal anchors, each refusing BY NAME when its anchor moved. All eleven converted; **all fourteen plants in that workflow were then watched running on macOS**, which none of the eleven had ever done. A validator guard keeps `sed -i` from returning, anchored on command position — the first draft matched any mention and flagged a step name, a comment and an `echo`, which is standing instruction #7 arriving inside its own fix. The plant for that guard rewrote its own source text twice before it worked, because every literal it names also exists inside it; it targets the last call site now. `truncate` eating the trailing newline was found by the fixture, not by reading. CI success on `2366059`, npm serves 0.17.1. |
| B-36 | **`sheleg-dev` is pinned at 0.4.2 while npm serves 0.4.3, and the installed plugin is already 0.4.3.** The plugin channel reads the marketplace directly, so `npx sshlg-skills@latest update` moved the local install to 0.4.3 while the umbrella still advertises 0.4.2 — the two channels now disagree on this machine. Pre-existing when the 2026-08-14 run found it; that run deliberately did not adopt the pin, because moving one to a release whose gate the run never executed is how *green* comes to read as *verified*. `check_pins.py` reports it (`BEHIND sheleg-dev pinned 0.4.2, @ssheleg/sheleg-dev has 0.4.3`) and is outside `npm test` by design, so nothing blocks on it. | 2026-08-14 run, stage 10 | 2 | 0 | 1 | **2.0** | **closed 2026-08-14 — the premise dissolved, and then was verified rather than assumed.** The pin now sits at `88b4c38` (`v0.4.3-1-g88b4c38`), `skills.json` and the README both say 0.4.3, `check_pins.py` reports `ok sheleg-dev 0.4.3`, and the installed plugin is 0.4.3. The row was filed because moving a pin to a release whose gate this run never executed is how *green* comes to read as *verified* — so that gate was executed: `python3 test/validate.py` inside the pinned submodule prints `OK: sheleg-dev structurally valid (12 checks, 6 skill(s), v0.4.3)`, exit 0. Another session moved the pin; this run supplied the missing evidence rather than inheriting it. |
| B-37 | **`agent-stack` does not use the family's `plant_guard.py`.** `make-skill` v0.17.1 and the umbrella v0.47.1 both ship one implementation of *did the plant actually land*; `agent-stack`'s nine plants assert inline in Python instead, which achieves the same effect but is the fifth careful copy of an idea the family already scripted — the exact shape `plant_guard.py`'s own docstring was written against. | 2026-08-14 run, stage 10 | 1 | 0 | 1 | **1.0** | **closed 2026-08-14, agent-stack v0.7.2** — the shared `plant_guard.py` and its nine fixtures adopted, every one of the eight plants wrapped in `snap`/`verify` with the step's own name as the description. Adopting it **found two plants doing nothing at all**: `cp -R . /tmp/x` into an existing `/tmp/x` nests the tree, so the plant edited a file left by the previous run, and `touch` on an existing file changes neither content nor mode. CI is always fresh, which is why it was invisible there and only ever bit the machine the plants were written on — the same class as B-33's seds. Every copy is `rm -rf`'d first now, and a negative asserts an unchanged copy is reported rather than passed. All eight watched landing locally; CI success on `5ce76c1`, npm serves 0.7.2. |
| B-34 | **`agent-sync`'s id registers read a config key that no shipped config writes, and the mismatch crashes rather than reports.** The script reads `spec.get("nextFreeIdPattern", "")`; every config in this family writes `pattern`. An absent key becomes `re.search("", text)`, which **matches the empty string at position 0** — so `check` takes the found branch and dies with `IndexError: no such group` instead of saying the register has no pattern. A component that never received its input approves rather than refuses: standing instruction #1, in the coordination tool. **It is not only my config:** `skills/task-pipeline/.claude/agent-sync.json` declares `DEC`, `OQ` and `B` with `pattern`, so that repository's `check` crashes the same way and its id reservation has never worked as declared. Two fixes, different owners: the tool should refuse an empty pattern, and the configs should use the documented key. Found 2026-08-14 while turning coordination on. | 2026-08-14 iteration 2 | 2 | 0 | 1 | **2.0** | **closed 2026-08-14, agent-sync v1.11.0** — `id_pattern()` accepts `pattern` (what every shipped config writes) and the legacy `nextFreeIdPattern`, and returns **None** on absence rather than `""`. The empty pattern was the whole defect: `re.search("", text)` matches at position 0, so `check` took the found branch and died with `IndexError: no such group`. Both call sites now report which register has no pattern. Watched failing: the pre-fix script raises that exact `IndexError` on the fixture. |
| B-35 | **Closing a board row while holding its claim is reverted by releasing the lease.** `agent-sync` restores the claimed cell to its pre-claim text verbatim — correctly, that is the protocol — and this repository's claim cell IS the status cell. So `close then release` loses the close, silently: the row reads `open` again and the work looks undone. Caught by `finish`, which reported the board as uncommitted after the release, not by reading. The skill's own doctrine says *closing is not the same as releasing, and the tool does not do it for you*; the sharper statement is that releasing UNDOES a close written in that cell. Either the claim tag moves to a cell nothing else owns, or the order is fixed as release-then-close and written where an agent will read it. | 2026-08-14 iteration 2 | 1 | 0 | 1 | **1.0** | **closed 2026-08-14, agent-sync v1.11.0** — the restore was verbatim; it now removes only THIS run's marker and keeps whatever the cell became. `close then release` preserves the close, `release` with nothing edited still restores the cell exactly, and the note says which happened. Watched failing against the pre-fix script. The ordering advice in stage 10's gate stands regardless — it is cheaper than relying on a tool being current. |
| B-40 | **The board carried four duplicate ids and one row whose Status field read `1.0`, and nothing had ever parsed it.** Found 2026-08-14 by parsing it for the first time: `B-22`, `B-23`, `B-34` and `B-35` each named two unrelated things, and each pair was already cited by number somewhere that cannot be corrected — `B-34` meant the agent-sync id registers in the PUBLISHED CHANGELOG and the sheleg-dev pin in the retro. `B-33` was structurally broken by a bare `\|` inside its own text, which added a cell and shifted every field after it; its subject is a `sed` pattern failing on pipes. **Resolved:** the two uncited closed rows renumbered to `B-38`/`B-39` and the two youngest to `B-36`/`B-37`, each annotated in place; `B-33` escaped; and `check_board_is_parseable()` in `test/validate.py` now fails on a duplicate id or a row whose unescaped-pipe count differs from the table's. The register is cited by id — that is its whole purpose — and for 39 rows nothing checked that an id meant one thing. | 2026-08-14 run, continuation | 2 | 0 | 1 | **2.0** | **closed 2026-08-14** — gate green, and the check was watched failing against each defect it names |
| B-42 | **The claim-tag mechanism cannot tag 19 of this board's 41 ids, and the lease is granted anyway.** `claimTags` is configured `mode: cell` on `docs/evidence/backlog.md`, and `agent_sync.py` collects every ROW CONTAINING the id — so any row that cites another row defeats the tag for the cited id. Cross-referencing is this board's norm, not an accident: **14 rows cite others** (B-02 to B-01, B-10 and B-15 to each other, B-14 to B-11, B-25 to B-35, B-40 to nine). Measured 2026-08-14: 19 ids are mentioned in more than one row and therefore cannot receive a tag. The tool behaves correctly at the moment of refusal — it prints *`B-36` appears in 2 table rows — refusing to guess* and declines rather than guessing (`agent_sync.py:1671-1673`) — **but it still returns `won`**, so an agent that does not read that line believes the row is marked and it is not. The narrowing that exists (`:1663-1669`) only applies on RELEASE, by looking for the marker it wrote. Fix, one line: on acquire, prefer the row whose FIRST cell equals the id — that is what a board table means — and fall back to refusal only if no row's id cell matches. Belongs to `agent-sync`, not here. | 2026-08-14 run, continuation | 2 | 0 | 1 | **2.0** | **closed 2026-08-14** — fixed twice independently on the same afternoon, which is itself the finding. The concurrent session shipped it as `agent-sync` v1.10.1 (a row OWNS an id when its FIRST cell is that id; every other match cites it) and this run had written the same rule minutes earlier. Theirs applies the narrowing AFTER the marker so a release still trusts what it actually wrote — the better ordering — so mine was dropped rather than merged. Kept as a regression test: `test/claim_cell_test.py` case 1 builds a board where `B-01` is cited by two other rows and requires the owning row to be tagged and the citing rows left alone. |
| B-50 | **`agent-orchestrator`'s body is 5670 tokens against the 4750 working budget this pack set itself, and the next addition should split rather than absorb.** Measured with `cl100k_base` at v0.10.0: 502 lines / 5670 tokens, up from 5009. The v0.8.0 notes made this exact argument to justify `agent-harness` as a fourth skill — *"already past the 4750 working limit and could not absorb a paragraph, let alone a layer"* — and then a layer was absorbed. Two duplicated sections (§8's learning tables, §10's prompt assembly) were compressed to offset it and recovered 21 tokens net, because prose is denser per line than the code it replaced. The material itself is in a reference and loads on demand; what is over budget is the always-loaded body. Candidates for the split: the multi-stage pipeline (§5, 691 tokens) and the tool-calling loop (§2, 590). | 2026-08-15 run, measured at release | 2 | 0 | 2 | **1.0** | **closed 2026-08-16** — 5670 → **4728 tokens**, under the 4750 budget for the first time, and by splitting rather than trimming: the planned path and its human interrupt became `references/pipeline.md`, the observability section folded into `runtime.md` beside the streaming contract it was the concrete half of, and sub-agent retry and the learning cycles went to `patterns.md`, whose mechanisms they were the surface of. The body keeps the decisions. The new duplication check (B-52) caught the move leaving one rule in two homes and it was removed. |
| B-51 | **`graphify . --update` cannot run in this environment and every doc-heavy member's graph is therefore frozen.** `agent-stack`'s is from 2026-08-08; the refresh exits 1 with *no LLM API key found (39 doc/paper/image files need semantic extraction)*, and no key is present. `--code-only` is available and is **not** an answer: it would index 10 code files and drop the 39 documents that are most of what these packs are, producing a fresher graph that knows less. Nothing stale ships — `graphify-out/` is gitignored in all three repositories — so the cost is that stage 0's harvest queries a graph a week old and stage 9's graph↔docs divergence check cannot run at all. The decision owed is which: provision a key for the machine, or write down that doc-heavy members keep no graph and stop asking stage 9 for one. | 2026-08-15 run, stage 9 | 1 | 0 | 1 | **1.0** | **open — human step.** Unchanged and correctly so: no code decides whether this machine gets an LLM key or whether doc-heavy members stop keeping a graph. Restated here so it is not read as forgotten. |
| B-52 | **Stamping a run invites the defect it then has to repair.** `retrospective.md` says stamp the run with its own commit; the only way to know that commit is to make it, so the stamp is filled by **amending** — and the amend changes the SHA the stamp just recorded. The result resolves on the machine that wrote it and in no clone, which is standing instruction #10 exactly. It happened twice in one close-out on 2026-08-16, in `task-pipeline` and then in the umbrella twenty minutes later, and both were repaired with follow-up commits. The fix is to the **procedure**: stamp in a follow-up commit rather than by amending, which makes the SHA knowable before it is written. A doctrine edit found at stage 10, filed rather than taken, because stage 10 accounts for a run and does not start work. | 2026-08-16 run, stage 10 | 2 | 0 | 1 | **2.0** | **closed 2026-08-16** — the doctrine states the freeze (*once a file names a SHA, that commit is frozen*), the order that removes the temptation, and the only repair that does not re-enter the loop. The gate now requires **reachability from `HEAD`**, not merely resolution — watched failing on a purpose-built repository whose stamped commit had been amended away. First use of the rule shipped in the same release: v1.60.0's stamp is a commit that follows the work. |
| B-53 | **`сделай дизайн лендинга` reaches no route at all, and the word that would fix it is the word that declines it.** Measured 2026-08-16 with the real matcher while closing B-49: `lib/triggers.js` returns `[]`. The bare `дизайн` cannot be a trigger — `test/triggers_test.js` rejects any trigger that is a substring of a refusal, and «без дизайна» is this route's own opt-out, so a trigger inside it would make the refusal unsayable. A two-word phrase (`дизайн лендинга`) clears the clash check and is precise enough not to steal `напиши текст для лендинга` from `copywriting`, but it costs description budget in another member and, more importantly, raises a question this row does not answer: per the family's composition order a landing page is `super-ux` → `sheleg-design` ∥ `copywriting`, so the right answer may be **three** routes rather than one. Filed as composition, not vocabulary. | 2026-08-16 run, B-49 measurement | 2 | 0 | 2 | **1.0** | **closed 2026-08-16** — `sheleg-design` 1.37.3 + umbrella v0.65.0. `дизайн лендинга` / `design a landing` replace `cinematic landing`, the description having six characters of budget left; `make the hero more cinematic` still routes via `hero` and `напиши текст для лендинга` still goes to copywriting alone. **The composition question is answered no:** `ux-flows` owns how users MOVE through a product and a marketing page has no flow, so `super-ux` is correctly absent from a landing. The unqualified `сделай лендинг` is B-57. |
| B-54 | **A member's own gate cannot see the invariant that its description breaks.** `sheleg-design` 1.37.0 shipped green — 2657 + 1448 + 531 checks — having dropped `фигма в код` from its description while that phrase stayed a live trigger in the umbrella's routing hook. Nothing in the member knows the trigger table exists; the umbrella's `test/triggers_test.js` caught it minutes later and 1.37.1 restored the phrase. The invariant *every trigger is a word its skill advertises* is enforced one repository away from the only file that can break it, and the member releases first. Candidate fixes, in ascending cost: a member-side check that reads the umbrella's table when the checkout is present and discloses when it is not; the umbrella's CI running on member releases; or moving the advertisement assertion into each member. | 2026-08-16 run, caught by the fixture | 2 | 0 | 2 | **1.0** | **closed 2026-08-16** — the first of the three candidate fixes, shipped in umbrella v0.64.0 and seven member patch releases. `test/advertised_check.js` makes the assertion addressable from a member and reads `lib/triggers.js` itself, so no copy of the table travels; all seven members carrying routed triggers call it from their own `validate.py` and **7 of 7 refuse a planted drop**. The negative self-test cannot live in a member — without an umbrella above it the check discloses, so a plant there would assert a refusal that cannot happen — so it lives in the umbrella as `test/advertised_plants.py`, watched failing with one member's call deleted. Kept out of `npm test`: it costs 21 s against a per-commit gate whose honesty is 3.3 s, and both ends of its alternative wiring are asserted. |
| B-55 | **`super-ux` carries derived graph output that no `.gitignore` covers, so the umbrella reads permanently dirty.** `git status` in `sshlg-skills` shows `? skills/super-ux` on a submodule whose pointer is clean; the dirt is an untracked `graphify-out/2026-08-15/` inside it. The pack's own doctrine says the directory is git-ignored and derived — in this member it is not, so every future run must decide whether a dirty submodule matters, and the stage-10 close-out rule that wants every repo clean and pushed now has a permanent false positive. One line in one `.gitignore`. | 2026-08-16 run, stage 10 close-out | 1 | 0 | 1 | **1.0** | **closed 2026-08-16** — and the one-line fix was the wrong one. This member commits the graph on purpose; the uncovered thing was `--update`'s **dated run snapshot**, one folder per refresh duplicating what `git log` already holds. Ignoring `graphify-out/` wholesale, which is what every sibling does and what the row implied, would have deleted a deliberate decision from the only member that made it. `graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/` instead; super-ux `a7fed2a`, no release — the pin is a version and 0.41.3 did not move. |
| B-56 | **The family launcher exits 1 on `sheleg-design` and leaves twelve non-Claude-Code channels on the previous version.** Observed 2026-08-16 immediately after the 1.37.1 release: `npx --yes sshlg-skills@latest update` prints `✗ Failed to update sheleg-design` and exits 1 while every other member succeeds, so the Claude Code plugin reaches 1.37.1 and the hub copy at `~/.agents/skills/sheleg-design` stays at 1.36.1 — with `.kiro` and `.openclaw` symlinked into it, those agents kept serving the exact description B-49 was filed against. Two causes were ruled out by measurement: the run's `GitHub API request limit reached` line disappears under `GH_TOKEN=$(gh auth token)` and the failure does not, and the hub directory is writable and holds the same file set as the source. The hub was brought to 1.37.1 by hand this time, which is the thing that must stop being the answer. **The launcher's own exit code is the only signal, and it is one line in a 900-line log** — a member can go stale for a full release cycle without anything saying so. Wanted: the failure's real cause, and a post-update assertion that every channel serves the version `skills.json` pins. | 2026-08-16 run, post-release ops | 2 | 0 | 2 | **1.0** | **partly closed 2026-08-16** — the durable half shipped and the row's premise turned out to be half wrong. Measuring every channel against its source found the hub **ahead**, not stale: `seo-aeo-audit`'s crash fix sat on `main` untagged for six hours while the pin advertised the version that crashes. `test/release_lag.py` (+6 fixtures, v0.63.0) now discloses any member whose branch has moved past its pinned tag, watched firing on a real one-commit-behind checkout. **Still open: the launcher's own failure.** `✗ Failed to update sheleg-design` reproduces, is not rate limiting (`GH_TOKEN` removes the limit line and not the failure), not size (super-ux is 2.2M/123 files and succeeds), and not the lock record (structurally identical to members that work). It fires only when an update is actually pending, which is why one release exposed it and seven did not — and "pending" is the loop: with the hub copy now byte-identical to the source, the CLI **still** prints `Found 1 global update` and still fails, because its staleness signal is `skillFolderHash` in `~/.agents/.skill-lock.json` and only a *successful* install refreshes it. One failure makes a member permanently pending and permanently failing. All 23 hub skills currently match their sources, one of them by hand. |
| B-57 | **The unqualified `сделай лендинг` reaches no route, and routing it needs two more descriptions to change.** Measured 2026-08-16 while closing B-53: a landing page is `sheleg-design` ∥ `copywriting` plus `task-pipeline`, and of those only `sheleg-design` advertises `лендинг` — `copywriting` advertises the English `landing` and `task-pipeline` neither. So a three-route answer is not reachable through the advertisement invariant without editing two more descriptions, both of which are at or near their 1024-character limit. Left unrouted deliberately rather than half-routed: firing `sheleg-design` alone on a phrase that names no craft would be the hook guessing, and the routing block in global instructions still covers the case. Worth revisiting only if an operator actually types it and notices nothing happened. | 2026-08-16 run, B-53 measurement | 1 | 0 | 2 | **0.5** | open |
| B-58 | **A board row may park work in a place nothing keeps.** B-43 recorded a script, 13 fixtures and 3 negatives as "built and parked at `scratchpad/b29-exposure/`" — a session-scoped temp directory. Two days later every artifact was gone and the row still read as ready-to-merge; the loss surfaced only because a run went to land it. Nothing in `backlog.md`'s doctrine says where parked work may live, so the next row can do the same. Wanted: the board template requires a branch name or a commit SHA for any row claiming work exists, and the validator refuses a `Status` that says parked without one. Small, and it protects the artifact class this repository loses most often. | 2026-08-16 run, B-43 close | 2 | 0 | 1 | **2.0** | **closed 2026-08-16** — `task-pipeline` 1.62.0 + umbrella v0.69.0. `open` claims nothing exists, `parked` claims something does and its status cell carries a branch or commit; an open row may not home work in a per-session directory. The prose detector was measured first — **3 hits on 187 rows, all three false**, two of them closed rows narrating this incident and one the row asking for the rule — and discarded rather than tuned. Both shipped rules measure zero across 191 rows. Guards 347 → 349. |

**Open: 4** — B-07 and B-08 (both waived deliberately) plus B-17 and B-18. B-16 and B-21 closed on 2026-08-13; B-20, B-22 and B-23 opened
and closed inside the same day's runs. Counted by reading the Status column of the table above,
not carried across from the previous edit of this file.

**Open: 8.** B-01 and B-02 opened and closed inside the same run: they live in
a repository another agent held throughout, so this run diagnosed them and
wrote down the evidence instead of touching the code. The other agent shipped
both fixes while it was in flight, and `agent-sync` is pinned at 1.7.0 here as a
result. The finding was still the deliverable — it just had a shorter life than
expected.

**Closed by the 2026-08-10 run:** C-06 — the umbrella now has `CLAUDE.md` house
rules of its own, which is the thing every member already had. B-01, B-02 and
B-03 closed the same day.
