# XR family release integration

Owner: sshlg-skills. Member content/spec is pinned at xr-dev 6a5053253f45916937bde0972e5cae4bcee57007; see ../releases/2026-09-21-xr-lifecycle/README.md for source links and bounded tasks. Operator requested family/site/npm updates.

Contract: skills.json remains the owner of member version, skillNames, role, description and declared npm identity. entry names a shipped command only; xr-dev uses null and exposes skill names. An optional npmPublished=false means npm lookup has not verified a publication; site and llms.txt must say pending, omit the dead npm link and keep GitHub/plugin commands. The flag is removed only after a registry receipt. It does not remove the npm identity or alter installer policy.

SCN-005 in docs/ux/scenarios.md owns the public path. Existing design template and installation channels stay in use. Social previews are generated by the existing site code. The release-set receipt is regenerated, never edited to manufacture a match. Acceptance: family suite, site regression with a synthetic unpublished member, source link review, desktop/mobile browser inspection, remote commit/tag verification, registry distinction, fresh checkout. Device, account and Store acceptance remain outside this knowledge release. Current npm authorization failure must not be hidden by a green tag-fallback pin check.
