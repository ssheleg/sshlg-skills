# agent-sync 1.11.1 @ e311c87 (1 commit past tag v1.11.1) — read-only audit, 2026-08-16
Gate GREEN: `npm test` → exit 0, `PASS: agent-sync v1.11.1 — all checks green` / `SELF-TEST PASS: every injected defect was caught (37 fixtures, 8 at a time)` / `PASS: claim cell + id registers — 6 cases`. Budgets: body 379 lines / ~4996 of 5000 tokens (**4 tokens headroom**), description 773/1024 chars (251 chars headroom).

## F-agent-sync-01 — The published npm package ships a 246 kB `__pycache__` bytecode blob that both `.gitignore` and `.npmignore` were written to exclude
- **Dimension:** budget
- **Severity:** major
- **Evidence:** `npm pack --dry-run` in `/Users/sshlg/DATA/sshlg-skills/skills/agent-sync` lists `npm notice 245.8kB plugins/agent-sync/skills/agent-sync/scripts/__pycache__/agent_sync.cpython-314.pyc` — larger than the 175.7 kB source beside it, 28 files / 231.5 kB package total. The **published** artefact carries it too: `npm pack @ssheleg/agent-sync@1.11.1 && tar tzf ssheleg-agent-sync-1.11.1.tgz | grep pycache` → `package/plugins/agent-sync/skills/agent-sync/scripts/__pycache__/agent_sync.cpython-312.pyc` (a *different* interpreter tag from the local one — it came off the publisher's machine). Both ignore files declare the intent and both are overridden by the `files` whitelist: `.gitignore:3-4` (`*.pyc`, `__pycache__/`), `.npmignore:1-2` (`__pycache__/`, `*.pyc`), `package.json:8-16` (`"files": ["bin/", "plugins/", …]`). `git check-ignore -v plugins/agent-sync/skills/agent-sync/scripts/__pycache__` → `.gitignore:4:__pycache__/` (so it is **not** committed), and `find ~/.claude/plugins/cache/agent-sync ~/.claude/plugins/marketplaces/agent-sync -name "*.pyc"` returns nothing (the plugin channel is a git clone and is clean). The npm channel is the only one affected — and it is the one `npx @ssheleg/agent-sync install` uses.
- **Why it matters:** every npm installer downloads an unreviewed binary built by someone else's Python 3.12, and it is 40 % of the tarball. The repo already decided this file must not travel; the mechanism silently does not enforce it, so the next generated artefact (a `.DS_Store`, a stray `.agent-sync/`) will travel the same way.
- **Fix:** in `/Users/sshlg/DATA/sshlg-skills/skills/agent-sync/package.json`, replace the bare `"plugins/"` entry with explicit negations (`"!plugins/**/__pycache__"`, `"!plugins/**/*.pyc"`) or run `find plugins -name __pycache__ -prune -exec rm -rf {} +` from a `prepack` script, and add a check in `test/validate.py` that shells `npm pack --dry-run --json` and fails on any `.pyc` in the file list.
- **Blast:** 3
- **Effort:** 1

## F-agent-sync-02 — README's own "check what you are actually running" command exits 2 on this machine, and the companion command prints no version at all
- **Dimension:** install
- **Severity:** major
- **Evidence:** `README.md:170-172` instructs: "Check what you are actually running — the plugin and the skill must report the same version" followed by `claude plugin list | grep agent-sync` and `python3 ~/.claude/plugins/cache/*/agent-sync/*/skills/agent-sync/scripts/agent_sync.py --version`. Run verbatim: the glob expands to **four** paths (1.10.0, 1.11.0, 1.11.1, 1.9.0 — `ls -d ~/.claude/plugins/cache/*/agent-sync/*/skills/agent-sync/scripts/agent_sync.py`), so the extras are passed as subcommands and the command dies — `agent_sync.py: error: argument cmd: invalid choice: '/Users/sshlg/.claude/plugins/cache/agent-sync/agent-sync/1.11.0/…/agent_sync.py'`, `EXIT=2`. The other half, `claude plugin list | grep agent-sync`, prints exactly `  ❯ agent-sync@agent-sync` — no version — so the stated comparison cannot be made from either command. Stale caches accumulate and this is what they break: `du -sh ~/.claude/plugins/cache/agent-sync/agent-sync/*` → 528K/680K/312K/372K across four version dirs, three of them dead. The correct answer exists: `python3 ~/.claude/plugins/cache/agent-sync/agent-sync/1.11.1/skills/agent-sync/scripts/agent_sync.py --version` → `1.11.1`.
- **Why it matters:** this is the one command the README gives an operator who suspects a stale channel, i.e. exactly the failure this family has been bitten by repeatedly. It fails, and it fails *more* the longer the machine has been used.
- **Fix:** in `README.md:170-172` replace the glob with a version-resolving form — read `installPath` out of `~/.claude/plugins/installed_plugins.json` for `agent-sync@agent-sync`, or `python3 "$(ls -d ~/.claude/plugins/cache/agent-sync/agent-sync/*/ | sort -V | tail -1)"skills/agent-sync/scripts/agent_sync.py --version` — and drop the claim that `claude plugin list` reports a version.
- **Blast:** 2
- **Effort:** 1

## F-agent-sync-03 — README's single-member update one-liner runs the bare skills-CLI update that this repo's own installer documents as creating a plugin-shadowing plain copy, and never prunes it
- **Dimension:** docs
- **Severity:** major
- **Evidence:** `README.md:157` — `claude plugin marketplace update agent-sync && claude plugin update agent-sync@agent-sync && npx --yes skills update agent-sync --global --yes`. The same repository already records what the third command does: `install.sh:29-30` — "The skills CLI recreates this copy on its own — often as a symlink — even when claude-code was never targeted. **It shadows the plugin and serves a stale skill**" — and `install.sh:31-34` therefore `rm -rf "$HOME/.claude/skills/agent-sync"` after every run. The README one-liner has no such prune. Two more independent records on this machine agree: `/Users/sshlg/CLAUDE.md` ("Bare `npx skills update <name>` re-creates `~/.claude/skills/<name>` as a plain copy and silently shadows the plugin. Use the family launcher.") and `~/.claude/CLAUDE.md` ("Никогда не запускай голый `npx skills update <name>` для скила, который стоит плагином… Проверено на этой машине 2026-08-03: копия осталась на 1.8.0, когда плагин уже был 1.8.1."). Not re-measured here — running it would have created the shadow. The contradiction between `README.md:157` and `install.sh:29-34` is measured and complete.
- **Why it matters:** a user following the README's own update instructions installs the exact permanent-staleness trap the launcher exists to prevent, and nothing in the session tells them: the plugin keeps updating while the shadow serves whatever version it was copied at, forever.
- **Fix:** in `README.md:157` either drop the third command entirely (the plugin channel is what Claude Code reads) or append the prune from `install.sh:31-34`; better, point the whole `<details>` block at `npx --yes sshlg-skills@latest update`, which prunes shadows as its last step.
- **Blast:** 2
- **Effort:** 1

## F-agent-sync-04 — `agent-sync.schema.json` rejects the id-register key the code deliberately accepts and a live config on this machine actually uses
- **Dimension:** docs
- **Severity:** major
- **Evidence:** `agent-sync.schema.json` declares the register object with `"required": ["file", "nextFreeIdPattern"]` and `"additionalProperties": false`. `plugins/agent-sync/skills/agent-sync/scripts/agent_sync.py:204-218` (`id_pattern`) accepts **both** keys and its own docstring states: "The script read `nextFreeIdPattern`; **every config this family ships writes `pattern`**." Validating a real config against the shipped schema (Draft7, `jsonschema`): `/Users/sshlg/DATA/task-pipeline/.claude/agent-sync.json` → **6 errors**, `['idRegisters','DEC'] - Additional properties are not allowed ('pattern' was unexpected)` and `- 'nextFreeIdPattern' is a required property`, repeated for `OQ` and `B`. `agent-sync.example.json:8-16` uses only `nextFreeIdPattern`, and `pattern` appears in no `.md` in the repo (`grep -rn '"pattern"' README.md SKILL.md references/ docs/` → no hits), so the key the tool says every shipped config writes is documented nowhere and forbidden by the published schema. The umbrella backlog row B-34 closed only half of this — "the tool should refuse an empty pattern, **and the configs should use the documented key**".
- **Why it matters:** anyone who wires `$schema` into their `.claude/agent-sync.json` (the example tells them to) gets red squiggles or a CI failure on a configuration the tool runs correctly, and the only two documents that could settle which key is canonical disagree with the code.
- **Fix:** in `agent-sync.schema.json`, change the register object to `"anyOf"` over `pattern` / `nextFreeIdPattern` (or `"required": ["file"]` plus both properties declared), and state the canonical key once in `README.md` and `plugins/agent-sync/skills/agent-sync/SKILL.md`; add a `test/validate.py` check that validates `agent-sync.example.json` **and** a `pattern`-keyed fixture against the schema.
- **Blast:** 3
- **Effort:** 1

## F-agent-sync-05 — `references/hooks.md` tells the reader to remove the hooks by editing `.claude/settings.json`; nothing ever writes them there and a plugin hook cannot be disabled that way
- **Dimension:** hooks
- **Severity:** major
- **Evidence:** `plugins/agent-sync/skills/agent-sync/references/hooks.md:104-107` — "## Removing them / Delete the `hooks` block from the project's `.claude/settings.json`. The skill keeps working…". The hooks are shipped by the plugin, not installed into settings: they live in `plugins/agent-sync/hooks/hooks.json` (SessionStart/PreToolUse×2/PostToolUse/SessionEnd), and `grep -rn "settings.json" install.sh bin/agent-sync.js scripts/agent_sync.py SKILL.md README.md` returns **nothing** — no code path in this repository writes a hooks block anywhere. `/Users/sshlg/CLAUDE.md` records the host behaviour: "Claude Code has no per-hook disable for plugin hooks, so turning the plugin off is the whole remedy."
- **Why it matters:** the SessionStart hook speaks in every session and the PreToolUse guard blocks edits; a user who wants it gone follows the shipped instruction, edits a file that has no such block, and concludes the removal worked while the hooks keep firing. The next step they take is usually to distrust the whole pack.
- **Fix:** rewrite `references/hooks.md:104-107` to name the two real levers — `enabledPlugins["agent-sync@agent-sync"] = false` in `~/.claude/settings.json`, or `claude plugin uninstall agent-sync@agent-sync` (already correct at `README.md:436`) — and state that every hook already self-disables in a project with no `.claude/agent-sync.json` (`hooks/_lib.sh:35-37`).
- **Blast:** 3
- **Effort:** 1

## F-agent-sync-06 — The generated "how coordination is wired here" snapshot omits the only two config keys that make the tool write into repository files
- **Dimension:** docs
- **Severity:** major
- **Evidence:** `setup_snapshot()` at `plugins/agent-sync/skills/agent-sync/scripts/agent_sync.py:2085-2199` renders backend, lease mode, TTL, env file, `idRegisters`, `guardedFiles`, `gates`, `mirror` and `integrationBranch` — and never reads `claimTags` or `mergeLog`, both of which are recognised keys (`agent_sync.py:105-106`) and both of which mutate files: `claimTags` rewrites a table cell (`agent_sync.py:1789-1821`) and `mergeLog` writes `docs/MERGES.md` (`agent_sync.py:1569`). The umbrella declares both — `.claude/agent-sync.json` maps `docs/evidence/backlog.md` to `{"mode":"cell","cell":-1,"held":"{prev} (claimed: {holder})"}` and `mergeLog` to `docs/MERGES.md` / 7 days — and `agent_sync.py check` in `/Users/sshlg/DATA/sshlg-skills` prints `✓ 1 claim-tag mapping(s) declared`, while `/Users/sshlg/DATA/sshlg-skills/docs/AGENT_SYNC.md` mentions neither anywhere in its 93 lines. The snapshot is otherwise current: regenerating it from the same config in a scratch checkout produced a byte-identical body (only the env-file name and the scratch repo's default branch differed), and `config_digest()` recomputed over `.claude/agent-sync.json` → `e135917d592a`, matching the stamp in line 1.
- **Why it matters:** `/Users/sshlg/DATA/sshlg-skills/CLAUDE.md` points at this file as "how coordination is wired here… **Read it before editing a shared registry**". An agent that reads it does not learn that `acquire` will rewrite the last cell of `docs/evidence/backlog.md` — the very file it is about to edit — nor that a merge log with a 7-day compaction exists. That is how B-35 (a close silently reverted by a lease release) was possible.
- **Fix:** extend `setup_snapshot()` in `plugins/agent-sync/skills/agent-sync/scripts/agent_sync.py:2085-2199` with a "### Claims written through to git" section (pattern, cell index, template) and a "### Merge log" line (file, retention), then regenerate `docs/AGENT_SYNC.md` in both `/Users/sshlg/DATA/sshlg-skills` and `/Users/sshlg/DATA/sshlg-skills/skills/agent-sync` with `agent_sync.py setup`.
- **Blast:** 2
- **Effort:** 2

## F-agent-sync-07 — CONTRIBUTING's two-process contention recipe never enters the scratch repository, so it takes an unreleased lease in the contributor's own checkout
- **Dimension:** docs
- **Severity:** major
- **Evidence:** `CONTRIBUTING.md:25-37`. Line 27 creates `T=$(mktemp -d)` and inits git **inside a subshell**; line 28 (`python3 "$S" status`) and lines 35-36 (`AGENT_SYNC_RUN_ID=alpha python3 "$S" acquire T-1` / `…beta … acquire T-1`) carry no `cd`, so the working directory is still the agent-sync repository. Only line 29 uses `(cd "$T" && …)`. `project_root()` at `plugins/agent-sync/skills/agent-sync/scripts/agent_sync.py:134-136` is `git rev-parse --show-toplevel` from the cwd, so those commands target `/Users/sshlg/DATA/sshlg-skills/skills/agent-sync` — which is initialised (`.claude/agent-sync.json` present, `agent_sync.py check` → `setup healthy (6 checks passed, 1 warning(s))`). Not executed here, on instruction: running it would have taken a real lease. The recipe also never releases `T-1`, and the repo's TTL is 2700 s.
- **Why it matters:** the documented demonstration of the protocol leaves a 45-minute abandoned lease on the contributor's own checkout, blocking their edits to nine guarded patterns — the exact failure the tool exists to prevent, taught by its own contributing guide.
- **Fix:** in `CONTRIBUTING.md:25-37` hoist a single `cd "$T"` before the demonstration (or prefix every line with `(cd "$T" && …)` as line 29 already does) and end the block with the two `release T-1` calls.
- **Blast:** 2
- **Effort:** 1

## F-agent-sync-08 — CONTRIBUTING says five version surfaces move together; the validator enforces six, and the missing one is exactly what forced the 1.11.1 patch release
- **Dimension:** docs
- **Severity:** major
- **Evidence:** `CONTRIBUTING.md:56-59` — "Five places move together, and the validator enforces it: `.claude-plugin/marketplace.json` · `plugins/agent-sync/.claude-plugin/plugin.json` · `package.json` · the top `CHANGELOG.md` entry · `metadata.version` in `SKILL.md`." `check_version_sync()` at `test/validate.py:235-263` collects **six**: those five plus `versions["agent_sync.py"]`, read from `^VERSION\s*=\s*"…"` at `test/validate.py:251-253` (the value at `plugins/agent-sync/skills/agent-sync/scripts/agent_sync.py:36`). The omission is not academic — the commit that produced 1.11.1 says so: `git show 45a3ffd` → "fix: two version surfaces the bump driver could not see; v1.11.1 … so this member tagged with **four of six** surfaces moved and CI refused it."
- **Why it matters:** the contributor doc is where a release driver is written from. It under-reports the contract by one surface, the one whose drift makes `status` print a stale version to every session, and that has already cost a tag rewrite and a patch release.
- **Fix:** amend `CONTRIBUTING.md:56-59` to six surfaces, naming `VERSION` in `plugins/agent-sync/skills/agent-sync/scripts/agent_sync.py`, and note that both quoted and bare forms must be matched by any bump driver.
- **Blast:** 2
- **Effort:** 1

## F-agent-sync-09 — The verification ledger claims to record how each requirement is verified "now" and stops four releases ago, never mentioning the second test file `npm test` runs
- **Dimension:** docs
- **Severity:** major
- **Evidence:** `docs/evidence/verification.md:3` — "One row per shipped requirement, and how it is verified **now** — not how it was verified once" — and `:6` — "`check_*` names are functions in `test/validate.py`, run by `npm test`". Its version sections (`grep -n "^## "`) are v1.5.3, v1.6.0, v1.7.0, v1.7.1, then "Standing gaps"; the shipped version is **1.11.1**, and every "Last run" cell reads `2026-08-10`. `npm test` today is three commands (`package.json:18`) and the third, `test/claim_cell_test.py`, contributes 6 cases (`PASS: claim cell + id registers — 6 cases`) covering B-42, B-35 and B-34 — `grep -c "claim_cell" docs/evidence/verification.md` → **0**. Four releases of new checks (1.8.x, 1.9.0, 1.10.x, 1.11.x, including `check_routed_triggers_still_advertised` at `test/validate.py:2058`) have no row.
- **Why it matters:** the ledger is the artefact anyone consults to ask "what actually proves this still works". It answers for a tree four releases old while asserting it answers for the current one, so a real coverage gap and a merely-unrecorded check are indistinguishable.
- **Fix:** add sections for v1.8.0 → v1.11.1 to `docs/evidence/verification.md`, list `test/claim_cell_test.py`'s six cases beside the `check_*` functions, refresh the "Last run" column from an actual `npm test`, and add a `test/validate.py` guard that fails when the top `CHANGELOG.md` version has no section in the ledger.
- **Blast:** 2
- **Effort:** 2

## F-agent-sync-10 — `v1.11.1` was force-moved as a lightweight tag while every other release tag is annotated, so `git describe` identifies this build as v1.11.0
- **Dimension:** version
- **Severity:** minor
- **Evidence:** `git cat-file -t v1.11.1` → `commit` (lightweight); `v1.11.0`, `v1.10.1`, `v1.10.0`, `v1.9.0` → `tag` (annotated). Consequence, both locally and on the remote: `git describe` → `v1.11.0-5-ge311c87` while `git describe --tags` → `v1.11.1-1-ge311c87`. That is the banner discrepancy this audit was asked to resolve — the session banner reads `agent-sync 1.11.1 — sshlg-skills@466a364` because it prints `VERSION` from `agent_sync.py:36` (`agent_sync.py:2438`), not `git describe`. The remote confirms the tag object type: `git ls-remote --tags origin` shows a `^{}` peel line for `v1.11.0`, `v1.10.1`, `v1.10.0`, `v1.9.0`, `v1.8.3`… and **none** for `v1.11.1` (`45a3ffd… refs/tags/v1.11.1` alone). Cause is recorded in the tagging commit: "so the tag is **moved** rather than superseded" (`git show 45a3ffd`) — a `git tag -f` without `-a` degrades the object. Everything else agrees: `package.json` / `plugin.json` / `marketplace.json` / top CHANGELOG heading / `SKILL.md metadata.version` / `agent_sync.py VERSION` = 1.11.1, npm `latest` = 1.11.1, GitHub release `v1.11.1 — agent-sync` exists, the umbrella pins `"version": "1.11.1"` in `skills.json` and `README.md:38`, and the pinned submodule commit `45a3ffd` **is** the v1.11.1 tag. HEAD is 1 commit ahead of the tag (`e311c87`, `.gitignore` only, no version surface touched).
- **Why it matters:** `git describe` is how a build is identified in CI logs, issue reports and release scripts, and it will keep naming every future commit `v1.11.0-N` until the next annotated tag lands. It also breaks the family's own habit of reading `git describe` to check whether a member is ahead of its release.
- **Fix:** re-cut the tag annotated — `git tag -f -a v1.11.1 45a3ffd -m "v1.11.1" && git push --force origin v1.11.1` — and add `-a` (or `-s`) to whatever tags in `.github/workflows/release.yml` and to the family's bump driver so the next moved tag does not degrade.
- **Blast:** 2
- **Effort:** 1

## F-agent-sync-11 — CONTRIBUTING says the self-test injects five defects; it injects thirty-seven
- **Dimension:** docs
- **Severity:** minor
- **Evidence:** `CONTRIBUTING.md:19-21` — "It copies the tree, injects **five** defects one at a time — an over-cap description, a version drift, a leaked host name, a token passed in `argv`, a stray `SKILL.md` — and requires each to be caught." Measured by running the command the same page names: `python3 test/validate.py --self-test` → `SELF-TEST PASS: every injected defect was caught (**37 fixtures**, 8 at a time)`, and `grep -c "self-test \[" ` over its output → 37. The named five are all still there, but so are 32 more (`the guard stops seeing commits`, `merge releases leases it did not land`, `check blesses a register nobody can reserve`, …).
- **Why it matters:** the number is the whole claim — "a validator that cannot fail is decoration" is argued from it, and a contributor reading five will not think to add a fixture for the sixth thing they change.
- **Fix:** in `CONTRIBUTING.md:19-21` state the count as computed rather than listed — keep the five examples, replace "five defects" with the measured total and say it is read from the run's own last line.
- **Blast:** 1
- **Effort:** 1

## F-agent-sync-12 — The Bash half of the commit guard is reachable only through an `if` permission filter that cannot express the command shapes `guard.sh` was rewritten to catch
- **Dimension:** hooks
- **Severity:** minor
- **Evidence:** `plugins/agent-sync/hooks/hooks.json` has two `PreToolUse` entries; the file-edit one (`matcher: "Edit|Write|MultiEdit|NotebookEdit"`) has no filter, but the commit one is `{"matcher": "Bash", "if": "Bash(git commit *)"}` — so a Bash payload reaches `guard.sh` only if that rule matches. `hooks/guard.sh:23-32` records why the guard's *own* parser was rewritten away from a prefix/substring test: "the old test was `case "$cmd" in *"git commit"*)`, a CONTIGUOUS substring. `git -C <dir> commit` does not contain it, so **every commit made that way skipped the guard entirely** … Measured 2026-08-07: a full day of commits to guarded registers", and it now tokenises so that "`git -c user.name=x -C dir commit` must" match. Verified by feeding `guard.sh` directly: a `{"tool_name":"Bash","tool_input":{"command":"git commit -m x"}}` payload with a guarded file staged → `exit=2` with the reason; malformed and empty stdin → `exit=0` (fails open correctly). Suspected, not confirmed: that Claude Code's `if` rule fails open on a command it cannot parse and does not match `git -C dir commit` — I did not measure the host's rule engine. The family's own house rules assert it (`/Users/sshlg/DATA/sshlg-skills/CLAUDE.md:57-60`: "Nothing depends on a hook entry's `if` filter: the reference states it is best-effort and **fails open** on a command it cannot parse") and `make-skill`'s reference calls `if` "a permission rule" (`skills/make-skill/.../references/host-capabilities.md:86`).
- **Why it matters:** if the assertion in the family's own CLAUDE.md holds, the forty lines of tokenising in `guard.sh` never run for the shapes they exist for, and the 2026-08-07 failure is reachable again one layer up from where it was fixed.
- **Fix:** drop `"if": "Bash(git commit *)"` from `plugins/agent-sync/hooks/hooks.json` and let `guard.sh` decide (it already exits 0 promptly for `ls` and for any non-commit payload); if the per-call cost matters, gate on a cheap in-script substring test for the word `commit` before invoking python, and record the measured latency next to B-005's 130–220 ms.
- **Blast:** 3
- **Effort:** 1

## F-agent-sync-13 — In a top-level repository the generated snapshot tells agents to reserve ids "in the parent repository", which does not exist
- **Dimension:** docs
- **Severity:** minor
- **Evidence:** `plugins/agent-sync/skills/agent-sync/scripts/agent_sync.py:2140` emits, unconditionally whenever `idRegisters` is empty, `"None declared here. Ids live in the parent repository; reserve them there."` — with no test for whether the repository has a parent. It lands in `/Users/sshlg/DATA/sshlg-skills/docs/AGENT_SYNC.md:26`, and `sshlg-skills` is the umbrella: it is the parent, and `git ls-tree HEAD skills/agent-sync` shows it holding the members as gitlinks. (In the member's own `skills/agent-sync/docs/AGENT_SYNC.md:26` the same sentence is correct.)
- **Why it matters:** `/Users/sshlg/DATA/sshlg-skills/CLAUDE.md` directs agents to read this file before touching a shared registry; the one who needs an id is sent looking for a repository above the top.
- **Fix:** in `setup_snapshot()` (`agent_sync.py:2139-2141`) branch on whether the checkout is a submodule (`git rev-parse --show-superproject-working-tree` non-empty) and emit "Ids are not reserved here — none are declared" for a top-level repository; regenerate both snapshots with `agent_sync.py setup`.
- **Blast:** 1
- **Effort:** 1

## F-agent-sync-14 — The committed design spec is an orphan that names the wrong publication repository and whose eleven relative links all dangle
- **Dimension:** docs
- **Severity:** minor
- **Evidence:** `docs/evidence/specs/2026-07-29-agent-sync-design.md:5` — "**Deliverable:** a distributable Claude Code plugin + Agent-Skills package, published at `github.com/appvillis-com/agent-sync`" — and `:420` — "`github.com/appvillis-com/agent-sync` public, CI green." The repository moved: `package.json`, `plugin.json`, `marketplace.json` and `README.md` all say `github.com/ssheleg/agent-sync`, and `CHANGELOG.md:767-778` records the move ("The repository moved from `appvillis-com/agent-sync` to **`ssheleg/agent-sync`** … so the `appvillis-com` name is never re-registered"). Every relative link in the file resolves to nothing — 11 of them, all of them in this file and none anywhere else in the repo: `:39,:206,:207,:390,:407,:408,:413 → ../../DECISIONS.md`, `:52,:410 → ../../../AGENTS.md`, `:411 → ../../WORKSTREAMS.md`, `:411 → ../../../CLAUDE.md` (paths from the `nicegram-business` layout it was written in). And nothing links to it: `grep -rn "agent-sync-design\|evidence/specs" --include=*.md --include=*.json --include=*.mdc` over the repo returns no hit outside the file itself.
- **Why it matters:** it is not in the npm tarball (`package.json` `files` excludes `docs/`) but it is on GitHub, unreachable from any index and asserting an address the CHANGELOG explicitly warns must never be trusted again.
- **Fix:** either link it from `README.md` and repair the six broken targets, or mark it historical in a one-line header (`> Superseded — written before the move from appvillis-com; kept for the reasoning, not the paths`) and strip the two `appvillis-com` URLs at `:5` and `:420`.
- **Blast:** 1
- **Effort:** 1

## F-agent-sync-15 — CHANGELOG.md carries two different `## 1.3.1` sections describing unrelated changes
- **Dimension:** docs
- **Severity:** minor
- **Evidence:** `grep -n "^## 1.3.1" CHANGELOG.md` → `852:## 1.3.1 — 2026-07-29` and `873:## 1.3.1 — 2026-07-29`. The first is "The git lease was invisible to everything that reads a lease — fixed"; the second is "Ignoring the state directory does nothing once git is tracking it". Parsing every heading: 48 version headings, exactly one duplicate. The release path extracts notes by version — `check_release_notes_are_extractable` at `test/validate.py:1631-1670` runs the workflow's own awk program — but only against the version in `package.json`, so a duplicate heading is invisible to the gate; the self-test fixture that exists is "the extracted section runs past its own version", not "two sections claim one version".
- **Why it matters:** the tag `v1.3.1` has exactly one meaning and the changelog gives it two, so a reader diffing a release against its notes gets half of them. The extraction will silently take the first block for any version this happens to again.
- **Fix:** renumber one of the two sections in `CHANGELOG.md:852` / `:873` to the version it actually shipped in (or fold them into one `1.3.1` section), and add a check in `test/validate.py` that fails on a duplicate `^## v?X.Y.Z` heading — it is three lines beside `check_release_notes_are_extractable`.
- **Blast:** 1
- **Effort:** 1

## F-agent-sync-16 — `references/backend-fs.md` enumerates what follows from `atomicAppend: false` and omits the one consequence that makes a shipped command fail every time
- **Dimension:** docs
- **Severity:** minor
- **Evidence:** `plugins/agent-sync/skills/agent-sync/references/backend-fs.md:22-33` states the capability (`{"atomicAppend": false, …}`) and then "## What follows from that" with a closed list of three items — says so at session start, keeps the lease as configured, marks runs `ungated`. Reserving is not among them. Measured against a scratch project with `"backend":"fs"` and one `DEC` register: `agent_sync.py reserve DEC` → `agent-sync: backend 'fs' cannot reserve ids safely (atomicAppend is false). Allocate by hand and record it, or configure a cloud backend. Pretending would hand two agents the same id.`, and `agent_sync.py check` → `✗ 1 id register(s) declared, but backend 'fs' cannot reserve ids (atomicAppend is false). \`reserve\` fails every time here`. `README.md:459-460` sends the reader to this exact file "when running without a cloud backend, or explaining degraded mode". Umbrella backlog B-45 is this same gap seen from the other side, closed 2026-08-16 by writing the manual procedure into `task-pipeline`'s CLAUDE.md rather than into this reference.
- **Why it matters:** the tool refuses honestly and the document that explains the degraded mode does not mention the refusal, so a project declaring registers over `fs` discovers at `reserve` time — mid-task, holding a lease — that its id allocation was never going to work.
- **Fix:** add a fourth item to `references/backend-fs.md:26-33`: `reserve` and `release-id` are unavailable over `fs`, `check` refuses a declared register, and the manual procedure is lease → compute from the **committed** register → commit before releasing. Cross-link it from `references/lease-protocol.md`'s id-allocation section.
- **Blast:** 2
- **Effort:** 1

## F-agent-sync-17 — The knowledge graph is a commit behind HEAD after the umbrella declared every graph "at HEAD", and its report names a different build commit than the graph beside it
- **Dimension:** graph
- **Severity:** minor
- **Evidence:** `graphify-out/graph.json` → `built_at_commit = 45a3ffd19120d9ee695032eb4ec707f29e0030d0`; `git rev-parse HEAD` → `e311c874…`; `git rev-list --count 45a3ffd..HEAD` → **1**. The umbrella's `CHANGELOG.md:3-7` (v0.79.0) claims "All nine … every one now sits **at HEAD**". The intervening commit is the `.gitignore` change shipped by that same release (`e311c87`, 2026-08-16 20:55), committed 13 minutes after the graph was written (`graph.json` mtime 20:42), so the claim was already false for this member when it was written. Separately, two files in the same output directory disagree: `graphify-out/GRAPH_REPORT.md:12` says "Built from commit: `18b77e37`" (and its title says 2026-08-15, mtime `Aug 15 16:41`, as does `graph.html`) while `graph.json` beside it says `45a3ffd` — two commits later. `graphify-out/` is untracked and ignored (`git check-ignore -v graphify-out` → `.gitignore:5:graphify-out/`; `git ls-files graphify-out | wc -l` → 0).
- **Why it matters:** the report tells its reader "Run `git rev-parse HEAD` and compare to check if the graph is stale" and then gives them the wrong baseline, and the umbrella's release notes assert a currency that was not measured per member.
- **Fix:** re-run `graphify update .` in `/Users/sshlg/DATA/sshlg-skills/skills/agent-sync` so `graph.json`, `GRAPH_REPORT.md` and `graph.html` share one `built_at_commit`, and change the umbrella's "at HEAD" wording in `CHANGELOG.md` / `docs/evidence/verification.md` to a per-member measured lag (`git rev-list --count <built_at_commit>..HEAD`) rather than a blanket claim.
- **Blast:** 1
- **Effort:** 1

TOTAL: 17 findings (0 blocker, 9 major, 8 minor)
