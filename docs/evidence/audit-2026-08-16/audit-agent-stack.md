# Audit — agent-stack 0.11.1 · repo HEAD `df3c9a8` (1 commit past tag `v0.11.1` = `ece85b5`) · 2026-08-16
Verified clean and NOT re-reported below: version agrees across all eight surfaces (package.json / plugin.json / marketplace.json / CHANGELOG top / newest tag / umbrella `skills.json` / umbrella README row / `npm view` → all `0.11.1`); `claude plugin validate . --strict` and `plugins/agent-stack --strict` both pass; the manifest declares no hooks, commands or agents and none ship, so nothing is unreachable; `npm pack --dry-run` ships 32 files including all four SKILL.md and all 20 references; all 20 references over 100 lines carry `## Contents`; 26 relative markdown links, 65 backticked paths (3 exceptions in F-04) and 35 in-file anchors resolve; 27 external URLs return 200; **no model id, price or context-window figure is asserted anywhere in the pack** — `agent-orchestrator/references/patterns.md:202-210` deliberately deleted the one table of vendor ids on 2026-08-15 and says why, and the only model literal left is `claude-opus-4` in `scripts/audit_agent.py:305`, a negative fixture for the hardcoded-model detector; MCP `2026-07-28` is what `specification/latest` serves today and `mcp.md:152-157`'s deprecation table matches the live register row for row, `mcp-scale.md:28,39`'s ~150,000 / ~2,000 tokens and 1%–5% are verbatim from the live client-best-practices page, all five `anthropics/claude-code` CHANGELOG quotes in `graph-engineering.md:232-238` are verbatim and nothing in 2.1.230–2.1.233 changes them, A2A v1.0(.1), agentgateway 1.4.1, MCP Registry schema `2025-12-11` + API `v0.1` and Pi's `reserveTokens` 16384 / `keepRecentTokens` 20k / `createAgentSession` / `ModelRuntime` / `SessionManager` / `AgentSessionRuntime` / `ExtensionAPI` all confirmed against the live sources; the B-52 duplication rule IS enforced (`test/validate.py:253-278`, unconditional, in CI) and currently holds — 24 documents, 276 pairs, largest overlap 12 runs of 12 words against a floor of 20; `graphify-out/graph.json` is `built_at_commit ece85b5` = tag `v0.11.1`, 1 commit behind HEAD (that commit is the `.gitignore` chore `df3c9a8`), gitignored and untracked, so B-51's "at HEAD" holds in substance; installed plugin is `0.11.1` at `gitCommitSha ece85b5`, enabled, the four hub copies at `~/.agents/skills/` are byte-identical to source (`diff -rq` silent), and no plain copy of any of the four exists under `~/.claude/skills/` to shadow it. Not checked: `promptingguide.ai` technique claims and the *Graph Engineering with Claude* article's own content (link 200s, content not read); the ACP→A2A merge date `2025-08-29`; the "one vendor reports about half" figure at `agent-evals/SKILL.md:94`, which names no vendor and cannot be resolved.

## F-agent-stack-01 — `npm test`, the gate the family states, does not exist in this member and nothing at the umbrella runs its real one
- **Dimension:** gate
- **Severity:** major
- **Evidence:** `package.json` has no `scripts` key at all (keys are `name, version, description, bin, publishConfig, files, repository, homepage, license, author, engines, keywords`); `npm test` → `npm error Missing script: "test"`, exit 1. The real entry points, run: `python3 test/validate.py` → `OK: agent-stack structurally valid (10 checks, 4 skill(s), v0.11.1)`, exit 0; `python3 test/plant_guard_test.py` → 9 cases, `PASS: plant_guard — 9 cases`, exit 0. Family-wide `scripts.test`: `agent-sync`, `make-skill`, `sheleg-design`, `sheleg-dev`, `task-pipeline` have one; `agent-stack`, `seo-aeo-audit`, `super-ux` have none. The umbrella's own `test/validate.py:651-669` asserts a `scripts.test` and a `run: npm test` CI step **for the umbrella only**, never for a member. The umbrella's cross-member sweep `test/advertised_plants.py:65-72` runs `skills/<member>/test/validate.py` only for members present in `lib/triggers.js` `ROUTES`, and `node test/advertised_check.js --member agent-stack --root skills/agent-stack` → `ok: agent-stack front matter is clean; it carries no routed triggers`, so agent-stack is skipped there too. Net: this member's suite runs only in its own GitHub Actions, never from any command an operator or agent runs locally.
- **Why it matters:** an agent told "the gate is `npm test`" gets exit 1 here and either believes the member is broken or skips gating it; a change made in the submodule and committed from the umbrella passes every local check while nothing ever ran the member's own validator.
- **Fix:** add `"scripts": {"test": "python3 test/validate.py && python3 test/plant_guard_test.py"}` to `/Users/sshlg/DATA/sshlg-skills/skills/agent-stack/package.json`, add a `run: npm test` step to `.github/workflows/validate.yml` in place of the two separate `run:` lines (keeping the `^\s*run:\s*python3\s+test/validate\.py\s*$` guard at `test/validate.py:302` satisfied or updating it in the same change), and extend the umbrella's `test/validate.py` to require `scripts.test` of every entry in `skills.json`.
- **Blast:** 2
- **Effort:** 1

## F-agent-stack-02 — `CONTRIBUTING.md` documents a different repository: eleven named files, one required command and one validator constant do not exist
- **Dimension:** docs
- **Severity:** major
- **Evidence:** the file is a near-verbatim copy of `seo-aeo-audit`'s. `CONTRIBUTING.md:3` — "twenty-one reference contracts plus a small standard-library auditor"; the pack ships **20** (`for f in */references/*.md` → agent-harness 7, agent-interop 6, agent-orchestrator 7, agent-evals 0), which `README.md:14` states correctly as "twenty references". `CONTRIBUTING.md:12-14` says four evidence-tier definitions are "quoted verbatim from `plugins/agent-stack/skills/agent-stack/references/evidence-tiers.md`, which is their single home. `test/validate.py` fails if the two copies drift" — `find . -name 'evidence-tiers.md'` returns nothing, there is no `skills/agent-stack` skill directory (the four are `agent-evals`, `agent-harness`, `agent-interop`, `agent-orchestrator`), and no such comparison exists in `test/validate.py`. `CONTRIBUTING.md:52-60` — "Both must pass: … `python3 test/test_page_audit.py`"; `ls test/` → `plant_guard.py  plant_guard_test.py  validate.py`. `CONTRIBUTING.md:62-67` names `deliverable-templates.md`, `scripts/page_audit.py` and a URL-scheme guard against `urlopen`; none exist. `CONTRIBUTING.md:32,91-98` route contributions to `benchmarks.md`, `technical-checks.md`, `aeo-geo.md`, `growth-plays.md`, `myths.md`, `threats-and-defense.md`, `algorithm-updates.md` and "A Google update" — every one MISSING by `find`. `CONTRIBUTING.md:101` — "`REQUIRED_REFERENCES` in the validator"; `grep -c REQUIRED_REFERENCES test/validate.py` → `0`.
- **Why it matters:** a contributor follows the stated gate, gets `can't open file 'test/test_page_audit.py'`, and has no way to tell a broken checkout from a wrong document; every routing row sends a real change to a file that does not exist, and the evidence-tier contract the file opens with is unenforceable because its named single home is fictional.
- **Fix:** rewrite `/Users/sshlg/DATA/sshlg-skills/skills/agent-stack/CONTRIBUTING.md` against this repository: gate = `python3 test/validate.py` + `python3 test/plant_guard_test.py` (or `npm test` once F-01 lands), reference count 20, routing rows naming the four skills' own references, and the real invariants (`PROTOCOL_PINNED` spec stamps at `test/validate.py:44-49`, the one-home-per-fact floor at `:244-246`, both-direction reference linkage at `:194-211`).
- **Blast:** 2
- **Effort:** 1

## F-agent-stack-03 — the plugin and marketplace manifests advertise "Two skills" to every installer while four ship
- **Dimension:** docs
- **Severity:** major
- **Evidence:** `plugins/agent-stack/.claude-plugin/plugin.json:4` and `.claude-plugin/marketplace.json:13` carry the identical string `"Two skills: agent-orchestrator — … ; and agent-evals — run/trace/thread evals, judges, and fixtures grown from production."`. Shipped: `ls plugins/agent-stack/skills` → `agent-evals agent-harness agent-interop agent-orchestrator`, and `npm pack --dry-run` lists all four `SKILL.md` plus their references. The umbrella already has this right — `skills.json` says "in four skills" and `skillNames` lists four — so the stale copies are the member's own, which is exactly the half umbrella backlog **B-48** left to the member ("writing a member's own description belongs to that member"). `package.json:3` is the same class one step weaker: it describes only the orchestrator and never mentions evals, interop or harness.
- **Why it matters:** the plugin.json description is what `/plugin` and the marketplace listing show; a user installing agent-stack is told the package has no interop and no harness skill, so the two newest skills are discoverable only by accident after install.
- **Fix:** replace the description in `plugins/agent-stack/.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` with a four-skill sentence (the umbrella's `skills.json` `desc` is already written and correct — reuse it), refresh `package.json:3` in the same change, and extend `test/validate.py` to fail when the manifest description names a skill count that disagrees with `len(skill_dirs)`.
- **Blast:** 3
- **Effort:** 1

## F-agent-stack-04 — two references send the reader to the file they are already reading and tell them the content is "not here"
- **Dimension:** docs
- **Severity:** major
- **Evidence:** `plugins/agent-stack/skills/agent-orchestrator/references/patterns.md:445-446` — "The extractors, the exact confidence arithmetic and the promotion query live in `references/patterns.md` — **Learning Extraction Heuristics**, **Confidence Management** and **Cross-Resource Learning Transfer** — and not here". This is `references/patterns.md`, and those three sections are its own `:241`, `:270` and `:350` (`grep -n '^## ' patterns.md`). `plugins/agent-stack/skills/agent-orchestrator/references/runtime.md:121` — "What makes the feed reliable rather than decorative is in `references/runtime.md`: a monotonic id per event…"; this is `references/runtime.md`, and those properties are its own `:80` and `:99`. Both paragraphs moved out of `SKILL.md` on 2026-08-16 in the B-50 split and kept their SKILL.md-relative pointer — the files say so themselves at `patterns.md:454` and `runtime.md:126`. A third instance is the same mechanical class but a genuine sibling: `references/pipeline.md:17` cites `references/runtime.md`, which does not resolve from inside `references/`; every other sibling citation in the pack uses the bare filename (`system-prompt.md`, `layers.md`, `mcp.md`, `context-engineering.md`, `patterns.md`, …). Nothing catches these: `test/validate.py:203` only scans `SKILL.md`, and its lookbehind deliberately ignores prefixed forms.
- **Why it matters:** an agent doing progressive disclosure reads "the mechanism is in X and not here", opens X, and lands on the file it already has — so it either loops or concludes the material is missing, in the two files that hold the confidence arithmetic and the streaming contract.
- **Fix:** in `patterns.md:445` and `runtime.md:121` drop the pointer and name the section inside the same file (`— see **Learning Extraction Heuristics** below`), correct `pipeline.md:17` to the bare `runtime.md`, and extend `test/validate.py`'s reference check to scan `references/*.md` too, failing on a `references/<name>.md` written inside `references/`.
- **Blast:** 3
- **Effort:** 1

## F-agent-stack-05 — the token figures this member's own verification ledger certifies do not reproduce at any commit under the encoding it names
- **Dimension:** docs
- **Severity:** major
- **Evidence:** `docs/evidence/verification.md:19` REQ 001 — "`cl100k_base` count of `agent-orchestrator/SKILL.md`, before and after | **5670 → 4728 tokens** against the 4750 budget — under it for the first time | **verified**", and `:66` restates the 5670 ("502 lines / **5670 tokens** … up from 5009 — measured with `cl100k_base`, not estimated"). Measured today with `tiktoken.get_encoding('cl100k_base')` on the body after front matter: v0.10.0 = **5005** (whole file 5304, 502 lines — the line count reconciles, the token count does not); v0.11.0 = v0.11.1 = HEAD = **4143** (whole file 4442). `git diff --stat v0.11.0..HEAD -- plugins/` shows only `plugin.json` changed, so the file measured at v0.11.0 is the file today. Every commit that ever touched the file: 4143, 5005, 4432, 4489, 4146, 4146, 5361 — **neither 5670 nor 4728 appears anywhere**. Other encodings on HEAD, for completeness: `o200k_base` 4143/4424, `p50k_base` 4751/5127, `r50k_base` 5065/5441. The chars÷3.9 figure this audit was asked for is **4570** body tokens (17,822 body chars), leaving 180 under 4750; by the ledger's own `cl100k_base` the headroom is 607. The conclusion "under 4750" holds; the receipt does not. Umbrella backlog `B-50` restates the identical wrong pair.
- **Why it matters:** the ledger's contract is that a row sits at `never` until somebody watched the check pass on the shipped artifact — a row marked `verified` whose two numbers cannot be reproduced by the command it names is the exact failure the umbrella's own `CLAUDE.md` Evidence section records ("both restated numbers were wrong and the counted one was right"), and the next person sizing a split will trim against a baseline that is 1,500 tokens off.
- **Fix:** re-measure and correct `docs/evidence/verification.md:19` and `:66` (and umbrella `docs/evidence/backlog.md` row B-50) to the counted values — v0.10.0 body 5005 → v0.11.1 body 4143 under `cl100k_base` — and add the measuring command to the row so the number can be recomputed rather than restated.
- **Blast:** 2
- **Effort:** 1

## F-agent-stack-06 — the 4750-token body budget the pack states it is "held under" is enforced by nothing
- **Dimension:** budget
- **Severity:** minor
- **Evidence:** `plugins/agent-stack/skills/agent-orchestrator/SKILL.md:21-22` — "it is **held under a 4750-token budget** — a body that grows absorbs the layer that should have been split, and this one did until 2026-08-16". `test/validate.py` contains no length or token check on any body; the only size check in the file is `len(fm_desc) > 1024` at `:186-190`. The budget's history is what a missing gate looks like: 4432 tokens at v0.8.0 and v0.9.0, **5005** at v0.10.0 and v0.10.1 — over budget through two tagged releases with nothing reporting it — then 4143 at v0.11.0 (all `cl100k_base`, body after front matter). Today's headroom is 607 tokens by `cl100k_base`, 180 by chars÷3.9.
- **Why it matters:** the same drift recurs the next time a layer is absorbed, and it will again be found by a human audit two releases later rather than by the commit that caused it.
- **Fix:** add a body-size check to `/Users/sshlg/DATA/sshlg-skills/skills/agent-stack/test/validate.py` beside the description check at `:184-192` — a stdlib-only proxy (chars ÷ 3.9, threshold set from the measured `cl100k_base` ratio) so the gate stays dependency-free and works offline — with a negative self-test in `.github/workflows/validate.yml` alongside the existing over-long-description plant.
- **Blast:** 1
- **Effort:** 1

## F-agent-stack-07 — the descriptions of three of four skills have no room for another routed trigger, and one is five characters from the hard cap
- **Dimension:** budget
- **Severity:** minor
- **Evidence:** measured with `test/validate.py`'s own `scalar()` folded-scalar reader: `agent-orchestrator/SKILL.md` **1019/1024** (gap 5, 49 over the 970 working limit, 19 quoted trigger phrases), `agent-evals` **986** (gap 38, +16 over 970, 12 phrases), `agent-interop` **983** (gap 41, +13 over 970, 14 phrases), `agent-harness` **916** (gap 108, 54 *under* 970 — the only one with room). Body sizes for context (chars÷3.9 / `cl100k_base`): orchestrator 4570 / 4143, interop 3202 / 3000, evals 3104 / 2740, harness 2605 / 2320. Concretely, one more routed trigger costs the phrase plus 4 characters (`, "…"`), so: **agent-orchestrator** — adding `, "graph engineering"` (21 chars) needs 21 freed against 5 available; the cheapest honest cuts, measured, are the 25-char `, a chatbot with tool use` (already covered by the `"agent"` and `"tool calling"` triggers) or compressing the 185-char resale clause `for resale: tiered wallets, the single markup boundary, two-phase commit across a database and a provider API, spend-delta polling, budget and loop guardrails, per-tenant key lifecycle.` to `for resale: tiered wallets, one markup boundary, two-phase commit, spend-delta polling, budget and loop guardrails, per-tenant keys.` (~132 chars, frees ~53 and lands the field at ~966, back under 970). Do **not** cut the 61-char `Not for a single LLM call in a script, or for prompt wording.` — the negative boundary is what stops false triggers. **agent-evals** — the 424-char `Covers the three observability primitives … so any of it is possible.` clause; dropping its 110-char tail `simulated users with adversarial personas, annotation queues, and what to instrument so any of it is possible.` frees 110 and lands at 876. **agent-interop** — the 175-char deprecation parenthetical `(server/discover, stateless per-request _meta, elicitation in form and URL mode, subscriptions/listen; sampling, roots, logging and dynamic client registration on the way out)` is the one compressible block; halving it frees ~85 and lands at ~898.
- **Why it matters:** the host truncates past 1024 silently rather than erroring (`test/validate.py:186-190` exists for exactly that), so agent-orchestrator's next description edit has a five-character margin before a trigger disappears from routing with nothing saying so.
- **Fix:** apply the measured cuts above in `plugins/agent-stack/skills/{agent-orchestrator,agent-evals,agent-interop}/SKILL.md` to bring all three under 970, and lower the fail threshold in `test/validate.py:186` from 1024 to a 970 working limit with the hard cap kept as a second, louder failure.
- **Blast:** 2
- **Effort:** 1

## F-agent-stack-08 — the README tells users the installer copies "both skills" and never documents one of the seven references it otherwise enumerates
- **Dimension:** docs
- **Severity:** minor
- **Evidence:** `README.md:140` — "**npm installer** — copies both skills into `~/.claude/skills/`". Four skills ship; `bin/agent-stack.js:70-93` and `install.sh:16-23` both enumerate the directory rather than naming skills, and `.github/workflows/validate.yml` asserts it (`for s in $(ls plugins/agent-stack/skills); do test -f "$HOME/.claude/skills/$s/SKILL.md"`). Separately, `README.md:84-127` describes six orchestrator references by name — `graph-engineering.md`, `context-engineering.md`, `runtime.md`, `governance.md`, `patterns.md`, `llm-proxy-billing.md` — and never `references/pipeline.md`, which the 2026-08-16 split created and which `agent-orchestrator/SKILL.md:378` lists and `:192` links. (`README.md:14`'s "twenty references" is correct — 20 counted.)
- **Why it matters:** the install section is the first thing a reader acts on and it understates what arrives by half; the reference index a reader uses to decide what to load omits the file that owns the planned path, checkpoints and resume.
- **Fix:** in `/Users/sshlg/DATA/sshlg-skills/skills/agent-stack/README.md`, change "both skills" to "all four skills" at `:140` and add a `references/pipeline.md` paragraph to the block at `:84-127`.
- **Blast:** 2
- **Effort:** 1

## F-agent-stack-09 — the validator prints a check count that has not moved while two checks were added, and the ledger quotes that line as proof
- **Dimension:** gate
- **Severity:** minor
- **Evidence:** `test/validate.py:385` — `checks = 6 + len(skill_dirs)`, printed at `:386` as `OK: agent-stack structurally valid (10 checks, 4 skill(s), v0.11.1)`. The `6` is byte-identical at v0.9.0, v0.10.1, v0.11.0 and v0.11.1 (`git show <tag>:test/validate.py | grep '^checks = '`), while v0.11.0 added `check_one_home_per_fact()` (`:253-278`) and v0.11.1 added `check_shipped_front_matter_survives_a_real_reader()` (`:348-376`) — so two checks landed and the reported count did not move. The file today holds 9 top-level check groups plus the per-skill loop. `docs/evidence/verification.md:24` quotes that exact output line as REQ 005's evidence.
- **Why it matters:** the number reads as a measurement of coverage and is a hardcoded constant; a future run comparing "10 checks" across versions concludes nothing changed when two gates were in fact added, and the ledger row inherits the wrong figure.
- **Fix:** in `/Users/sshlg/DATA/sshlg-skills/skills/agent-stack/test/validate.py`, count the checks that actually ran (increment a counter in each check function, or register them in a list and report `len()`), or drop the count from the output line rather than printing a constant as a measurement.
- **Blast:** 1
- **Effort:** 1

TOTAL: 9 findings (0 blocker, 5 major, 4 minor)
