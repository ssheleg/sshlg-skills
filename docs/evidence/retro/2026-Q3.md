# Retro archive — sshlg-skills, 2026 Q3

**Append-only, and QUERIED rather than read.** Entries and retirements rotated out of
`docs/evidence/retro.md` land here so the in-force file stays short enough to be read in
full at every stage 0, and pruning costs no knowledge.

Created 2026-08-13 (v0.46.0) by the first retirement that carried text worth keeping.
Before that date the umbrella's retro had never rotated: nothing is missing from this
file, it simply had no earlier contents.

## Retired standing instruction #3 — the original text (retired 2026-08-13, v0.46.0)

Kept verbatim because the retirement was COLD, not solved: CI still does not run the
suite under a faked clock, so the failure this described is still possible.

> **(2026-08-06) A date literal in a fixture that a check compares against "now" is a
> test that schedules its own failure.** `brand_lint_test.py` in `super-ux` hardcoded
> `Last calibrated: 2026-08-05` against a check that reads `foundation.md`'s **mtime** —
> which for a fixture is always today. The suite was green on the day it was written and
> red the next, with no code change, and nothing in the suite could report that. Compute
> the boundary date at runtime; keep an explicitly stale literal only in the case that
> must fire, and plant against it. *(Retire when CI runs the suite under a faked future
> clock.)*

Its incident is the **2026-08-06 (second run)** entry in `docs/evidence/retro.md`: CI was
red on a `super-ux` PR for two brand-linter checks in code the PR never touched, and
`main` had been red since the previous day. The slot it occupied is left **vacant** in the
in-force list rather than reused — two CHANGELOGs and a merged PR body cite these
instructions by number, and renumbering would rot those citations (`B-23`).
