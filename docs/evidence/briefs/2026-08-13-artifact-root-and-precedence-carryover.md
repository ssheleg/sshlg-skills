# Carry-over ledger — artifact root and precedence (2026-08-13)

Append-only. Every stage writes here the moment something is deferred, dropped or
half-done; stage 10 reads it in full and gives every unresolved row a board id.
**Deferred out loud is forgotten.**

| id | What | Raised at | Why it was not done here | Home | Status |
|---|---|---|---|---|---|
| C-01 | **B-19 / the two-channel question is not resolved by this run.** The family wires hooks into `~/.claude/settings.json`; `task-pipeline` and `agent-sync` also ship hooks from their plugins. Axis A adds output to a hook that is already wired rather than a new channel | stage 0, operator decision D-01 | The board says decide it once, deliberately. Deciding it inside a run about a directory name would be the opposite of deliberate. `statusLine` cannot move to a plugin at all — no `plugin.json` among the 36 installed declares such a field — so the channel would stay double regardless | **B-19** | closed by decision — the row now carries the reason and the `statusLine` fact |
| C-02 | The count first stated for this run's scope was **1122 occurrences in 241 files**; the real figure is 340 total / 185 live. The inflated one counted `graphify-out/graph.json`, which indexes the whole tree | stage 0 harvest | Corrected before any work began, and the corrected number changed the operator's scope decision. Recorded because it is the fourth time this family has written a number it did not compute | retro entry + prune note (2026-08-13, fourth run) | closed |
| C-03 | Standing instruction **#4 fired inside this run's own harvest**: a shell loop reported `0 0 0` for all seven repositories because zsh does not word-split an unquoted `$all`. Uniformity across inputs that should differ was the tell | stage 0 harvest | Instrument replaced with Python on the spot; no code change owed. Recorded because the instruction earned its slot again | retro entry + prune note (2026-08-13, fourth run) | closed |
| C-05 | *(inherited, unchanged)* `npm run test:negatives` in `task-pipeline` reports one broken guard when run from a submodule checkout — the guard reads `git log -p`, the harness copies to `/tmp` without a resolving `.git`. CI clones normally and is green | carried from 2026-08-12 | Belongs to that repository's harness, not to this change. **This run must not read it as its own breakage** | **B-17** | closed — the row now records TWO guards, re-measured |
| C-06 | *(inherited, fires by construction)* An OAuth push to `.github/workflows/` over HTTPS is refused for want of the `workflow` scope. This run edits `validate.yml` in `task-pipeline` and `seo-aeo-audit` | carried from 2026-08-12 | Per-machine workaround: set the submodule's **push** url to SSH, leaving `.gitmodules` HTTPS so the invariant holds | **B-18** | closed — the row records that it fired again here, and the remedy worked |
| C-07 | **C-05 undercounts.** Run from this submodule checkout, `npm run test:negatives` leaves **two** guards not firing, not one — *the high-water mark lowered in a COMMITTED change* and *a version number already spoken for by a tag*. Both print `fatal: not a git repository`, both read git history, and neither touches the artifact root | stage 6, after the plants were repointed | Same root cause as C-05 and the same remedy (CI clones normally). Recorded as a count correction rather than an investigation: the error text already names the cause, and a four-minute pristine re-run would confirm what it says | **B-17** | closed — the correction IS the B-17 update: two guards, not one, re-measured and evidenced |
| C-08 | The negatives suite takes longer than a foreground command's timeout, so it runs in the background — and the wrapper reported **exit code 0 while the suite's own output said FAIL**. Read the output, never the wrapper's status | stage 6 | Not this repository's defect to fix; the discipline is the fix, and it is now a retro entry rather than a hope | standing instruction **#8** | closed — promoted from a note to a rule |
