# Carry-over — реестр роутеров и настройки пака

Append-only. Пишет любая стадия, читает целиком стадия 10. Отложенное вслух —
забытое, поэтому запись делается в момент, когда решение прозвучало, а не в конце.

| # | Запись | Стадия | Статус | Почему |
|---|---|---|---|---|
| C-01 | Граф кода для `sshlg-skills` | 0 | **переоткрыт → в работе (R-15)** | 2026-08-05 закрыт решением «22 файла, отвечать нечему». Оператор развернул на стадии 0 этого прогона. Override записан, а не молчалив |
| C-03 | Блок не записан в реальный `~/.claude/CLAUDE.md` | 0 | **в работе (R-16)** | был решением оператора; в этом прогоне закрывается |
| C-04 | Полная UX-цепочка для CLI лаунчера (`docs/ux/`) | 0 | отложено | вейвер D-09; взамен REQ-строки с точным выводом |
| C-05 | design-time трек в `seo-aeo-audit` | 0 | отложено | другой репозиторий; роутер `seo-llmo` покрывает правило, скил остаётся аудитом |
| C-06 | У зонтика нет `CLAUDE.md` домашних правил | 0 | отложено | у всех шести сабмодулей есть, у родителя нет; стадия 9 заведёт минимальный `DOCMAP.md`, полные правила — отдельно |
| C-07 | Четыре участника не делегируют `routers --member` | 0 | отложено | делегирует только `bin/super-ux.js:354`; бандл пишет все восемь роутеров без этого |

| C-08 | Резервная копия `~/.claude/CLAUDE.md` перед первой записью | 7 | сделана | `~/.claude/backups/CLAUDE.md.before-routers-v0.23.0`; понадобилась через десять минут |
| C-09 | Механизм резервной копии в самой команде | 7 | отложено | сегодня копию сделал агент руками. Команда, которая правит невосстановимый файл, должна снимать копию сама — не потому, что осталась ошибка, а потому что две уже были |
| C-10 | Согласие записано в обход интерактивного промпта | 7 | закрыто решением | shell не TTY, `askConsent` записал бы вечный отказ. Согласие оператора дано в чате явно и транскрибировано в `state.json`; промпту фальшивый ответ не подсовывался |

**Открыто на конец прогона: 5** (C-04, C-05, C-06, C-07, C-09). C-01 и C-03
закрыты как REQ этого прогона; C-02 закрыт прошлым; C-08 и C-10 закрыты по
факту.

**Три REQ появились после стадии 4** — R-17, R-18, R-19. Все три родились из
дефектов, которых план не предвидел, и все три найдены не тестом: два числами
в выводе, один прогоном против настоящего файла.
