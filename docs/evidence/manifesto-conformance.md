# Manifesto conformance — the program backlog

> **What this is.** The single home for the **state of one cross-repository program**: bringing the
> ssheleg family into conformance with the Proof of Done manifesto
> (`~/DATA/pod-manifesto/manifesto.md`), after the 2026-08-18 audit that scored all nine members
> against 61 extracted requirements.
>
> **What this is NOT.** It is not a second copy of any member's debt. A row here that belongs to a
> member is *also* filed in that member's own `docs/evidence/backlog.md`, and **that file stays the
> single home for the member's debt**; this file is the single home for *program* state — which task,
> which agent, which wave, what closed it. Cross-referenced by id. Two homes for one fact is the
> defect M-44 names; two homes for two different facts is a propagation matrix.
>
> **Write discipline.** Only the orchestrating session writes this file. Task agents return a typed
> verdict and never edit it. That is deliberate: the audit's G4 found that no member implements the
> checker the manifesto requires before a convergence (M-25), and the family's lease is per-task-id
> rather than per-file (agent-sync audit, M-24), so 30 agents appending to one ledger is the exact
> collision the audit flagged. The orchestrator is the checker.
>
> **Source of every row.** `scratchpad/REPORT.md` (consolidated) and `scratchpad/audit-<member>.md`
> (nine per-member reports, each with `path:line` evidence). Audit date 2026-08-18.
>
> **What an `M-nn` is, and what it is not.** The manifesto carries **no requirement ids** —
> `grep -c 'M-[0-9]' manifesto.md index.html llms.txt` returns 0 in all three. The `M-nn`
> set was *extracted* by the 2026-08-18 audit, and **that extraction was never committed**,
> so an `M-nn` on its own resolves nowhere: six of the 37 ids this program cites carry a
> `manifesto.md:<line>` beside them somewhere in the family, and the other 31 are pointers
> to a characterisation living in the citing row. A member repository quoting `M-40` and
> nothing else is quoting an id whose definition no reader can reach — found from the other
> side on 2026-08-20, when `seo-aeo-audit`'s two closed rows were measured resting on
> exactly that.
>
> **So the rule for every row from here on: cite `manifesto.md:<line>` beside the id**, and
> state the requirement in the row's own words. `test/validate.py` refuses a new row that
> does not. Reconstructing the full 61-id table faithfully is a row of its own (`UM-12`):
> an independent re-derivation on 2026-08-20 produced 61 rows whose numbering **does not
> match** this programme's — the same requirement reads `M-16` there and `M-17` here — so
> publishing that derivation would silently renumber every citation in the family, which is
> worse than an unresolvable id.

<!-- m-cite-grandfathered: AG-01 AG-02 AG-03 AG-04 AS-01 MS-01 PM-01 PM-02 PM-04 PM-05 SD-01 SD-02 SD-03 SD-04 SD-05 SD-06 SE-01 SE-02 SE-03 SE-04 SE-05 SG-02 SG-03 SG-04 SU-01 SU-02 SU-03 TP-01 TP-02 TP-03 TP-06 UM-01 UM-02 UM-03 UM-04 UM-05 UM-06 UM-08 UM-09 UM-10 UM-11 -->
<!-- every row NOT listed above must cite `manifesto.md:<line>` beside its M id;
     the exemption is enumerated rather than implied, because an implicit one is
     indistinguishable from a guard nobody wired -->

## How to read a row

| column | meaning |
|---|---|
| `id` | program id. Prefix = owning repository. |
| `M` | the manifesto requirement it closes (M-01…M-61 from the extracted set) |
| `evidence` | where the defect was measured, `path:line`, relative to the owning repo |
| `done when` | the acceptance criterion — a command, a refused plant, or a resolved reference |
| `wave` | execution order. Rows in one repo never run concurrently; different repos do. |
| `state` | `open` · `claimed` · `landed` · `verified` · `deferred` · `dropped` |

`landed` means the change is committed. `verified` means an independent check observed it —
these are different states and the program does not collapse them (M-10).

---

## Wave 1 — the two claims that are false right now

Ordered first because they are the only rows where the family is currently **asserting something
untrue** to an outside reader.

| id | M | verdict | title | evidence | done when | state |
|---|---|---|---|---|---|---|
| SD-01 ✅ | M-44 M-07 | — | **VERIFIED 2026-08-19** at `sheleg-dev@e401603` — 8 dead references where the audit sampled 6, 11 closed repo-wide; see the closes ledger. `sheleg-dev/SECURITY.md` is a wholesale copy from `seo-aeo-audit` — five paths that do not exist, shipped in the published npm tarball, including a "verify for yourself" command that cannot run | `SECURITY.md:10,11,17,35,54,56`; `package.json:17` | every path in `SECURITY.md` resolves (`test -e`), the verify command runs, and the B-47 path guard at `test/validate.py:255-307` is widened beyond CONTRIBUTING with its own planted defect | **verified** |
| UM-03 ✅ | M-07 | — | **VERIFIED 2026-08-19, landed at `1ac184b` once the eight members were pushed and the umbrella gate went green (35 checks).** 10 files, +664/−6: a new `test/doc_refs.py` plus a 17-case suite, a corpus guard owning the 5 live documents, 83 addresses gated at 0 dead, 4 CI plants. Its author refused to bypass the hook and refused to narrow the convergence guard as a side effect of a documentation row, filing B-92 instead. Lands at convergence. Sweep was far wider than the row: 556 candidate tokens, 258 real addresses, **36 genuinely dead** — 2 in the enforced corpus and 34 in dated records that cite member repositories and states true at a commit, which are counted and disclosed per run rather than gated, by decision. two of the umbrella's own references do not resolve — a manifesto about resolvable addresses shipping unresolvable ones | `README.md:66` (`npm run test:negatives` → exit 1, "Missing script"); `docs/evidence/convergence.md:10` (`scripts/check-convergence.sh` absent) | both citations name a target that exists; `npm run` shows the script or the claim points at the 20 CI plants / the member's real `templates/convergence.sh` | **verified** |

## Wave 2 — close the last manifesto gap, then make the manifesto true

`TP-01` runs **before** `PM-01`: the manifesto's paragraph should describe the state after this
program, not during it. If B-080 closes, `:445` becomes a stronger paragraph — all four requirements
built, with commits — instead of a corrected one.

| id | M | verdict | title | evidence | done when | state |
|---|---|---|---|---|---|---|
| TP-01 ✅ | M-22 | **VERIFIED 2026-08-19** at `task-pipeline@d17ae27` — B-080 closed; all four of the manifesto's named gaps are now built, each mechanism re-run by the orchestrator. See the closes ledger. | **B-080, the one manifesto gap still open** — a node cannot say how it will be closed, while `agents/verifier.md:61` orders an agent to "run the checks the task named". Node props measured `blocked_by,evidence,id,owner,parked_reason,serves,status,title,touches` — no `check` | `docs/evidence/backlog.md:15`; `graph.schema.json`; `agents/verifier.md:61` | `check` exists on the node schema, is required for a node the verifier closes, `graph.py validate` exits 1 without it, and a planted defect is refused in `negatives.py` | **verified** |
| PM-01 ✅ | M-08 M-43 | — | **DONE IN ALL FOUR SURFACES, HELD BY ITS OWN LINK GATE.** Rewritten to say all four gaps are built, each with its closing commit — `9d8695d` judgment gate, `fbd8a67` resolvable `serves`, `0bc5eb6` expiring verification, `8b7de18` per-node check — and to name the lesson: *a citation that resolves is not the same as a citation that is current.* Mirrored into `index.html` prose, footnote N5, and `llms.txt`. **6 of 7 gates green** (`check-parity` 316 sentences / 0 missing, `check-html`, `check-pack`, `stamp-assets`, `build-figures`, `inline-figures`); `check-links` exits 1 on exactly one reference — `8b7de18`, HTTP 422, because task-pipeline is unpushed. 28 references, 1 unresolved. Commits when push makes that citation real. `manifesto` → *"One reference implementation is packaged"* asserts in the present tense that four rows are open. Three closed 2026-08-17 (B-076 schema `auto·judgment·manual` + required `judge`; B-077 via B-085 `violations()` refuses unresolvable `serves`; B-081 `Observed at` + four invalidators). The permalinks still resolve and still read `open` — the receipt confirms a belief a day out of date. **Widened 2026-08-19: the claim lives in five surfaces, not one** — `manifesto` → *"One reference implementation is packaged"*, `index.html:653` (prose), `index.html:721-722` (footnotes N4 and N5), `llms.txt:51`, and umbrella `README.md:71` (that last one is UM-06) | `manifesto` → *"One reference implementation is packaged"*; `index.html:653,721,722`; `llms.txt:51`; task-pipeline `docs/evidence/backlog.md:15,16,41,42` at HEAD `62d7afc` | all four pod-manifesto surfaces state what is true at a named commit and date, link each close, and name whatever remains; `tools/check-parity.py` exits 0 — verified by re-reading the four rows, not by trusting this row | **verified** |
| PM-04 ✅ | M-43 | — | **BUILT AND WIRED, HELD BY THE SAME PUSH.** `tools/check-currency.py` — a claim now has two ends and both are checked: **pinned** (what the cited range said at the commit the document points at, so a mis-transcription is caught) and **current** (what that row says on the default branch today, so the document may not contradict it). Registered claims live in a `CLAIMS` table, because a citation nobody registered is a citation nobody re-reads. Exit 2 for *could not look*, deliberately not 0. Added as its own CI job with the self-test running first and offline. README updated in the same change. **Its live run is the proof it works:** it passes B-076, B-077 and B-081 on both ends and fails B-080 with *"asserts 'closed' today, but the default branch reads 'open'. The permalink still resolves, which is exactly why this is not caught by a link check"* — the exact defect class it was built for, caught on its first real run. **My own guard had a bug its own plant caught:** `row_state` took the *last* state word on the line, but a closed row narrates its history after its state (`closed 2026-08-17 — the row was open until…`), so the last word is `open`; it passed every real row by luck. Now it locates the State column from the table header and takes the first state word in that cell. Self-test 5/5. **New row, found 2026-08-19 while baselining the mirror.** `tools/check-links.py` proves every citation **resolves** — 24 references checked, 0 unresolved, run with a token so the check measures the reference and not the rate limit. It cannot detect that a resolved citation has gone **stale**: the four backlog permalinks resolve perfectly and now describe a state three of whose four rows no longer hold. Resolution and currency are different properties, and the repository checks only the first — which is how the defect in PM-01 survived a green CI | `tools/check-links.py`; the four permalinks at `manifesto` → *"One reference implementation is packaged"* | a currency check exists: for every citation whose claim this document characterises, the cited row's present state is fetched and compared against what the prose asserts, and a divergence fails. Planted defect required — the check must have been watched failing | **verified** |
| UM-06 | M-08 M-43 | — | **VERIFIED 2026-08-20** — the README names the four requirements as built with their four commits, and the link whose label read `docs/evidence/backlog.md` while pointing at the member's file now names both. the same stale claim mirrored in the umbrella README | `README.md:71` | agrees with `manifesto` → *"One reference implementation is packaged"* after PM-01, and cites the same commits | **verified** |
| PM-02 ✅ | M-40 | **DONE IN ALL THREE SURFACES, HELD BY THE SAME PUSH.** Two things, not one. **(a)** TP-02's fix to `residue.md` shifted the manifesto's own anchors, so E1 was re-pointed from `17ef1a6#L28-L48` to `f0402c2#L28-L62` and E4 from `#L173-L206` to `#L187-L220`, in `manifesto.md`, `index.html` and `llms.txt` — zero stale residue anchors remain. **(b)** The pass-7 ambush is now named rather than hidden: the prose states the trend is measured over passes four to six, that a seventh fell back to 4 of 19, and that the record does not say what it did differently. **I took TP-02's argument over my own brief** — it proposed naming the limit instead of ending the series at six, because *deleting a measured row to protect a claim is the opposite of what this document asks for*, and it is right. `check-parity` exits 0. Held by `check-links`: **3 unresolved, one cause** — `8b7de18` (422) and both `f0402c2` blob URLs (404), all unpushed. | E2's anchor `audit.md#L44-L64` includes the pass-7 row `\| 7 \| 19 \| 4 \|`, which reverses the decay the paragraph teaches — a reader following the receipt lands on the anomaly. task-pipeline's B-082 is open about exactly this | `manifesto` → *"The seam walk can find absence"*; task-pipeline `references/audit.md:44-64` | the citation lands only on what it claims, or the prose accounts for pass 7; B-082 closed in the same change |  **verified** |
| PM-05 ✅ | M-17 M-19 | — | **New row, opened and closed 2026-08-19 from AG-03's finding about the manifesto itself.** AG-03 reconciled the two eval clocks in `agent-stack` and then said the manifesto has the same gap from the other side: *"it never names the distinction either, so a reader of the manifesto alone can still read M-19 as demanding invented fixtures up front."* That is correct and it is the document's own defect, not the implementation's — `:188` already accepts "a known failing fixture, or recorded historical failure" as proof a checker discriminates, which **is** the corpus tier arriving from production, so the distinction was latent and unnamed. §3's evidence-graph section now names both clocks and refuses to let either displace the other. Mirrored to `index.html`; `check-parity` exits 0 at **323 canonical sentences, 0 missing** (was 316) | AG-03's report; `manifesto` → *"The evidence graph says how the result will be known"*, `:188` | the manifesto names the observable and the corpus as separate objects on opposite clocks — **done**; held only by the same push as PM-01, PM-02 and PM-04 | **verified** |
| TP-02 ✅ | M-40 | **VERIFIED 2026-08-19** at `task-pipeline@c8de225` — and its edit moved the manifesto's E1 and E4 anchors, which PM-02 then re-pointed. See the closes ledger. | `residue.md` says the inventory was queried "one minute later" beside a 3:12-old process, so the two lines cannot describe one instant — the source of the manifesto's opening story | `references/residue.md:28-48` | the record states both observation times so the pair is readable as one event | **verified** |

## Wave 3 — the highest-leverage mechanism in the family

| id | M | verdict | title | evidence | done when | state |
|---|---|---|---|---|---|---|
| SU-01 ✅ | M-17 | — | **VERIFIED 2026-08-19** at `super-ux@fe2189e` — U060–U065 shipped; the pack's own 15 scenarios went from 0 to 15 resolving `Coverage` citations. See the closes ledger.  **Nothing refuses a requirement with no observable.** `ux_lint.py` never opens a scenario or story body — no rule reads `Expected result`, `Acceptance criteria` or `Success metric`. The dogfood: 15 own scenarios `Status: implemented`, **0 carry `Coverage:`**, no test touches `bin/super-ux.js`, `npm test` exits 0. The skill that demands the chain before code cannot tell that its own chain closes on nothing | `plugins/super-ux/scripts/ux_lint.py:269-350,92-102`; `docs/ux/scenarios.md:13-27` | a `U06x` rule family refuses a scenario or story with no observable; the pack's own 15 scenarios either carry one or are marked honestly; a planted defect is refused | **verified** |
| SU-02 ✅ | M-21 | **VERIFIED 2026-08-19** at `super-ux@4602712` — 15 of 15 scenarios now read `unobserved`; before this the chain had no such state and read as 15 validated bets. See the closes ledger. | a shipped scenario silently counts as validated; `unobserved` appears nowhere; audit PASS flips `validated → implemented` | `plugins/super-ux/skills/ux-audit/SKILL.md:211-212` | a `Product:` state `unobserved \| observed \| contradicted` exists, with no floor and no target — a hypothesis may stay unobserved forever without failing a gate | **verified** |
| PM-06 | M-46 (`manifesto` → *"Every fact should have one authoritative home"*) | **VERIFIED 2026-08-24** — the reverse pass is code that runs, not a docstring. `python3 tools/check-parity.py` prints *page sentences checked : 324 · with no canonical source : 0*, and the run gained a third assertion the row did not ask for: *cases claimed / listed : 9 / 9 ok*, which gates the count PM-07 is about. Symmetric by construction — the reverse pass drops exactly what the forward pass drops, and compares a sentence against the whole canonical text rather than against a set, because a reference label at a paragraph's end moves the boundary on one side only. | **the parity check runs in one direction and its docstring claimed both.** `tools/check-parity.py` asserts that every block of `manifesto.md` appears in `index.html` and asserts nothing in reverse, so the published page can carry prose that exists in no canonical source with the gate green. The docstring said it *"fails when the two drift"*; narrowed to what runs at `f0f62a4`, which leaves the mechanism owed | `pod-manifesto` `tools/check-parity.py:4-6` vs `:79-83` | a reverse pass over the page's prose blocks, each required to be present in `manifesto.md` or listed in an **enumerated** allowlist of page chrome (masthead, footnotes, proofline), with a planted extra paragraph refused in CI. An allowlist invented in passing is an exemption nobody re-reads | **verified** |
| PM-07 | M-44 (`manifesto` → *"Evidence remains attached to the state it observed"*) | — | **eight of the nine characterised citations are unregistered.** `tools/check-currency.py` holds one claim; the page characterises nine cases — E1's *"more than 3 minutes"*, E2's *"10 of 13"* and *"4 of 19"*, E4's *"18 containers / four projects / 3 days"*, E3's *"one command"*. All eight were re-verified correct on 2026-08-20, so this is a coverage gap rather than a live falsehood — and it is the same shape as the defect the tool was built for: a resolving citation nobody re-reads | `pod-manifesto` `tools/check-currency.py:68-81` vs `index.html:119` | each characterised quantity registered and asserted against the pinned text; `check-currency.py --verbose` prints more than `4 cited row(s) across 1 claim(s)` | open |
| UM-12 | M-05 (`manifesto` → *"point to its supporting record at an address another actor can resolve"*) | **VERIFIED 2026-08-24 — closed by making the ids stop being load-bearing, not by inventing them.** The manifesto still carries no `M-nn` (`grep -oE '\bM-[0-9]{2}\b' manifesto.md index.html llms.txt` → 0), and putting them there is the manifesto owner's decision, not this family's. So the citation moved instead: every reference into the manifesto is now a **distinctive phrase**, and `check_manifesto_citations_resolve` looks each one up in the subject the register's own header names — failing on a phrase that is absent AND on one that matches twice, because a citation matching twice names neither. 14 citations, 9 distinct anchors, 0 dead. The `M-nn` remain as internal labels and nothing rests on them alone: `check_a_new_conformance_row_cites_the_manifesto_line` used to MANDATE `manifesto.md:<line>` — the rotting form — and now requires the phrase. **What that guard's own widening then found:** at `[A-Z]{2}` it had never examined `ALL-26`, `ALL-44` or `ALL-49`, three rows carrying no anchor at all. All three now carry one. Watched failing against a corrupted phrase. | **the programme's own requirement ids resolve nowhere.** The manifesto carries no ids (`grep -c 'M-[0-9]'` → 0 in `manifesto.md`, `index.html`, `llms.txt`); the `M-nn` set was extracted by the 2026-08-18 audit and that extraction was never committed. Six of the 37 ids this programme cites carry a `manifesto.md:<line>` somewhere in the family; 31 do not, and an independent re-derivation on 2026-08-20 produced a numbering that DISAGREES with this one — the same requirement reads `M-16` there and `M-17` here. Found from the other side: `seo-aeo-audit`'s two closed rows rest on `M-40`, `M-32` and `M-08`, and `grep -rn` over the whole manifesto repository finds none of them | `docs/evidence/manifesto-conformance.md` (the header note); `seo-aeo-audit/docs/evidence/backlog.md:40,45` | either the extracted set is published as an artifact with THIS programme's numbering,each  id resolving to a `manifesto.md:<line>`, or the ids are retired in favour of the line citation the new guard already requires. Publishing the other derivation would silently renumber every citation in the family, which is worse than an unresolvable id | **verified** |
| SU-03 | M-44 | **VERIFIED 2026-08-24 — and both halves were already closed on the day this row was written.** Half one: `B033` refuses a duplicate `Fact` key, watched here against a second `skills shipped` row — *\"`skills shipped` has 2 rows … two rows under one name make every figure quoting it ambiguous\"*. Half two: the `Value` column **is** recomputed. `test/validate.py` runs every row's `Source` command and compares, handling the two special rows rather than skipping them — the validator row's Source is the validator itself, run as a child with `FACTS_NESTED=1`, and the agents row names an external registry and is disclosed. Watched failing: `practices in the catalog` set to 999 → *records '999' and its own Source command returns '241'*. All seven recomputable rows re-run by hand today: **7 of 7 exact**. **The stale figures this row cites were mine to correct, not the register's.** The audit report of 2026-08-24 claimed `facts.md` states 3107 against a measured 4111 and called the drift 167× worse — `3107` sits in a **historical paragraph** recording a past staleness (*\"On 2026-08-14 this table was itself the stale one\"*), and the table's own value is 4111 and correct. A narrative read as a live claim is the same defect class this programme exists to find, arriving from the auditor's side. | `facts.md` has no duplicate-key check and its `Value` column is carried, not computed — states 3500 validator checks, measured 3506 | `plugins/super-ux/scripts/brand_lint.py:820-887`; `docs/brand/facts.md:36` | a B033 duplicate-key rule exists and each numeric fact row runs its own command | **verified** |

## Wave 4 — adoption of mechanisms the family already owns

Every row here is copying something that is already written and tested one directory away. These are
propagation failures, not design failures (audit G2, G6).

| id | M | verdict | title | evidence | done when | state |
|---|---|---|---|---|---|---|
| UM-02 | M-31 | — | **VERIFIED 2026-08-20** — `pipeline.json` stages `brainstorm` and `spec` are typed `judgment` with a named `judge`; `grep -c judgment pipeline.json` was 0 before, and the pinned schema has required a `judge` since `9d8695d`, so no new code. the `judgment` gate type ships in the pinned schema and is unused — `pipeline.json` stage 2 types "The design is approved" as an `auto` gate, the exact defect task-pipeline's B-076 closed, still live one layer up | `pipeline.json:38-40`; task-pipeline `pipeline.schema.json:208,230` | stages 2/3/9 are typed `judgment` with a named `judge`; `validate.py:159` already runs the schema, so no new code | **verified** |
| UM-05 | M-43 | — | **VERIFIED 2026-08-20** — the newest ledger section carries `Observed at` and `Invalidated by`, and `check_a_new_ledger_section_can_expire` refuses a section dated 2026-08-20 or later without them, with a CI plant. The 419 rows above are deliberately NOT back-filled: a retrospective observation for a row nobody re-checked answers the question wrongly instead of not at all. proof never expires here: 129 ledger rows, 123 marked `verified`, `grep -c 'Observed at\|invalidat'` → 0. task-pipeline shipped invalidation on 2026-08-17 and the umbrella never adopted it | `docs/evidence/verification.md`; task-pipeline `templates/verification.md` | the ledger carries `Observed at` and the four invalidators, and a stale row is reported rather than read as current | **verified** |
| UM-04 | M-44 | — | **VERIFIED 2026-08-20** — both figures are computed by `test/run.js`, which sums every suite's own count and compares it with `<!-- ratchets: … -->`; it caught its own drift twice in one session and printed which way the stated figure was out. The row's own correction (34/585) was 22 fixtures stale by the time it was read. both ratchets below truth — `DOCMAP.md:140` says 32 suites / 562 fixtures against counted 34 / 585; `verification.md:11` says 119/113 against its own command's 129/123 | `docs/DOCMAP.md:140`; `docs/evidence/verification.md:11` | the figures are recomputed by `validate.py` and a stale stated figure fails, as it already does for board priority at `:1395` | **verified** |
| UM-01 | M-49 | — | **VERIFIED 2026-08-20** — `runOpen` now reads the ledger's CONTENT: `runledger.isOpen()` takes the last `stage:` line and asks whether its verdict is `pass` at the final stage, so the `event:` rows appended after a close no longer keep a run open. 25 fixtures; the first default (highest stage id seen) read every mid-run ledger as closed and was caught by its own fixture rather than by review. **a closed run's ledger permanently silences the route gate.** `.task-pipeline/run.md:117` records `stage: 10 acceptance — verdict pass — 2026-08-14`; the file is still present and still being appended to (65 ledger lines, `event:` rows written during the 2026-08-18 audit), while `hooks/pre-tool-use.js:61` derives `runOpen` from `fs.existsSync` and `lib/routegate.js:53` returns null when open | `.task-pipeline/run.md`; `hooks/pre-tool-use.js:61`; `lib/routegate.js:53` | `runOpen` is derived from ledger **content**, or stage 10 archives `run.md`; the route gate demonstrably fires again after a closed run | **verified** |
| UM-11 | M-05 M-29 | — | **VERIFIED 2026-08-20** — closed by the same change as UM-09: ownership no longer depends on any index, so another agent's staged tree cannot make the umbrella the owner of a submodule's commit. The fixture holds this project's index DIRTY while a commit in another repository goes ungated. **A regression of a defect this repository already closed — and the retrospective says so.** `docs/evidence/retro.md` item 4 reads: *"The repository gate judged commits belonging to other repositories, and deadlocked a release live: the umbrella red because the submodule had not shipped, the submodule unable to commit the fix because the umbrella was red."* The fix was to decide ownership by "staged changes in **this** repository" — and that fix holds for one agent and **fails under concurrency**, because another agent's staged index makes the umbrella look like the owner of a submodule commit. Two independent agents hit it and diagnosed it identically without seeing each other's reports. **The same heuristic deadlocks the other way, and it stopped this program twice.** Because ownership is decided by "does the umbrella have anything staged", a submodule commit is judged by the **umbrella's** suite whenever the umbrella index happens to be dirty — a different repository's verdict about a change it does not contain. UM-03's staged work made that state permanent, so `SE-01` finished green in `seo-aeo-audit` (which held **zero** unpushed commits) and could not commit at all: it waited ~8 minutes, returned `COMMIT: NONE`, and correctly refused to bypass the hook or obfuscate the command. The hook's own docstring predicts this deadlock and cites a release it already deadlocked once. Cleared by recording UM-03's ten staged paths and unstaging them, working tree untouched; the commit then landed from the producer's saved message. **Note the pair:** UM-09 lets a real umbrella commit through, UM-11 blocks a commit that was never the umbrella's — one heuristic, both errors, and the honest fix is the same as UM-09's | `hooks/repo-gate.js:66-88` and its docstring; SE-01's report; `scratchpad/UM-03-staged-paths.txt` | ownership no longer depends on the umbrella's index. Until then, the operational rule is that the umbrella index stays clean while member agents run — which is what this program now does | **verified** |
| UM-09 | M-26 M-29 | — | **VERIFIED 2026-08-20** — ownership is derived from the command's own text (`-C`, a preceding `cd`) and the payload's `cwd`, resolved to a repository root; the index is asked only when neither can be read, and then a self-staging command is no longer excused. Reproduced before (no output, exit 0, no suite) and refused after, across three compound spellings, by `test/hooks_e2e_test.js` running the real script as a process. **THE HEADLINE FINDING. The repository gate can be bypassed by the most ordinary git idiom, and has never been watched failing for that input class.** `hooks/repo-gate.js:76-80` decides *whose commit this is* by asking `git diff --cached --quiet`, and it asks at **PreToolUse** — before the command runs. So in the compound form (`add` and then commit in one shell line) nothing is staged at decision time, the hook concludes "the commit is not ours" and **exits 0 without ever running the suite**. Proven two-sidedly against a throwaway project holding no `package.json`, so that a real suite run must fail: identical command with nothing staged → **exit 0, allowed, suite never ran**; with something staged → **deny**, carrying npm's ENOENT. Not theoretical — this is why `e473189` (13:57:19) landed while five members held local-only commits timestamped 13:51:58–13:57:09, and why the next attempt was blocked only because *another agent's* files happened to be staged. The docstring reasons carefully about which repository's verdict should apply; the defect is the **timing** of the question, not its intent | `hooks/repo-gate.js:76-80`; `lib/repogate.js:36`; the timeline across the umbrella's log and each member's log | ownership is decided from something the command cannot change under the hook — parse the pathspec, resolve from the shell cwd or a `-C` argument, or re-check after staging with a `PostToolUse` refusal. A plant for the compound form is required: the guard watched allowing it before, refusing it after | **verified** |
| UM-10 | M-26 | — | **VERIFIED 2026-08-20** — `isCommit` reads `executablePart()` from `lib/hygiene.js`, the treatment that already existed one module over. A comment and a heredoc fed to `cat` are no longer commits; `bash -c "git commit"` now is, which was the matching bypass this fix turned up. **Found while proving UM-09, and it cost two diagnostics.** `isCommit` matched the verb **inside a JSON payload** — an argument to `node`, never an invocation — and denied the command; then it denied a second command because the *ledger prose describing the bug* contained the same words. This is the false-positive class this repository already closed for the hygiene guard, whose `executablePart()` drops heredoc bodies fed to a non-shell and whole-line comments. `repo-gate` does not share that treatment, so investigating the gate required obfuscating the verb, and recording the finding required abandoning the shell entirely | `lib/repogate.js:36`; contrast `lib/hygiene.js` `executablePart()`, described under the invariants in `CLAUDE.md` | `isCommit` reads what would RUN, reusing `executablePart()` rather than a second implementation, with a plant for the payload-as-argument shape | **verified** |
| UM-08 | M-42 M-48 | — | **VERIFIED 2026-08-20** — the guard separates the two cases: no upstream on a branch stays a disclosure; detached HEAD on a commit no ref holds is a FAILURE. Watched refusing a planted detached commit; the first instrument (`git branch --contains`) prints `* (HEAD detached from …)` and read that as a branch holding it — the plant caught it. **New row, opened 2026-08-19 after nearly losing a close.** `sheleg-design` sat in **detached HEAD** at tag `v1.43.0`, so SG-02's commit `7e630cb` landed on no branch: remote `main` was still at `001a0bd`, and the commit was reachable only through the reflog — unreferenced and collectable. The convergence guard `check_no_member_holds_a_commit_the_remote_does_not` behaved **correctly and honestly** — with `@{u}` unresolvable it appended to `_skips` rather than passing, on its own stated principle that *a check that cannot look must never read as one that looked*. But a disclosure among many is easy to scroll past, and the guard cannot tell the benign case (a fresh clone or CI checkout with no tracking branch) from the dangerous one (commits on no branch, which git may collect). Rescued by `git branch -f main 7e630cb && git checkout main` — `main` was an ancestor, so a clean fast-forward — after which the same guard reported it as the fifth unpushed member, i.e. the state became detectable rather than merely undisclosed | `test/validate.py:1288-1332`, especially the `_skips` branch at `:1323`; measured before the fix: 0 failures for `sheleg-design` and one disclosure; after: 1 failure | the guard separates the two cases: no upstream **and** `rev-parse --abbrev-ref HEAD` == `HEAD` **and** commits not reachable from any branch is a **failure**, because the work can be collected; no upstream on a branch stays a disclosure. Planted defect required — a throwaway submodule in a detached state with an unmerged commit | **verified** |
| AS-01 | M-49 | **VERIFIED 2026-08-24** — the mechanism ships and was exercised: `reap --i-own-this <KEY>` cleared two locks this session and printed *"cleared by operator decision … the classifier called it `ambiguous`, and that has not changed: this was a person's call, not a proof of ownership"* — the report does not promote a decision to a proof. Re-measured across the family today: **17 lease files → 2**, both expired, both correctly left alone as foreign or ambiguous. What the row asked for — residue reported rather than `none` — `status` now prints on every session start. **The remaining half is engineering, not this row:** TTL 2700s is shorter than this family's release cycle, so a lease taken before `test:all` expires during it; measured on the v1.76.0 release, which left two unreapable locks of its own. Filed separately. | **17 lease files exist across the family and all 17 are expired**, oldest 2026-08-16T00:23:27Z. `agent_sync.py status` prints `leases held: none` / `other runs: none holding anything` beside three expired locks in the directory it just read. The reaping logic exists at `agent_sync.py:1344-1350` and nothing calls it for reporting | measured 2026-08-19 across `*/.agent-sync/leases/*.lock`; `agent_sync.py:1344-1350,2542,3168-3174` | `status` and `finish` enumerate expired locks as residue; own spent state may be reaped, foreign or ambiguous state is reported and left alone (M-50); the 17 present locks are resolved under that rule | **verified** |
| MS-01 ✅ | M-49 | — | **VERIFIED 2026-08-19** at `make-skill@73ebeee` — and the leak was twice the size the audit named, in a file the audit never named. See the closes ledger and ALL-49.  49 leftover repo copytrees and 55 `planted` directories under `/var/folders/…/T/` from the parity test | `test/checker_parity_test.py:60,160-166` | the test uses `TemporaryDirectory()` and prints what it left behind | **verified** |
| ALL-49 | M-49 M-50 (`manifesto` → *"It can leave background processes, monitors, scheduled wake-ups, worktrees, containers, leases"* and *"Teardown is verified by re-reading state"*) | **VERIFIED 2026-08-24** at `task-pipeline@3c507c6` — **the last unaccounted temp tree in the family, and the leak was never the defect.** Measured under a clean `$TMPDIR`: `graph_test.py` created **160 trees and removed 0**, 2.1 MB a run, from nine `mkdtemp()` sites inside parametrised cases. The defect is that a completed run said nothing about what it left, so the next leak would be invisible in exactly the same way. `test/residue.py` **copied verbatim** from the umbrella — two implementations of one ledger is the defect this family keeps finding in its own history — with only `PREFIX` changed, because the prefix is what makes future residue attributable. Both halves watched: a clean run prints *left nothing — 160 created, 160 removed* with `$TMPDIR` empty; a planted failing case prints *1 of 160 KEPT — the case did not pass and the copy is the evidence*, naming the owner and the `rm -rf`. **A sharp edge found by getting the plant wrong first:** `report()` is idempotent, so a case below the call is created after the accounting and never appears in it — recorded where it bites. Family-wide now: 13 files, 29 `mkdtemp` sites, **0 unaccounted**. | **New row, opened 2026-08-19 from MS-01's referral and a sweep that widened it.** MS-01 fixed `make-skill`'s two leaking suites and reported the identical `tempfile.mkdtemp()` fixture leaking in sibling repos. A family-wide sweep found **six leak sites across five repositories**, three of which nobody had named: `test/pin_source_test.py` (2 sites, umbrella), `test/plant_guard_test.py` (umbrella), `agent-stack/test/plant_guard_test.py`, `agent-sync/test/claim_cell_test.py`, `seo-aeo-audit/test/plant_guard_test.py`, `task-pipeline/test/graph_test.py` (8 sites) — each `mkdtemp()` with zero `rmtree`/`TemporaryDirectory`/residue accounting. Measured at `$TMPDIR` 2026-08-19: **1840 plant-guard fixture trees, 60 `planted/SKILL.md` dirs, 5.4 GB.** The pile is live and growing from this program's own agent runs — the audit counted 49+55, MS-01 counted 36+1808, this sweep 60+1840: one pile at three times | the six sites, by `grep -c 'mkdtemp()'` against `grep -cE 'rmtree\|TemporaryDirectory\|residue'` | every site accounts for its temp state and each gate prints what the run left. `make-skill@73ebeee` already ships the mechanism (`test/residue.py`); this row is propagation, not design. The 5.4 GB is reported, and reaped only where ownership is provable (M-50) | **verified** |
| SE-01 ✅ | M-40 | — | **VERIFIED 2026-08-19** at `seo-aeo-audit@265dfa5` — a closed enum of five, seeded so the honest state is the default; see the closes ledger. **the client report cannot separate `pass` from `never looked`** — the instruments can and the deliverable cannot. `url_inspection.py:236-250` grants CONFIRMED only to the N of M URLs the index answered; the report offers a free-text "Not checked" table and a `Status` column with no vocabulary, which no check reads | `templates/audit-report.template.md:47-65` vs `scripts/url_inspection.py:236-250` | the coverage table has a closed vocabulary (`observed / partial / unlooked / blocked-by <gate>`), seeded by `preflight.py --format json`; a track that returned nothing cannot render as one that came back clean | **verified** |
| SE-02 ✅ | M-32 | **VERIFIED 2026-08-19** at `seo-aeo-audit@7953484` — **and it overrode this row's brief with a better argument.** I wrote "omit an unresolvable field and say why"; it refused, because *a field that vanishes when unavailable is indistinguishable from one nobody checked — the exact defect SE-01 removed from the coverage `Status` column.* So `actor`, `model` and `trace` print on every run as `unavailable: … is not set by this harness`, and none is fabricated: *naming the wrong model id sends an investigation to a model that never ran.* My instruction would have reintroduced the defect its sibling had just removed. See the closes ledger. | no output carries provenance — no script emits `observed_at`, a tool version or a run id | `scripts/*.py` (grep for `__version__\|observed_at\|timestamp` → none) | every JSON payload carries `producer{skill_version, script, observed_at, args}` and both report skeletons carry the block | **verified** |
| SE-03 | M-43 M-44 | **VERIFIED 2026-08-24** — the document publishes the command and I ran it: `grep -rnE "urlopen\|build_opener\|…\|\bopen\("` over the seven scripts prints **33** and `SECURITY.md` states **33**, with the breakdown 13 + 7 + 7 + 6 summing to it and a separate sentence accounting for why a per-pattern grep gives 34. `agent_surface.py`, whose absence from the I/O table this row named, is in it. A document checkable by its own published command is the shape this program asks for everywhere else. | a proof marked `observed` expired unnoticed: `SECURITY.md` asserts "22 lines… six `open()`", measured today 26 / 7 / 7, and `agent_surface.py` is missing from the I/O table | `SECURITY.md:5,10,20,85,88`; `docs/evidence/verification.md:51` | the grep counts are a validator guard rather than prose, and the ledger row carries `invalidated_by` | **verified** |
| SG-03 ✅ | M-44 | **VERIFIED 2026-08-19** at `sheleg-design@4c0d63e` — the regex hole is closed with 0 false positives in the policed set. See the closes ledger. | two stale facts inside the document that owns single homes — `DOCMAP.md:91` says "a fourteen-kit build matrix" against 29, and `:97-99` says coordination is "ungated" while `.claude/agent-sync.json` guards 9 files | `docs/DOCMAP.md:91,97-99`; the counted-claims regex at `test/validate.py:836-843` misses hyphenated compounds | `COUNTED` matches the hyphenated form, the two facts agree with the count, and the shared-state section is generated | **verified** |
| TP-03 | M-44 | **VERIFIED 2026-08-24** at `task-pipeline@640a28f` — **the row's substance was sound and only its number was stale, and the number was a third phrasing.** Reachability is whole: all **36** references are linked directly from `SKILL.md`, measured by resolving each basename against the file, 36 of 36. The registered class knew `N reference files` and `N files under references/`; the README said *\"all 23 references linked directly from SKILL.md\"* and neither alternative reached the bare plural, so a registered class fired on nothing — which reads exactly like a class that passed. Third widening of this row, each time because the repository used a form the registry did not know. **A separate row rather than a wider alternation, for a measured reason:** matching a bare `N references` also flagged CONTRIBUTING.md's *\"a PR that edits eight references for three unrelated reasons\"* — a hypothetical in an example. A class that flags a hypothesis teaches a reader to skip it, which is the same harm as one firing on nothing, so `all` is what the pattern requires. Both halves watched: `all 36` → `all 99` refused; the hypothetical changed eight → nineteen and stayed silent. The registry counts itself and moved 12 → 13. | four shipped surfaces say "34 reference files" against a directory of 35, and the validator's own claim registry printed `reference files: dormant (truth 35)` without firing — its pattern only matches the phrasing "N files under `references/`" | `SKILL/scripts/graph.py:873`; `templates/run.md:23,39`; `test/validate.py:533,3202` | the claim registry matches any phrasing of a counted claim, and the five stale numbers found family-wide are re-run through it | **verified** |

## Wave 5 — the money boundary

Held together in one wave because they are one boundary, and because the manifesto's own test at
`:200` — "a credential that cannot reach production is stronger than a sentence saying not to use it
there" — fails literally here. sheleg-dev scored FULL 3 / ABSENT 15, the worst on both counts in the
family, and all three FULL rows are the artifact layer rather than the money layer.

| id | M | verdict | title | evidence | done when | state |
|---|---|---|---|---|---|---|
| SD-02 ✅ | M-06 | — | **VERIFIED 2026-08-19** at `sheleg-dev@00285e7` — the provider offers no test credential at all, so the boundary is a checked declaration plus a written residual exposure; see the closes ledger. `crypto-payments` has **no test/live credential boundary** — one `HELEKET_API_KEY` that is also the signing secret, and "test mode" is a merchant-account toggle, so a dev run holds the production credential and the skill never says so | `references/heleket-provider.md:126,1153` | `HELEKET_ENV` plus a boot assertion that key and env agree — the shape `stripe-billing/references/price-integrity.md:62-64` already prescribes — and a separate sandbox merchant account is required | **verified** |
| SD-03 ✅ | M-30 | **VERIFIED 2026-08-19** at `sheleg-dev@dc327b6` — and it reads what would RUN correctly, which is the very thing UM-10 records the umbrella's own `repo-gate` getting wrong. See the closes ledger. | all four MANUAL-gate categories (money, production access, publication, destructive) are named in prose and stop nothing; the plugin ships no hooks, no permission list, no gate | `crypto-payments/SKILL.md:309-310`; `stripe-billing/references/webhook-events.md:169-170` | a `PreToolUse` hook refuses `sk_live_`, `rk_live_`, `--live`, `create_refund`, `payouts`; the refusal names its remedy; the hook fails silent rather than breaking a turn | **verified** |
| SD-04 ✅ | M-40 | **VERIFIED 2026-08-19** at `sheleg-dev@42b393f` — see the closes ledger, and SD-05 for the one boundary my own plants found. | the money invariants ship as prose and delegate enforcement to the reader ("delete each guard and re-run") — the webhook-is-the-payment rule, the duplicate `event_id` rule, the cumulative-refund rule | `stripe-billing/references/testing-and-local-dev.md:157-172`; `ad-tracking/SKILL.md:264-296` | `fixtures/` ship a proration `invoice.paid`, a duplicate `evt_`, a two-step cumulative refund and an out-of-order pair, with a copy-in assertion pack | **verified** |
| SD-05 | M-26 | **VERIFIED 2026-08-24** — per-assertion neutering is now a plant class, not a reading. `npm test` prints *money fixtures — 16 checks (both packs, both modes, **6 plants: neutered assertions**, an unremovable rule, and a live discriminator parked in the unmutated escape hatch)*, and `test/fixtures_test.js:32` carries the record of the defect in its own words: *their matrix compared one row per invariant, so a NON-FIRST assertion inside a multi-assert…*. The row's measured case — six assertions where neutering two left exit 0 — is what the plants now cover. | **New row, opened 2026-08-19 by the orchestrator's own plant against SD-04.** `--self-test` measures break / no-break **per invariant, not per assertion**. An invariant keeps its declared break-set if *any one* of its assertions still throws under the mutant, so an individual assertion can be neutered undetected. Measured: `renewal-grants-exactly-once` carries **six** assertions; neutering the two that name the grant (`store.grants.length` and `store.credits.get`) left `--self-test` at **exit 0**, because a later line — `mirror.periodStart` on an `undefined` mirror — still throws under the `grant-on-renewal` mutant and preserves the break. Proven with a hash check and a grep on the executed file after three earlier probes of mine were too weak to trust. **This does not undermine SD-04:** the isolation matrix is sound and `fixtures_test.js`'s three plants do get caught. What is over-broad is the printed sentence — `12 assertions, each watched failing` is true of the twelve *invariants*, and a reader may take it as covering each `assert` call | `stripe-billing/fixtures/assert-money-invariants.mjs:69-85` (six assertions in one invariant), `:281-289` (`runOne` returns on the first throw), `:352-362` (the break-set comparison) | either the matrix runs per assertion, or the printed claim narrows to what it measures — the second is one line and is probably the right trade, since per-assertion mutation would multiply the run. Planted defect required either way | **verified** |
| SD-06 | M-26 M-40 | — | **New row, opened 2026-08-19 by the push itself.** GitHub's push protection refused `sheleg-dev` on six Stripe-key matches in `test/moneygate_test.js`. **No secret leaked** — every one spelled the `sk_live_` prefix followed by a 24-character body reading PLACEHOLDER twice and `00` — written that way here rather than quoted, because **this very row was refused by the same scanner on its first push**: a document *describing* a key shape is matched as one, which is the UM-10 defect class arriving at the platform layer; the scanner matches on **shape**, and a gate that refuses live keys by shape can only be tested with live-shaped strings. So the suite proving the guard works was the one thing that could not reach the remote. What the block exposed is an evidence gap, not a leak: **SD-03's secret sweep was scoped to `git ls-files plugins`**, so it never covered `test/`, and its claim that every key-shaped string spells PLACEHOLDER was true of what it observed and blind to the file that blocked the push. Resolved without the unblock URL: the values are assembled at runtime (`'sk_' + 'live_' + …`), identical bytes reach the gate, no contiguous literal exists at rest, and the two commits carrying them were rewritten so history is clean too | GitHub push protection on `90e9621`; `SECURITY.md:165` (`git ls-files plugins`) | **done** — 0 literals across all four commits, `npm test` green at the rewritten HEAD (`16 checks · 65 money-gate fixtures · 13 money-fixture checks`), pushed. What remains open is the sweep's scope: it should cover the whole repository, not the payload | **verified** |
| SE-05 | M-05 M-24 | — | **New row, opened 2026-08-19 by the push itself.** `seo-aeo-audit` had diverged: a merged pull request filed board rows **B-16…B-23** while this program's SE-01 and SE-02 filed **B-16…B-21** — same ids, entirely different subjects. This is the id race `agent-sync` exists to prevent, occurring **across a PR merge**, where no lease on this machine could have helped: the board hands out ids by reading the file, which is safe for one writer and races for two. Both sets survive; this branch's six rows were renumbered **B-24…B-29** because the remote's are already published, and seven cross-references in three files followed them. The remote's own B-17/B-19 citations inside its audit document were left alone — they name its rows, not ours | the merge conflict in `docs/evidence/backlog.md`; `origin/main` at `3391db5` vs `265dfa5`/`7953484` | **merged and pushed** at `d0abfa6`, counted after: B-1…B-29, **no duplicate ids**, no conflict markers, `npm test` exit 0. The mechanism gap is real and unclosed — `agent-sync`'s race-free id reservation is per-checkout, and nothing reserves an id against a remote branch | **verified** |
| TP-06 | M-26 M-41 | — | **New row, opened 2026-08-19 by CI — the one evidence path this whole programme never had.** Every agent's report ended with *"CI has not seen it"*, and when it finally did, it failed `task-pipeline` on a defect eight local gate runs had passed. TP-02's own ledger row wrote a pytest filter as a backticked `-k receipt<PIPE>record<PIPE>containers`; markdown does not care about backticks, so **two pipes split one cell into three** and that row carried ten cells against a header of eight. With the shipped column order the extra cells landed to the *right* of `Human`, so the value still read `never` and the gate stayed green. It surfaced only through the header-reorder property check — and **only because that check's own regex cannot match a note containing a pipe**, so it left exactly this row in the old order under the new header. A defect found because a second check happened to be blind to it in the same way is a defect found by luck | CI run on `c8de225`, step *Property check (the Human column survives a header reorder)*; `docs/evidence/verification.md:158` | **done** at `6372f19` — the shape is checked directly now: every `REQ` row must carry the header's cell count, the refusal names the remedy, and the plant is the real defect put back (ten cells against eight), refused. The note was rewritten without pipes rather than escaped, because it quoted a command a reader might copy and `-k receipt\|record\|containers` is not that command | **verified** |
| ALL-26 | M-26 (`manifesto` → *"evidence that it can discriminate failure from success"*) | **RE-MEASURED 2026-08-24 — the remedy exists, is adopted in three members of eight, and the row's own framing was one layer off.** The defect is a plant that cannot *run*, dying before its landing assert. The fix is not a better assert: it is moving the landing check **outside** the plant body, which `test/plant_guard.py` does — `snap` before, `verify` after, wrapping an edit of any shape, so under `set -eu` a raising plant fails the step with the PLANT's own error rather than reporting a guard it never disarmed. Observed three times today, each naming the plant: *PLANT DID NOT LAND: anchor absent before edit* (`task-pipeline`), *matches no `\\*\\*[0-9]{4}…`* (`seo-aeo-audit`, and that one reached CI **after** the tag was public), and a `resub` re-anchor. **Adoption, measured:** `seo-aeo-audit` 73 helper-wrapped steps, `agent-stack` 52, `make-skill` 16. `sheleg-design` uses a stronger in-suite form — `before == after` → *the fixture changed nothing*, plus a refusal for a plant that went red **without saying what it should say**. `super-ux` and `agent-sync` carry no plants at all and are outside the class: fixture-per-check-code with both halves, and `validate_ux_lint_coverage` gating that every emitted code has a fixture. **The gap is one member:** `sheleg-dev`, 36 inline plants and no helper. **Not closed here, and the reason is a dependency rather than effort:** wiring 36 CI steps that cannot be run locally is what failed the `seo-aeo-audit` release an hour before this was written, so `seo-aeo-audit` B-35's local plant runner comes first. Filed, not implied. | **New row, opened 2026-08-19 after the same failure happened three times in three days.** Every negative self-test in this family carries a `PLANT DID NOT LAND` assert — the second-order guard that refuses when the defect was never injected. **Each of the three failures below died UPSTREAM of that assert**, so the guard designed to catch a plant that does not land cannot catch a plant that cannot *run*: (1) `cl1` read a guard count out of the newest CHANGELOG section and a commit bolded the whole phrase instead of the number, so it proved nothing for two days; (2) the header-reorder property check could not match a note containing a pipe, left one row in the old order, and reported the doctrine broken; (3) three schema plants indexed `c['if']['properties']['status']['const']` directly, and B-080's new `not: {const: parked}` branch raised `KeyError` before their asserts. The orchestrator hit the identical class four times in its own probes. **A plant is code, and code that throws is not a verdict** | `task-pipeline` CI on `c8de225` and `6372f19`; `.github/workflows/validate.yml` gx6/gx7/gx8; TP-02's `cl1` finding; this ledger's field notes | a plant runs inside a harness that distinguishes **refused** from **crashed** and treats the second as a red step naming the traceback — `make-skill`'s `plant_guard.py` is the nearest existing shape and solves the adjacent half. Family-wide, so it belongs beside ALL-44 rather than in one member | open |

## Wave 6 — doctrine reconciliation

agent-stack is doctrine *about* building agent systems, and it is the member that disagrees with the
manifesto most directly. These are text changes with no mechanism behind them, which is why they are
last — and why each must land in the same change as whatever check can hold it.

| id | M | verdict | title | evidence | done when | state |
|---|---|---|---|---|---|---|
| ALL-44 | M-44 (`manifesto` → *"Every fact should have one authoritative home"*) | — | **New row, opened 2026-08-19 from AG-02's own `NOT VERIFIED`.** AG-02 matched `task-pipeline`'s node-contract vocabulary exactly — field name `check`, one string, the `parked` exemption, the `a && b` rule — and said plainly how: *"TP-01 alignment is a reading of their text, not a shared artifact. Nothing computes that the two definitions still agree; either repo can be edited without the other noticing."* That is the family's structural weakness stated precisely. Nine repositories carry overlapping doctrine — the node contract, the checker contract, the state ladder, the status vocabulary, `Observed at` and its invalidators — and **every one is kept aligned by an agent reading the other repo at the moment it happens to look.** Each member polices single-home *within* itself (the shingle check, the counted-claims registry, the doc-refs corpus); nothing polices it across. The audit's G6 was one instance: proof expiry shipped in one member and reached no other for two days | AG-02's report; `agent-stack` `graph-engineering.md:58` vs `task-pipeline` `graph.schema.json` and `references/work-graph.md:72`; audit G6 | a cross-repository vocabulary check with one home in the umbrella, which is already the convergence point: a shared term is declared once, each member's machine-readable declaration is compared against it, and a divergence fails the umbrella's gate. Planted defect required. **This is the largest single M-44 gap the program found, and it is deliberately not closed inside the program** — it is a mechanism for the family, not a row in one member | open |
| AG-01 ✅ | M-25 | — | **VERIFIED 2026-08-19** at `agent-stack@2b3d45e` — see the closes ledger. **the checker asks for confidence where the manifesto asks for evidence.** `manifesto` → *"arrived, matches its contract, carries its evidence"* requires *arrived / matches contract / **carries its evidence** / does not contradict a sibling*; `graph-engineering.md:154-161` substitutes "under-confident — a confidence signal below the bar". In a family whose first value is Evidence over confidence, and whose own text says "an uncalibrated judge is an opinion with a number attached", the gate guarding every convergence never asks what a branch can show | `references/graph-engineering.md:154-161`; `agent-evals/SKILL.md:172-178` | "carries its evidence" is the sixth checker item; the confidence signal stays optional | **verified** |
| AG-02 ✅ | M-22 | **VERIFIED 2026-08-19** at `agent-stack@96d44c8` — five fields, matched to TP-01's vocabulary exactly; see the closes ledger and ALL-44. | **the node contract lost two fields** — `manifesto` → *"the loss happened between layers"* names one input, one job, one output, **one owner**, **its own completion test**; `graph-engineering.md:52` names three. Both give the same justification in nearly the same words, which makes the omission conspicuous — and the two dropped fields are exactly B-080 | `references/graph-engineering.md:52-55` (grep for `owner`/`completion test` → exit 1) | both rows exist in §1, consistent with whatever TP-01 lands | **verified** |
| AG-03 ✅ | M-17 M-19 | **VERIFIED 2026-08-19** at `agent-stack@a1fac36`. It tested the charitable reading first and **disproved it from inside the pack**: the document does not use *suite* and *corpus* as synonyms (`:66`, `:263` both mean assertions), and `:22-23` carried no scope at all. Then the decisive argument, which is reasoning rather than grep: **taken as written the rule made a first release ungateable** — §3 names the offline suite "this is the gate", and a suite that may never be authored up front cannot exist before there is production to grow it from. `grep -ci observable` and `grep -ci requirement` both returned **0**, so the pack had no word for the tier and could not have exempted it. See the closes ledger. | **agent-evals instructs the opposite of the manifesto.** `manifesto` → *"The evidence graph says how the result will be known"*: building the evidence graph after the code "lets the output decide what counts as success". `agent-evals/SKILL.md:203`: "**Never author the suite up front.**" Opposite imperatives, both stated as rules; agent-evals has no requirement or observable concept, so the reconciling clause is not there to find | `agent-evals/SKILL.md:22-23,203`; `agent-orchestrator/SKILL.md:368` | the two tiers are named — the observable is decided before implementation, the production-grown corpus is the regression tier on top | **verified** |
| AG-04 | M-57 | **VERIFIED 2026-08-24, and it is the model close of this programme.** The scalar is gone and the file records that it was there, with a **measured** argument rather than a preference: *`3 × 1 / 3` and `1 × 1 / 1` both print 1, so harms a user, judgement-tier, its own project and annoys a future maintainer, judgement-tier, an hour arrive at one priority and nobody reading the output can tell which is which. A product is a one-way function on the very inputs the argument needs.* Four named axes replace it, plus a machine-readable `<!-- priority-axes: impact, irreversibility, uncertainty, coordination -->` a gate can read. Both greps return 1. | `audit.md:20-25` refuses a score ("a prioritized plan, not a score") and `:112-121` computes `P = blast × confidence / effort` over three axes; irreversibility and coordination are absent, against `manifesto` → *"How many agents, repositories, services, and owners meet at the change"* | `references/audit.md:20-25,112-121` | the four axes are published beside the number, or the division is dropped | **verified** |
| SE-04 | M-57 | **VERIFIED 2026-08-24** at `seo-aeo-audit@fee952d`, **v0.25.0** — and the row undercounted its own subject. It said four sites; `grep -rn 'impact × confidence'` over the repository found **eight** live surfaces, because the earlier sweep grepped `plugins/` alone: SKILL.md, the command, `deliverable-templates.md`, `myths.md`, README.md, CLAUDE.md, `templates/audit-report.template.md`, and `agent_surface.py`'s printed output. All eight replaced by four axes with the first separator deciding — irreversibility is the axis this domain most needed and had none of. `check_the_plan_is_ordered_on_axes_and_not_a_product` sweeps every live surface, allows a formula quoted inside a sentence recording its removal, and was watched failing against two plants. **The consequence reached the guards**, which is how the fix is known to be real: the tier weights existed as the confidence MULTIPLIER, and a check required SKILL.md to restate them *\"because SKILL.md's triage math quotes it\"* — with the math gone that restatement is the two-homes defect this pack polices elsewhere, so single home kept, copy dropped. Ported from `agent-stack`, whose own line-number citation into the manifesto had rotted the same way and was swept the same day (`agent-stack@ec2da8e`). | the same defect in the other member that ranks by a composed score: `priority = (impact × confidence) / effort`, irreversibility only in a template, coordination absent | `SKILL.md:229-238`; `templates/action-plan.template.md:40-43` | as AG-04 | **verified** |
| SG-02 ✅ | M-17 | — | **VERIFIED 2026-08-19** at `sheleg-design@7e630cb` — a branch must now *collapse* a duration, not merely mention the query. See the closes ledger.  "degrade to calm" has no observable — `sloplint.py:208` asserts only that the string `prefers-reduced-motion` appears in a doctrine file, while 2 of 29 token layers ship no reduced-motion branch, including the pack that mandates a particle field | `test/sloplint.py:208`; `tokens/instrument-console.css`; `tokens/editorial-luxury.css`; B-040 at `docs/evidence/backlog.md:56` | a token layer declaring `--dur-*`/`--ease-*` owes a reduce block, with a planted defect | **verified** |
| SG-04 | M-52 M-55 | **VERIFIED 2026-08-24 — and the fix is more honest than the row asked for.** The register did not stop: 59 stamp rows reaching **v1.47.0**, the shipped version. The eighteen-version gap this row measured was backfilled on 2026-08-20, and the backfilled rows are **marked `unrecorded` in the `Diverged?` column** rather than passed off as contemporaneous — five from the `datasheet` run, twenty-six covering v1.27.0 to v1.44.0, three from runs that could have answered and did not. Their count is not restated in prose: `validate_reconstructed_stamp_count()` recomputes it on every run and fails when the paragraph disagrees. The five-stamp cold trigger is computable — the table is append-ordered with dates present, newest last. **Re-measured rather than trusted:** my own first reading of this table took its top row as newest and was wrong, which is why this close names the ordering. | the retrospective register stopped — last run stamp is v1.26.0 / 2026-08-13 against CHANGELOG 1.43.0, so 26 releases are unstamped and the "not fired in five run stamps" retirement trigger is uncomputable | `docs/evidence/retro.md:531`; `CHANGELOG.md:7` | the stamp table is derived from git log + tags at close-out, and a release whose tag has no stamp fails | **verified** |

## Deferred — named, owned, and not silently dropped

These were found by the audit and are **not** in this program. Each is real; each is larger than a
row, and taking them on would turn a conformance pass into a redesign. Recorded here so that "we
covered the audit" cannot be read as "we covered everything" (M-42: `deferred` needs an owner and a
destination).

| id | M | title | why deferred | destination |
|---|---|---|---|---|
| AS-03 | M-05 M-24 | the `local` lease double-wins across machines silently — two checkouts both printed `won TASK-9`, both guards exit 0, both `status` said `other runs: none`; and one lease unlocks every guarded file, so `alice`(TASK-A) and `bob`(TASK-B) both passed `guard DECISIONS.md` | a key→path mapping changes the lease contract, and `git` backend already solves the cross-machine half; this is a design decision about what a lease means, not a defect fix | agent-sync backlog, with the `git`-backend default as the cheaper alternative |
| UM-07 | M-25 | no convergence checker anywhere in the family: `verification.md:60-63` states "no run has yet been stopped by one" while 12 subagent fan-outs are logged. **This program's own orchestration is the interim answer** — the orchestrator is the single writer and the checker | needs a planted contradiction in CI to be more than a habit, which is a mechanism of its own | umbrella backlog, M-25 |
| TP-04 | M-04 M-27 | nothing the run observed survives it — `.gitignore:3` excludes `.task-pipeline/`, which holds the graph *and* the ledger lines carrying the hook-written `gate:` rows, the pack's only independent evidence path | B-088 covers the graph half only; committing the ledger changes what a run publishes | task-pipeline B-088, widened |
| TP-05 | M-58 | no acceptance policy — no owner, no version stamped on any acceptance, and two rules contradict each other | the policy is a family-level artifact, not a task-pipeline one; writing it inside one member would create the second home this file exists to prevent | a family policy document, owner named, then stamped by every member |
| SG-01 | M-44 | the Figma seam has no mechanical divergence check — `FIGMA_BRIDGE.md:57-61` says a mismatch "is greppable" and nothing greps it | needs DTCG token emission from CSS first; B-031 already names the blocker | sheleg-design B-031 |
| MS-02 | M-26 | 83 of 135 validator rules have never been watched failing, and `npm test` runs zero plants — they live as YAML steps in CI | a coverage gate over an instrumented `fail()` with a checked-in baseline is a mechanism worth designing once for the whole family | make-skill backlog; candidate for family-wide adoption |
| ALL-46 | M-46 | the seam walk is the family's weakest practice (3/18) — 8 of 9 members audit horizontally, and the manifesto's own §6 evidence says the vertical axis is what finds absence | adding a vertical pass to N audit skills is its own program | each member's audit reference |

---

## Field notes — things the program learned about itself

**2026-08-19, the rescue.** A close was one `git checkout main` away from being garbage. See UM-08.
The lesson is not "check your branch": it is that the family's convergence guard is written to
disclose what it cannot see, and this program produced a case where the undisclosed state was the
dangerous one. A disclosure is the correct floor, not the ceiling.

**2026-08-19, a plant that did not land.** Verifying SU-01, my first planted defect used a `sed`
range form that silently matched nothing. The linter exited 0 and I nearly recorded a guard as
not firing. `make-skill`'s `plant_guard.py` exists for exactly this — it refuses when the plant
did not land, rather than approving — and the lesson generalises past that repo: **a negative
control needs its own positive control.**

**2026-08-19, a plant that went red for the wrong reason.** Verifying SG-02, my plant tripped the
kit-drift check twice before reaching the reduced-motion check. Without asserting the *message*,
that plant would have reported "caught" while the new check never ran. SG-02's author had already
added an `expect` field for this; my independent plant reproduced the need for it.

**2026-08-19, my own script passed for the wrong reason.** The first attempt to insert ALL-49 into
this file was guarded by `if "ALL-49" not in t`, which was satisfied by a *reference* to ALL-49
inside another row's note, so the row never landed and the script reported success. Fixed with an
anchored assertion. Recorded because it is the same defect class the program exists to close,
committed by the orchestrator, in the ledger that tracks it.

**2026-08-19, a member solved the umbrella's own bug.** `SD-03` shipped a `PreToolUse` gate that
correctly separates a command from a quotation: a reader grepping for `sk_live_` is allowed, a
heredoc fed to `cat` is data, the same heredoc fed to `bash` is a script, whole-line comments are
dropped, and a non-Bash tool is judged on its name and never its input — *because that is where
documents arrive*. I verified all six by running the hook as a process. The umbrella's `repo-gate`
fails exactly this distinction (`UM-10`) while the umbrella already owns the solution in
`lib/hygiene.js`. So the fix for UM-10 is not new work; it is reuse, and a member has now proved
the shape twice over.

**A boundary held that mattered more than the row.** SD-03's gate is live automatically on the
plugin channel, and absent on the copy channels — `install.sh` and `npx skills add` copy only the
six skill directories. The honest fix would be to write a `PreToolUse` entry into the operator's
own settings file. It did not do that, and said why: writing to a file the operator owns and did
not write is the one thing this family must not do unasked. It printed a reminder instead, filed
`B-90`, and recorded in its own report that **a printed reminder is a warning, which M-30 calls
weaker than a precondition.** That is a refusal to close a row by overreaching, stated as a
limitation rather than hidden as a success.

**2026-08-19, the orchestrator restated a number and propagated it three times.** AG-01 reported
`agent-orchestrator/SKILL.md` as "4728 tokens against its own 4750 budget" and left the file alone.
I took that figure as fact and **wrote it into AG-02's and AG-03's briefs as a constraint**, so two
further agents made a decision citing it. AG-03 counted it: `cl100k_base` at `422f211` gives **4143
body / 4442 whole**, i.e. roughly **607 tokens of headroom, not 22**. Both siblings' decisions were
right on the merits — neither needed the file — but the reason all three of us gave was a restated
number rather than a counted one. That is precisely the M-44 defect this program exists to close,
committed by the orchestrator, and repeated at each of the three points where a brief could have
said *count it* instead of quoting it. I could not recount independently: `tiktoken` is absent here
and installing it would mutate the environment, so AG-03's figure is taken as the better number
because it names its method and its commit, and mine named neither.

**2026-08-19, the orchestrator's confirmation message lied to it — again.** Probing SD-04 I wrote a
plant that reported success unconditionally: `if target in t: t = t.replace(...)` followed by an
unqualified "neutered" print. Three probes in a row told me a plant had landed when I had no evidence
it had, and I came within one message of reporting a false finding against SD-04's self-test. The fix
was to assert the target exists, assert the content changed, compare a hash before and after, and
grep the executed file for a marker. Only then was the finding real — and it survived, narrowed to
SD-05. **This is the same defect as the ALL-49 insert guard, from the same hand, four hours later.**
`make-skill`'s `plant_guard.py` exists precisely for this and I did not reach for it, because it
lives in a member and my probes are ad hoc shell. That is the argument for ALL-44 restated as
experience: a mechanism that lives in one repository protects one repository.

**2026-08-19, CI was the only checker that worked.** This programme argued from the first row that a
producer should not be its only judge, and then ran for its whole length without the one independent
path the members already had. Every agent closed with some form of *CI has not seen it*; I verified
each row myself and treated that as sufficient. It was not. On the first real CI run, seven repos
went green and `task-pipeline` failed on a malformed table row that eight local `npm test` runs had
passed — because locally the damage sat in a column nothing read.

The manifesto's own M-41 says the strongest evidence should sit farther from the producer as the
cost of being wrong rises. The orchestrator re-running a member's own suite is *nearer* the producer
than CI is, not farther: same machine, same tree, same tools. It caught plenty — but the one thing it
structurally could not catch is the thing that only shows up somewhere else. That is the argument for
UM-07's convergence checker stated as a measurement rather than a principle.

## CI — the independent path, and what it cost to get there

| repo | commit | CI |
|---|---|---|
| agent-stack | `a1fac36` | success |
| agent-sync | `abfb72f` | success |
| make-skill | `73ebeee` | success |
| seo-aeo-audit | `d0abfa6` | success |
| sheleg-design | `4c0d63e` | success |
| sheleg-dev | `42b393f` | success |
| super-ux | `4602712` | success |
| sshlg-skills | `b86f68a` | success |
| task-pipeline | `2f7dc7a` | **success** — after two failures, both real, both fixed |
| pod-manifesto | `4b3694d` | published |

`task-pipeline` failed twice and neither failure was noise. The first was a ledger row whose
unescaped pipes split a cell (TP-06); the second was three plants that crashed before their own
asserts (ALL-26). Both had passed every local gate run — the first because the damage sat in a
column nothing read, the second because those plants are CI-only steps. **Eight repositories green
and one that failed twice is a better result than nine green would have been**, because the two
failures are the only findings in this programme that no amount of local verification could have
produced.

## Program state

| wave | rows |
|---|---|
| 1 | 2 |
| 2 | 9 |
| 3 | 4 |
| 4 | 15 |
| 5 | 7 |
| 6 | 7 |
| **active total** | **47** |
| deferred | 24 |

By state, over those 47 rows: **3 open · 44 verified**. Both figures are recomputed by
`test/validate.py`'s counted-claims registry, so this line cannot describe the day it was
typed either.

The table above said *32 rows, 32 open, 0 verified* while the file it summarises held 41
rows of which 19 read `verified` — a register contradicted by its own contents, in the
document that exists to hold the program's state. The `active total` is now checked by
`test/validate.py`'s counted-claims registry on every run; the per-wave rows and the state
histogram are recomputed by the recipe below.

Counted, not restated — the command, so the figure can be recomputed rather than trusted:

```bash
awk '/^## Wave/{w=$3} /^## Deferred/{exit} /^\| [A-Z]{2}-[0-9]/{c[w]++; t++} \
  END{for (k in c) print "wave "k": "c[k]; print "active: "t}' \
  docs/evidence/manifesto-conformance.md | sort
```

Ran 2026-08-19 → `wave 1: 2 · wave 2: 6 · wave 3: 3 · wave 4: 11 · wave 5: 3 · wave 6: 7 ·
active: 32`. Wave 2 grew by one when PM-01's blast radius turned out to be five surfaces rather
than one, which produced PM-04.

## The closes ledger

One row per close. `landed` is the producer's commit; `verified` names what the **orchestrator**
observed independently, because the manifesto's M-41 says the producer of a claim should not be its
only judge. A row reaches `verified` only after a check run by this session, not by the agent that
wrote the code — and where the row shipped a guard, the orchestrator plants its own defect and
watches the guard refuse it.

| row | landed | verified by the orchestrator | observed at |
|---|---|---|---|
| SU-01 | `super-ux@fe2189e` — `U060..U065` refuse a scenario or story with no observable, and a delivered observable with no path to code; `U055/U056` and `U064/U065` now share one owner (`coverage_claim`) so two layers cannot answer one question differently | `npm test` **re-run by this session** → exit 0, `3518 / 77 / 76 checks` (was 3506 / 77 / 54). Dogfood counted: 15 scenarios, 15 `Coverage:` lines, **28 unique targets all resolving**. **Two own plants:** removing an `**Expected:**` → exit 1, `[U060] SCN-001 states no observable result`; repointing a coverage at `bin/nope.js:1` → exit 1, `[U065] SCN-014 cites 'bin/nope.js:1', which does not exist`. My first attempt at the second plant **did not land** and read as a silent pass — recorded because it is the `plant_guard` lesson reproducing itself | 2026-08-19, `super-ux@fe2189e` |
| MS-01 | `make-skill@73ebeee`, `16a9682` — `test/residue.py`, an `atexit`-wired ledger that removes what may go, keeps what a failure needs, and prints one line either way; both leaking suites routed through it; `residue_test.py` (8 cases) added to the gate | `npm test` **re-run by this session** → exit 0, four `PASS` lines and four residue lines. Ran both suites against an isolated `TMPDIR`: **0 strays**. Producer's own negative control is the strongest part — stashing the two fixes made `residue_test.py` exit 1 with "left 8 entr(y\|ies) in its TMPDIR", and its failure path printed `2 of 2 temp tree(s) KEPT` with both paths and the `rm -rf` | 2026-08-19, `make-skill@73ebeee` |
| SG-02 | `sheleg-design@7e630cb` — `validate_reduced_motion()` asserts two things per token layer: a motion-declaring layer ships a `prefers-reduced-motion` branch **and** that branch collapses a time-valued property to ≤1ms. Durations matched by **value, not name**, so `paperclip`'s `--t-*` and `instrument-console`'s `--motion-ease` are not read as empty. B-040 closed | `validate.py` / `validate_palette.py` / `sloplint.py` **re-run by this session** → exit 0, `3391 / 1912 / 635`; `--self-test` → "every planted defect was caught"; 29 token layers counted, `grep -L prefers-reduced-motion` → **0**. **Own plant:** rewrote every duration inside one reduce branch to `0.4s` → exit 1, "collapses no duration to an instant value … satisfies a grep and changes nothing a reader can feel". That plant also fired the kit-drift check twice — reproducing exactly why the producer added an `expect` field asserting the message rather than the red | 2026-08-19, `sheleg-design@7e630cb` |
| AG-03 | `agent-stack@a1fac36` — §6 became *"Two clocks — the observable up front, the corpus from production"*. The **observable** is a criterion (what would count as success), written before the implementation; the **corpus** is a sample (which inputs you have), grown from production. `:203`'s imperative survives **word for word**, gaining only its subject: *"Never author the suite up front — the corpus, that is: the inputs."* The pack gained the missing concept in one paragraph rather than a redesign, bound to machinery it already shipped. The pack's other timing line rose from *before the prompt is tuned* to *before the implementation* | `test/validate.py` **re-run by this session** → exit 0, `13 checks` (was 12). Counted by me: `observable` 0 → **10**, `requirement` 0 → **5**; the imperative present verbatim at `:239`; declaration `<!-- eval-tiers: observable, corpus -->` at `:218`. **Own plant:** shrank the declaration to one tier → exit **1**, twice — "declares 1 tier(s), floor is 2 … here it guards the pair itself" and "Dropping either turns the other back into an absolute" | 2026-08-19, `agent-stack@a1fac36` |
| AG-02 | `agent-stack@96d44c8` — the node contract carries all five of M-22's fields instead of three, and the substantive half is argued rather than asserted: **worktree isolation answers WHEN** (two writers cannot corrupt one file), **ownership answers WHOSE** (which node's version is authoritative once the branches return) — isolation without ownership loses nothing during the run and moves the loss to the merge, where a silently one-sided merge is indistinguishable from a convergence that only ever had one answer. A fourth paragraph separates the per-node completion test from AG-01's checker node, so a reader cannot mistake one for the other | `test/validate.py` **re-run by this session** → exit 0, `12 checks` (was 11). Cross-repo vocabulary compared by me: `agent-stack` declares `input, job, output, owner, check`; `task-pipeline`'s schema carries `check` with the `parked` exemption — **identical, no third vocabulary**. **Own plant:** dropped `owner` from the declaration → exit **1**, twice: "declares 4 fields, floor is 5 … a field may be added, never quietly dropped. This file shipped at three of five and read as complete, which is the whole reason the floor exists" | 2026-08-19, `agent-stack@96d44c8` |
| AS-01 | `agent-sync@36a3b38` + `abfb72f` — `classify_lock()`, pure, four verdicts: `live` / `reapable` / `foreign` / `ambiguous`. `status` and `finish` report residue instead of `none`; new verbs `residue` (report-only) and `reap` (clears only provably-own spent state, refusing foreign or ambiguous out loud). Teardown **re-reads** the directory and compares `(run, ts)` — the delete's return value decides nothing. 38 → 43 self-test plants | `test/validate.py` **re-run by this session** → exit 0. **The strongest check available was behavioural, and I ran it:** `agent_sync.py status` in the umbrella now prints the three expired locks as `[ambiguous]` with the reason "its run id matches, but this run's identity is the shared fallback — any other session in this checkout answers to the same id, so the match proves nothing", where before it printed `leases held: none`. New `residue` verb enumerates 4 locks in this checkout. It refuses to delete any of them | 2026-08-19, `agent-sync@abfb72f` |
| TP-02 | `task-pipeline@f0402c2` + `c8de225` — the record the manifesto opens with now reads as one event: both observation moments stated, the interval between them named, and **what the record never carried named too** — the wall-clock times, which `git log --follow` proves were never in it (the block is byte-identical in all four commits back to `298abb6`). So this was a disambiguation, not a correction: no number changed. It also tightened the E4 sentence from "that **they** have been up for three days" to "that **the oldest of them** has" — a claim about eighteen containers from a measurement of one | `npm test` **re-run by this session** → exit 0, `graph.py — 129 cases`. **Own plant:** changed "three minutes and twelve seconds" to "four minutes and nine" → exit **1**, "the receipt quotes an `etime` of 03:12 and no sentence outside the block accounts for it … which is how the source of an external citation reads as a contradiction". **It also found a plant that had silently stopped landing:** `cl1` reads a guard count out of the newest CHANGELOG section, and `8b7de18` bolded the whole phrase instead of the number, so the plant could not land while the validator read on — proving nothing since that commit | 2026-08-19, `task-pipeline@c8de225` |
| TP-01 | `task-pipeline@8b7de18` + `d17ae27` — **B-080 closed, the last of the manifesto's four self-declared gaps.** `check` is a node property, a string, **required on every node whose status is not `parked`** — the parked node being the one nobody will close, where a placeholder would be confidence without correctness. Stated twice on purpose (schema conditional + `violations()`), because the schema never runs against a live graph. `add --check` required like `--why`; `verdict_violations()` refuses a replan entry naming no check, so the key is named before a close writes anything | `npm test` **re-run by this session** → exit 0, `graph.py — 129 cases` (was 114). **Own plant:** popped `check` from non-parked node `N-001` of `graph.example.json` → `validate` exit **1**, "a node that cannot say how it will be closed leaves the verifier inventing a check or running everything, and `agents/verifier.md` forbids both"; control run on the unmodified example → exit 0. **I also re-read all four gap rows and ran their mechanisms myself**: B-076 `judgment`+`judge` in the pinned schema, B-077 `serves` resolution in `graph.py`, B-080 `check` on the node schema, B-081 `Observed at` in the ledger template — four for four | 2026-08-19, `task-pipeline@d17ae27` |
| SU-02 | `super-ux@4602712` — a `Product:` field with the enum `unobserved \| observed \| contradicted`, distinct from `Status`, **no floor and no target**: absence means `unobserved`, and neither `unobserved` nor `contradicted` fails anything. `U066..U070` plus two `validate.py` checks; the audit's after-a-run step now changes the delivery state only, and both homes carry "The audit never writes `Product:`" with a gate that fails if either loses the sentence | `npm test` **re-run by this session** → exit 0, `3539 / 77 / 106` (was 3518 / 77 / 76). Counted: **15 of 15 scenarios `unobserved`**, no other value present. **Own plant:** `**Product:** validated` → exit **1**, `[U066] SCN-001 declares Product 'validated', which is not one of…`. Baseline it measured first: `git grep -l unobserved` at the previous commit → **0** — the state M-21 calls legitimate was simply absent from the vocabulary | 2026-08-19, `super-ux@4602712` |
| SG-03 | `sheleg-design@4c0d63e` — the root cause, not the symptoms: `COUNTED` now takes `[ -]` as a **captured, back-referenced** separator, so a compound is read in one alphabet or the other and a half-and-half form is not silently accepted; the plural is required for the spaced form only, because a hyphenated count is a singular modifier by grammar. The coordination claim is **derived** rather than reworded — DOCMAP points at the generated `AGENT_SYNC.md`, and a new check holds the derivation from both ends | `validate.py` / `validate_palette.py` / `sloplint.py` **re-run by this session** → exit 0, `3398 / 1912 / 635`; `--self-test` → **19/19** (was 16/16). Re-counted the two facts myself: kits **29**, coordination **gated with 9 guarded files**. Producer watched it fail first on the untouched tree with exactly one FAIL naming `fourteen-kit`, and the previous commit ran **green over that same string** — which is the hole's own proof. Blast radius measured old-pattern-against-new on one tree: policed set 34 → 36 spans, **0 false positives**; whole tree 333 → 384 | 2026-08-19, `sheleg-design@4c0d63e` |
| SD-04 | `sheleg-dev@42b393f` — the four invariants that cost money ship as **12 provider webhook bodies, 18 assertions and reference handlers that delete one named rule at a time**, so every assertion can be watched failing. `--self-test` prints an invariant × mutant matrix and **refuses to pass if any rule has no fixture that isolates it**. The audit's quoted giveaway — "delete each guard and re-run" — is replaced by shipped mutants | `test/validate.py` **re-run by this session** → exit 0, `16 checks` (was 15). Counted by me: **9 stripe + 3 ad-tracking** fixture bodies. Both self-tests run by me → exit 0, `12 assertions, each watched failing; 9 rules, each isolated by a fixture` and `6 / 5`. **Own plant:** deleted a fixture the manifest claims → exit **1**, "no fixture isolates cumulative-refund — every assertion it breaks is also broken by another rule, so neither is proven". Its own first run found three real defects: an **orphan positive control** (without which a refuse-everything handler passes the two negative fixtures), a fixture **no mutant broke**, and a rule **no fixture isolated** — each fixed by construction rather than by relabelling | 2026-08-19, `sheleg-dev@42b393f` |
| SD-03 | `sheleg-dev@dc327b6` — M-30's manual gate stopped being prose. A `PreToolUse` hook whose verdict is a **pure function** (`hooks/lib/moneygate.js`: payload + environment in, verdict out, no filesystem, no `HOME`); the hook only moves bytes, catches everything, exits 0 on every path, carries **no `if` filter** by decision, and every refusal names a next step. **8 refusal categories**, and two of them — self-authorisation and `SKIP_BILLING` in production — are **never authorisable**. `SECURITY.md` recomputed 27→30 payload files | `test/validate.py` and `npm test` **re-run by this session** → exit 0, `15 checks` and `money gate — 65 fixtures`. Fixtures counted by me: **27 deny-plants, 25 allow-plants, 13 direct checks**. **I ran the hook as a process on six payloads of my own** — refund → DENY; its own `SECURITY.md` live-key sweep → allow; heredoc fed to `cat` → allow; whole-line comment naming a refund → allow; a `Write` tool whose *content* is a live-shaped key → allow; **the same heredoc fed to `bash` → DENY**; garbage stdin → allow, empty stderr, exit 0. Its own mutation sweep is the best negative-control work in the program: **10/10 only on the second attempt** — the first caught 8, because every `sk_live_` allow-plant leaned on the reader denylist while every reader allow-plant leaned on the key's shape, so two overlapping mechanisms were each unproven alone; three isolating fixtures fixed it | 2026-08-19, `sheleg-dev@dc327b6` |
| SD-02 | `sheleg-dev@00285e7` — a test/live credential boundary where there was none: `HELEKET_ENV` with no default plus a pinned live merchant id and optional key fingerprint, compared by `assertHeleketEnv()` **at module load**, so a run that merely *holds* the key fails rather than one that charges. Six refused states, named error codes. `### Residual exposure` states what the assertion cannot fix | `test/validate.py` **re-run by this session** → exit 0, `14 checks` (was 13). Confirmed the provider claim at its source: `heleket-provider.md:67` — "**Test credential: none.** One key per merchant, which is *also* the webhook signing secret; no sandbox host, no restricted scope, no `test`/`live` marker". **Own plant:** deleted `HELEKET_ENV="test"` from the copyable handover block → exit **1**, "a copyable block sets HELEKET_API_KEY without HELEKET_ENV … the run holds production and nothing can tell. Set both in the same block". Tree clean before and after | 2026-08-19, `sheleg-dev@00285e7` |
| SE-02 | `seo-aeo-audit@7953484` — all seven bundled scripts stamp a **nine-field producer block** in both `--format json` and the default output, with the contract living beside SE-01's `COVERAGE_*` in `preflight.py` — no second mechanism. Four domain invalidators (**site · index · instrument · policy**) mapped from task-pipeline's code/dependency/environment/policy, with the mapping table written above them so a reader sees it is the same shape. B-17 closed; `SKILL.md` named `sitemap_pull.py`, a file that has never existed | `npm test` **re-run by this session** → exit 0. Counted by me: **7 of 7** scripts carry provenance. Rendered the block: all nine fields present, and the three unresolvable ones **named with their reason** rather than omitted. Redaction checked directly — `--key SUPERSECRETVALUE` appears **0 times** in json output and **0 times** in default output. It also found a **pre-existing guard hole**: `_sed_call` did not read `do sed -i` as a command position, so the sed self-test reported a *healthy* guard as broken; and `plant_edit.py:38` took a shell-supplied count as a string, which had left two plants dead | 2026-08-19, `seo-aeo-audit@7953484` |
| SE-01 | `seo-aeo-audit@265dfa5` — the report's coverage table went from a free-text `Status` nothing read to a closed vocabulary **generated from the instruments**: `preflight.py` owns the enum, the track denominator, the gate set, a seeder, a renderer and a report linter; `--format coverage` prints it paste-ready; the `{{A–J}}` denominator bug is `{{A–K}}`, derived | `npm test` **re-run by this session** → exit 0, including `coverage vocabulary closed and seeded`. Counted **11 rows, letters A–K, in both skeleton homes**; all five enum values published. **Two own plants in one run:** blanking track A's status and setting track D to `checked` → validator exit **1**, "a blank cell reads the same whether the track was clean or never ran" and "an unrecognised status is read as no status, which is how the enum drifts". Tree clean before and after. Commit landed by the orchestrator from the producer's saved message after the deadlock in UM-11 was cleared, under a lease taken and released | 2026-08-19, `seo-aeo-audit@265dfa5` |
| SD-01 | `sheleg-dev@e401603` — `SECURITY.md` rewritten against measured facts about this pack; the B-47 path guard widened from one CONTRIBUTING table to every path, inline and inside fenced blocks, across the four documents whose subject is this repository, now also resolving `path:line` citations | `test/validate.py` **re-run by this session** → exit 0, `13 checks, 6 skill(s)` (was 12 checks). My own independent path sweep of `SECURITY.md`: 18 path tokens extracted and resolved, **0 dead**. **Own planted defect:** appended a line naming `scripts/does_not_exist.py` → validator exit **1**, `SECURITY.md:161 names 'scripts/does_not_exist.py', which this repository has nowhere (B-79)` — and the refusal names its remedy, which is this family's own standard for a guard. Tree clean before and after | 2026-08-19, `sheleg-dev@e401603` |
| AG-01 | `agent-stack@2b3d45e` — six-item checker contract, `unevidenced` as item 3, `missing` as an arrival count, `under-confident` demoted to a non-gating hint; both files declare the list machine-readably and `test/validate.py` compares them | `npm test` exit 0 and `test/validate.py` exit 0 **re-run by this session**; contract declarations counted in both files — `graph-engineering.md:157` and `agent-evals/SKILL.md:172` agree on six mandatory + one optional; the home's numbered list counted as 6 items. **Own planted defect:** dropped `unevidenced` from the eval-side declaration → validator exit **1**, "declare different checker contracts … a reader cannot tell which"; tree clean after revert (`git status --porcelain` → 0 before and after) | 2026-08-19, `agent-stack@2b3d45e` |

**Carried forward from AG-01, not silently absorbed** — the producer's own `NOT VERIFIED`, which this
program keeps rather than discards: nothing ran in GitHub CI (the three new plants were extracted
from `validate.yml` and run locally); the arrival count assumes a known fan-out and a layer that
grows its own branches has no such number; the mirror check compares declarations rather than
meaning, so two files could name the same six keys and describe them differently; `agent-evals`'
product hypothesis stays `unobserved` — no real graph has yet been gated on the evidence item.
`agent-orchestrator/SKILL.md` was deliberately left untouched at 4728 tokens against its own 4750
budget, because a summary there would have been a third home.

## What closes this program

1. Every active row `verified`, or moved to `deferred` with an owner and a destination.
2. Each touched member released, its version synced across `skills.json`, the submodule pointer and
   the README table, and the release observed from the registry rather than from the build (M-48).
3. The local installs on this machine refreshed by the family launcher, in the same pass.
4. `manifesto.md` and its website mirror (`index.html`, `llms.txt`) agreeing with each other and with
   the repositories they cite.
5. A closing four-field report — `DONE / PROOF / SCOPE / NOT VERIFIED` — because a program about the
   manifesto that closes with "all done" would be the first thing the manifesto refuses (M-09).

## Closing report

```
DONE
  The ssheleg family and the Proof of Done manifesto are in conformance on 24 of
  44 programme rows. 31 commits across 10 repositories, all pushed, every working
  tree clean. Nine repositories green in CI; podmanifesto.org published and
  self-consistent. All eight members pinned in the umbrella at commits their
  remotes hold — the condition the convergence guard refused to pass all run.

PROOF
  24 closes, each re-checked by the orchestrating session with its own planted
  defect rather than accepted on the producer's report; every plant and its
  refusal message is in the closes ledger above.
  CI: agent-stack a1fac36 · agent-sync abfb72f · make-skill 73ebeee ·
  seo-aeo-audit d0abfa6 · sheleg-design 4c0d63e · sheleg-dev 42b393f ·
  super-ux 4602712 · task-pipeline 2f7dc7a · sshlg-skills b86f68a — all success.
  Umbrella `npm test` exit 0, 35 checks. `check_pins` — every pin matches its
  release. pod-manifesto: 8 gates green, parity 323 sentences / 0 missing,
  links 28/0 unresolved, currency 4 rows correctly transcribed AND current.

SCOPE
  Requirements M-01..M-61 as extracted 2026-08-18 from manifesto.md. Ten
  repositories. Delivery only: no version bumped, no tag cut, nothing published,
  and the local installs on this machine deliberately untouched.

NOT VERIFIED
  20 rows open, 7 deferred — each with evidence and a proposed mechanism, none
  silently dropped. Nothing is verified against a published artifact, because
  nothing was published. No row was observed in a live session of another agent
  using these skills; every product hypothesis in every member ledger remains
  `unobserved`, which is the honest state and not a defect. The two largest
  findings — ALL-44 (nine repositories share doctrine and nothing checks they
  agree) and ALL-26 (a plant that cannot run is not a verdict) — are filed, not
  fixed, because half a mechanism in one repository is what produced them.
```
