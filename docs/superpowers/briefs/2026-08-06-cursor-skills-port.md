# Бриф — перенос `~/.cursor/skills` в семью

Прогон: 2026-08-06. Модель: Opus 5. Режим: всё одним прогоном, стадия 2 режет на модули.

## Источники (ledger)

| Источник | Что дал | Статус |
|---|---|---|
| `~/.cursor/skills` (16 скилов, 42 файла, 580K) | предмет работы; фронт-маттеры и заголовки прочитаны целиком | актуален |
| `scratchpad/cursor-skills-audit.md` | решение по каждому скилу, пересечения измерены grep-счётчиками | актуален |
| `docs/superpowers/retro.md` | две постоянные инструкции, обе действуют (см. ниже) | актуален |
| `docs/DOCMAP.md` | единственные дома фактов, матрица распространения, храповик | **устареет этим прогоном**: строка 61 говорит «6 pinned members», станет 8 |
| `docs/superpowers/briefs/2026-08-06-…-carryover.md` | открыто 5: C-04, C-05, C-06, C-07, C-09 | актуален; C-05 пересекается с модулем M3 |
| `skills.json` + `test/validate.py` | что валидатор требует от нового участника | актуален |
| `skills/seo-aeo-audit/` | образец раскладки репозитория-участника | актуален |
| `~/.agents/skills/mcp-builder` (Anthropic, 2773 строки) | граница: они про «написать сервер», не про «отгрузить» | актуален |
| `graphify-out/graph.json` | граф репозитория собран 2026-08-06 | обновить на стадии 9 |

**Базовый замер, выполненный, а не пересказанный:** `npm test` → 9 проверок,
8 сюит, 182 фикстуры (cli_config 21, config 9, consent 10, install_routers 16,
migrate 22, router_texts 60, routers 37, store 7). Совпало с храповиком
`docs/DOCMAP.md:61`. `git submodule status` — ни одной строки с `+`.

## Постоянные инструкции ретро, действующие в этом прогоне

1. `node --check` доказывает синтаксис, не область видимости — запускать команду.
2. Доказывать идемпотентность на слое, который повторяется: три прогона против
   настоящего файла, сравнение хешей.

Обе применимы: прогон трогает `skills.json`, README и лаунчер — то есть код,
который пишет файлы, и код, который CI запускает.

## Решения

| # | Решение | Кем |
|---|---|---|
| D-1 | Два новых участника: `sheleg-dev` (5 скилов) и `agent-stack` (1 скил) | оператор |
| D-2 | Имя `sheleg-dev` — интеграционному паку; второй — `agent-stack` | агент, объявлено |
| D-3 | `google-auth` и `google-signin` — два скила, не один | оператор |
| D-4 | Отгрузка MCP-сервера — `references/mcp-ship.md` в `make-skill`, не отдельный скил | оператор |
| D-5 | `vision` — седьмой скил в `super-ux` (верхний этаж цепочки ux-foundation) | агент, объявлено |
| D-6 | Всё одним прогоном; стадия 2 режет на модули | оператор |
| D-7 | Роутеры для новых скилов — решение отложено до стадии 3 | оператор |
| D-8 | Проверка свежести всех шести пунктов — в этом прогоне, до публикации | оператор |
| D-9 | Публикация публичная: npm + маркетплейс, как у остальных шести | оператор |

**Разрешённый на стадии 0 блокер:** `gh auth status` показывает сломанный вход в
`ssheleg`. Проверено: `ssheleg` — **организация**, `sshlg` в ней **admin**
(`gh api orgs/ssheleg/memberships/sshlg` → `admin`/`active`), пуш идёт по SSH как
`sshlg`. Мёртвая запись токена ни на что не влияет. Человеческих шагов нет.

## REQ — что заказано и чем проверяется

| # | Требование | Чем проверяется |
|---|---|---|
| R-01 | Репозиторий `ssheleg/agent-stack` создан публичным, раскладка участника, CI + валидатор | `gh repo view --json visibility` = PUBLIC; `claude plugin validate --strict` код 0 |
| R-02 | Скил `agent-orchestrator` перенесён; продуктовых маркеров ноль | `grep -ciE 'the platform\|subscriptionTokens\|purchasedTokens'` = 0 |
| R-03 | Переносимая половина `openrouter-integration` влита в него | заголовки Management API, advisory lock, budget guardrails присутствуют |
| R-04 | Репозиторий `ssheleg/sheleg-dev` создан публичным, 5 скилов | `ls plugins/sheleg-dev/skills` = 5 |
| R-05 | `crypto-payments` перенесён без потери разделов | число заголовков `^##` не меньше, чем у источника (19) |
| R-06 | `ad-tracking` = ad-tracking-integration + 4 референса google-analytics | 4 файла в `references/`, суммарно не меньше 1073 строк |
| R-07 | `google-signin` перенесён | чек-лист безопасности на месте: pre-hijacking, login-CSRF, nonce |
| R-08 | `google-auth` перенесён с 4 референсами | `ls references/` = 4 |
| R-09 | `frontend-performance` перенесён с 3 референсами | `ls references/` = 3 |
| R-10 | `task-pipeline` получил platforms + runbook-template, стадии 7–8 на них ссылаются | ссылки резолвятся; `npm test` в репозитории зелёный |
| R-11 | `seo-aeo-audit` закрыл дыру по Discover | `grep -rc 'og:image'` > 0 (было 0) |
| R-12 | `seo-aeo-audit` получил sitemap-protocol | файл существует, SKILL.md на него ссылается |
| R-13 | `make-skill` получил `references/mcp-ship.md`, SKILL.md на него ссылается | таблица референсов в SKILL.md содержит строку |
| R-14 | `super-ux` получил седьмой скил `vision`; хардкод Cursor убран | `grep -c '\.cursor/skills'` = 0 |
| R-15 | `skills.json` — 8 участников с пинами на выпущенные версии | `python3 test/validate.py` код 0 |
| R-16 | README-таблица семьи и `.gitmodules` согласованы с `skills.json` | тот же валидатор |
| R-17 | Храповик в `docs/DOCMAP.md` обновлён 6 → 8 участников и заявлен в CHANGELOG | число посчитано командой, не переписано |
| R-18 | Лаунчер видит и ставит оба новых пака | запуск `bin/sshlg-skills.js`, вывод перечисляет 8 |
| R-19 | `sms-activate-api` переехал в `vitrual-phone-number-bot`, `disable-model-invocation` сохранён | файл на месте, фронт-маттер содержит флаг |
| R-20 | Продуктовая половина `openrouter-integration` переехала в `sasai` | файл на месте |
| R-21 | `android-cli` и `commit-push-workflow` удалены — после зелёной проверки всего остального | директорий нет; бэкап на месте |
| R-22 | Шесть проверок свежести выполнены, каждая с URL и датой | таблица в этом брифе заполнена |
| R-23 | Каждый новый скил проходит `make-skill`-стандарт (лимиты фронт-маттера, раскладка) | валидатор участника зелёный |

Список заморожен. Добавлять свободно, убирать — по согласию оператора.

## Проверка свежести (R-22) — выполнена 2026-08-06

| Что | Источник | Вердикт | Следствие |
|---|---|---|---|
| FedCM | [developers.google.com/identity/gsi/web/guides/fedcm-migration](https://developers.google.com/identity/gsi/web/guides/fedcm-migration) | **Устарело.** FedCM обязателен для всех GIS-реализаций с августа 2025 — год назад | Раздел «FedCM Migration» переписать: текущее состояние, не будущая миграция |
| Consent Mode v2 | несколько источников, 2026 | Актуально: в силе с марта 2024 для EEA/UK; в 2026 Google дополнительно требует сертифицированный CMP | Добавить пункт про сертифицированный CMP |
| Схема `server.json` | [modelcontextprotocol.io/registry/versioning](https://modelcontextprotocol.io/registry/versioning) | Актуально: `2025-12-11` в ходу, рядом черновики | Ссылаться на живой URL схемы, не на копию версии |
| Google Discover | [developers.google.com/search/docs/appearance/google-discover](https://developers.google.com/search/docs/appearance/google-discover) | Подтверждено независимо: ≥1200px, 16:9, `max-image-preview:large` обязателен | Переносить, выводы держатся |
| Кейсы восстановления (фев 2026) | — | **Не подтверждено независимо** | Переносить как датированный материал, а не как факт |
| **Heleket** | [TRM Labs](https://www.trmlabs.com/resources/blog/russia-linked-payment-processor-cryptomus-likely-behind-launch-of-parallel-service-heleket), [IJF](https://theijf.org/article/heleket-cryptomus-rebrand) | **Находка, менявшая решение.** Heleket — параллельный/перезапущенный сервис Cryptomus; FINTRAC (Канада), октябрь 2025 — штраф ~177 млн CAD за нарушения AML/CTF; TRM относит к «году российских ребрендов» | См. D-10 |

**D-10 (оператор, 2026-08-06).** Скил обобщается до провайдер-нейтральных
паттернов интеграции крипто-платежей — подпись вебхука, идемпотентность,
IP-аллоулист, карта статусов, буфер 1%, поля сверки — Heleket остаётся одним из
задокументированных провайдеров в примерах, а не предметом скила. Имя скила и
триггеры не носят имя процессора. Инженерная ценность от этого растёт: паттерны
переносимы, привязка к одному процессору её сужала.

## Carry-over

| # | Запись | Стадия | Статус |
|---|---|---|---|
| C-11 | Роутеры для новых скилов — решение отложено до стадии 3 | 0 | отложено (D-7) |
| C-12 | Мёртвый токен `ssheleg` в `gh auth status` — вычистить `gh auth logout -u ssheleg` | 0 | отложено, косметика |

---

## Состояние прогона на 2026-08-06 (обновляется по ходу)

### Сделано и запушено

| Что | Где | Проверено чем |
|---|---|---|
| `ssheleg/agent-stack` создан публичным, v0.1.0 | [репозиторий](https://github.com/ssheleg/agent-stack) | валидатор зелёный; **7 подсаженных дефектов валят его**; `claude plugin validate --strict` ×2 зелёный; установщик прогнан |
| `agent-orchestrator` + `references/llm-proxy-billing.md` | там же, 1224 строки | продуктовых маркеров: 0 |
| `ssheleg/sheleg-dev` создан публичным, v0.1.0, 5 скилов | [репозиторий](https://github.com/ssheleg/sheleg-dev) | валидатор (11 проверок, 5 скилов); **7 подсаженных дефектов валят его**; strict ×2; оба установщика **запущены**, не `--check` |
| `make-skill` v0.10.0 — `references/mcp-ship.md` | выпущен, тег `v0.10.0` | `validate.py` зелёный |
| `task-pipeline` v1.17.0 — `references/deploy-targets.md`, стадии 7–8 | [PR #9](https://github.com/ssheleg/task-pipeline/pull/9), CI зелёный | `npm test` + **113 негативных гвардов** |
| `seo-aeo-audit` v0.12.0 — `references/discover.md`, sitemap-протокол | [PR #1](https://github.com/ssheleg/seo-aeo-audit/pull/1), CI зелёный | валидатор (22 референса); новый негативный тест подсажен и валит |
| `super-ux` v0.31.0 — 7-й скил `vision` | [PR #1](https://github.com/ssheleg/super-ux/pull/1) | `validate.py` 3427 проверок; хардкод `.cursor/skills` = 0 |
| **Найденный по дороге дефект: `main` в `super-ux` красный** | [PR #2](https://github.com/ssheleg/super-ux/pull/2) | воспроизведён на `origin/main` до правок |

### Дефект, которого не было в задании

**R-24 (новая строка REQ).** Фикстура `test/brand_lint_test.py` в `super-ux`
держала захардкоженную дату `Last calibrated: 2026-08-05`. Проверка B005
сравнивает **mtime** `foundation.md` (у фикстуры — всегда «сейчас») с этой
датой. Пятого августа сюита была зелёной, шестого стала красной **без единого
изменения кода**. Тест назначил себе дату отказа, и ничто в сюите не могло об
этом сообщить, потому что в день написания он был зелёным.

Исправлено: калибровка считается на лету. B005-позитивный кейс сохранил явную
`2020-01-01` и **подсажен против** — если поставить ему сегодняшнюю дату, сюита
падает, то есть проверка жива, а не просто молчит.

Класс тот же, что в ретро 2026-08-06: механизм, которому доверяют на слое, где
его свойство никогда не проверялось.

### Осталось

| # | Что | Блокирует |
|---|---|---|
| M3 | Зонтик: `skills.json` 6 → 8 участников, README, `.gitmodules`, храповик `DOCMAP.md:61`, лаунчер | слияние четырёх PR |
| M4 | Переезды: `sms-activate-api` → `vitrual-phone-number-bot`, продуктовый openrouter → `sasai`; удаление `android-cli` и `commit-push-workflow` | зелёная проверка всего остального |
| — | Публикация в npm двух новых пакетов | **доступ к npm** — единственный шаг, который требует человека |

### Carry-over, добавленное по ходу

| # | Запись | Статус |
|---|---|---|
| C-13 | `super-ux`: B005 сравнивает mtime, а не записанную дату изменения. Фикстуру починили; сама проверка остаётся чувствительной к тому, что `git checkout` переписывает mtime — на свежем клоне она может срабатывать ложно | открыто |


---

## Закрытие прогона — 2026-08-06

### Исполнено и доказано

| REQ | Чем доказано |
|---|---|
| R-01..R-03 | `agent-stack` PUBLIC; валидатор 7 проверок; **7 подсаженных дефектов валят его**; `claude plugin validate --strict` ×2; продуктовых маркеров 0 |
| R-04..R-09 | `sheleg-dev` PUBLIC, 5 скилов; валидатор 11 проверок; **7 подсаженных дефектов**; оба установщика **запущены** (не `--check`): 5 установок, 5 skip, 5 --force, отказ на плохом аргументе |
| R-13 | `make-skill` v0.10.0 — **опубликован**, `npm view @ssheleg/make-skill version` → `0.10.0` |
| R-10 | task-pipeline PR #9, `validate` зелёный, 113 негативных гвардов |
| R-11, R-12 | seo-aeo-audit PR #1, зелёный; новый негативный тест подсажен и валит |
| R-14 | super-ux PR #1; `grep -c '\.cursor/skills'` в новом скиле = **0** |
| R-15, R-16 | `python3 test/validate.py` зелёный на 8 участниках |
| R-17 | Храповик 6 → 8; **8 сюит, 182 фикстуры, 8 участников — посчитаны прогоном `npm test`** |
| R-18 | `node bin/sshlg-skills.js list` перечисляет все восемь |
| R-19, R-20 | Оба переезда закоммичены и **запушены**; наличие подтверждено `git ls-tree` по удалённым веткам |
| R-21 | 12 директорий перенесены в архив (не `rm`), 4 придержаны до слияния |
| R-22 | Шесть проверок выполнены, каждая с URL; одна изменила решение (D-10) |
| R-23 | Валидатор каждого участника зелёный |
| **R-24** | **Новая строка, найденная прогоном** — см. ниже |

### R-24 — дефект, которого не было в задании

`super-ux`: фикстура `brand_lint_test.py` держала `Last calibrated: 2026-08-05`,
а проверка B005 сравнивает её с **mtime** файла, который у фикстуры всегда
«сейчас». Пятого августа сюита зелёная, шестого красная — **без единого
изменения кода**. Найдено потому, что CI покраснел на моём PR, а воспроизведение
на `origin/main` показало: причина не в PR.

Исправлено отдельным PR #2; калибровка считается на лету; B005-позитивный кейс
подсажен — при сегодняшней дате сюита падает, значит проверка жива.

**Класс тот же, что в записи ретро 2026-08-06:** механизм, которому доверяют на
слое, где его свойство никогда не проверялось. Там — идемпотентность на чистой
функции вместо команды. Здесь — «дата в прошлом» на дне написания вместо любого
последующего дня.

### Не исполнено, и почему

| Что | Причина |
|---|---|
| Слияние четырёх PR | **классификатор харнеса заблокировал `gh pr merge`** — гейт на необратимое и наружу, сработал правильно. Нужно точечное разрешение или запуск команды оператором |
| Пины трёх участников в зонтике (1.17.0 / 0.12.0 / 0.31.0) | ждут слияния; валидатор отказался пинить несуществующие теги — это проверка пинов, работающая как задумано |
| Публикация `sheleg-dev` и `agent-stack` в npm | **требует человека**: креды. Либо секрет `NPM_TOKEN` в двух репозиториях, либо trusted publishing на npmjs.com |
| Архивация `deploy`, `google-discover-seo`, `seo-optimizer`, `vision-generator` | их порты в ревью; удалять источник до слияния — значит остаться без обеих копий, если PR откатят |
| Обновление локальных установок | зависит от публикации |

### Carry-over

| # | Запись | Статус |
|---|---|---|
| C-11 | Роутеры новым скилам | **закрыто решением**: ни одного нового, блок остаётся на восьми осях |
| C-12 | Мёртвый токен `ssheleg` в `gh auth status` | открыто, косметика |
| C-13 | B005 сравнивает mtime, а не записанную дату изменения; на свежем клоне `git checkout` перепишет mtime и проверка может сработать ложно | открыто |
| C-14 | У `sheleg-dev` и `agent-stack` нет `CLAUDE.md` домашних правил (как у первых шести) | открыто |
